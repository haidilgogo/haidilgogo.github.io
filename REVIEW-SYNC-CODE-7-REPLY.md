# 코덱스 7차 검증 회신 (원문 보관) — 2026-07-31 · **코드 검토 종료 판정**

> 🔴 **코덱스가 보낸 회신을 그대로 옮긴 것이다.** 클로드가 요약하거나 판단을 섞지 않는다.
> 대상: `1bd0933`(브랜치 `fix/sync-review4`, script v533) / 요청서: `REVIEW-SYNC-CODE-7.md`

---

**7차 코드 검증 결과 새로 발견된 배포 차단 결함은 없습니다.** 6차 지적 2건은 의도대로 수정됐습니다. **이제 반복적인 코드 검토는 종료하고, 실기기 Safari ↔ 홈 화면 복구만 확인한 뒤 배포해도 되는 상태로 판단합니다.**

## 판정표

| 항목 | 판정 |
|---|---|
| 6차 발견 1 — 열린 시트의 옛 코드 | 수정 완료 |
| 6차 발견 2 — 삭제된 기록 수정 시 입력 유실 | 수정 완료 |
| D1 데이터 보존 | 통과 |
| D3 즐겨찾기·좋아요 해제 유지 | 통과 |
| D5 Firebase 실패 격리 | 통과 |
| `renderSyncCode()` 통합 | 안전 |
| 삭제 후 새 기록 저장 | 사용자가 선택한 정책대로 안전하게 구현 |
| 새 기록 중복 가능성 | 존재하지만 **허용 가능한 드문 충돌** |
| 열린 수정 시트의 원격 수정 덮어쓰기 | 기존 **최종 저장자 우선** 정책의 한계, **배포 차단 아님** |
| 추가 코드 수정 필요성 | **없음** |
| 배포 판단 | **실기기 복구 확인 후 가능** |

## 6차 발견 1 재검증 — 수정 완료

- 위치: `script.js` 3838행 · 4393행 · 4564행 / 심각도: 해결됨
- 수정 내용: `renderSyncCode()`가 현재 `getSyncCode()`를 읽어 코드 글자와 `syncMine.hidden`을 함께 갱신한다. 적용 경로 = 시트를 열 때 / 불러오기 성공 / 다른 탭의 코드 변경을 `storage`로 받았을 때.
- 재현 경로 판정: 탭2가 B를 불러오면 탭1의 `syncExternalState()`가 `syncCodeMem`을 B로 바꾸고 **바로 이어서** `renderSyncCode()`가 열린 시트도 B로 바꾼다. 복사·보내기도 `getSyncCode()`로 B를 읽는다. **이전처럼 화면 A와 복사 B가 지속적으로 어긋나는 경로는 없어졌다.**
- **시트가 닫혀 있을 때 호출되는 것도 무해하다.** 숨겨진 요소의 글자와 표시 상태만 갱신하며, 다음에 시트를 열 때 다시 한번 그린다.
- **코드가 조용히 바뀌는 동작도 현재는 적절하다.** 옛 코드를 계속 보여주는 것보다 이 기기가 실제로 사용하는 코드를 즉시 보여주는 것이 우선이다. 종이에 적은 옛 코드는 기존 서버 자료를 가리킬 수 있지만 이후 변경 사항은 새 코드에 저장된다. 이를 별도 경고로 안내하려면 새로운 문구와 상태 관리가 필요하며, 현재 수정 범위에서는 추가할 필요가 없다.
- **복사 경쟁 조건도 배포 차단 문제가 아니다.** 클릭 처리 중에는 자바스크립트가 중간에 다른 `storage` 이벤트를 실행하지 않으며, 복사 버튼은 클릭 순간의 코드를 변수에 담아 `writeText()`에 전달한다. 클립보드 Promise가 끝나기 전에 코드가 다시 바뀌는 극히 짧은 경우는 가능하지만, 이때도 **사용자가 클릭할 때 화면에 보였던 값이 복사된다.**
- **불러오기 성공 순서**도 `setSyncCode(code)` 다음에 `renderSyncCode()`가 호출되도록 현재 코드에서 명확히 보장된다.

## 6차 발견 2 재검증 — 수정 완료

- 위치: `script.js` 3551행 / 심각도: 해결됨
- 수정 내용: 원래 ID를 찾지 못하면 현재 입력 내용을 **새 ID와 새 `addedAt`**을 가진 기록으로 저장한다.
- 재현 경로 판정: 삭제 동기화 후 `find()` 결과가 없어 새 기록 분기로 진입하고, **`st1` tombstone은 그대로 유지**된다. 동기화에서는 삭제된 `st1`만 제외되고 새 ID의 기록은 정상 보존된다. **지운 원본이 되살아나는 것이 아니라, 방금 작성한 내용이 별도의 새 기록으로 남는다. 선택한 방향과 정확히 일치한다.**

### 부작용 판정

- 여러 기기가 같은 수정 시트를 열어둔 상태에서 원본을 삭제한 뒤 각각 저장하면 **비슷한 새 기록이 둘 생길 수 있다. 실제로 가능한 조합이다.** 하지만 이를 막으려면 충돌 안내, 편집 세션 ID 또는 추가 확인 화면이 필요하다. **입력 유실을 피한다는 현재 우선순위에서는 허용 가능한 결과다.**
- 새 기록은 같은 날짜 안에서 위로 이동한다. `addedAt=현재 시각`이므로 **정렬 규칙상 정상**이다.
- 옛 `addedAt`은 수정 시트를 열 때 별도 변수로 기억해두는 방식으로 기술적으로 보존할 수는 있다. 다만 **이번 정책은 원본 복원이 아니라 새 기록 생성이므로 현재 시각을 사용하는 편이 의미상 맞다.**
- `editedAt`이 없어도 `touchedAt`은 새 `addedAt`을 사용하므로 **병합 비교에 문제가 없다.**
- 존재하지 않는 옛 ID를 `Map.delete()`로 지우는 것은 **안전**하다. 새 기록 카드는 `renderStamps()`에서 새 ID로 생성된다.
- 평소 수정에서는 원래 ID가 존재하므로 기존 기록만 수정되고 `editedAt`만 갱신된다. **코드상 ID가 임의로 바뀌는 다른 경로는 확인되지 않았다.**

## 남아 있는 위험 판정

- 자동 재시도 부재, tombstone 누적, 구버전 즐겨찾기·좋아요 이력 문제는 **이미 합의된 잔여 한계이며 이번 수정으로 악화되지 않았다.**
- **수정 시트가 열린 동안 같은 기록이 다른 기기에서 수정되면**, 현재 사용자가 저장할 때 그 내용을 덮을 수 있다. 이는 같은 ID의 동시 편집에서 **마지막 저장이 이기는 현재 정책**이다. 자동으로 시트에 반영하면 사용자가 입력 중인 글을 지울 수 있고, 충돌 확인을 추가하면 기능이 상당히 무거워진다. 발생 빈도와 구현 부담을 고려하면 **지금은 그대로 두는 것이 적절하며 배포 차단 사유가 아니다.**

## 검증 상태

브랜치 `fix/sync-review4` / 원격과 동기화 / 작업 트리 깨끗 / HEAD `00c5c4b` / 검증 대상 `1bd0933` / 현재 `script.js`와 `1bd0933`의 `script.js` 동일 / 캐시 버전 `script.js?v=533` / `node --check` 통과 / `git diff --check` 통과 / 저장소 파일 수정 없음.

🔴 **읽기 전용 지시를 지키기 위해 Firebase 스텁 임시 페이지나 테스트 파일은 만들지 않았고, 라이브 Firebase도 조작하지 않았다. 따라서 Claude가 수행한 브라우저 재현 결과를 독립적으로 반복한 것은 아니며, 이번 판정은 실제 코드 경로와 정적 검사에 근거한다.**

**최종 결론: 코드에서 더 확장해 볼 필요는 없다. 실기기에서 Safari의 데이터를 홈 화면 앱이 코드로 정상 복구하는지만 확인하고, 통과하면 배포해도 된다.**

---

## 클로드 주 (2026-07-31)

⚠️ **이번 회차는 코덱스가 실행 검증을 하지 않았다**(위 굵은 문단). 6차까지와 달리 **정적 검사·코드 경로 판독만**이다. 실행으로 확인된 것은 클로드가 v532↔v533 사본을 나란히 띄워 돌린 결과뿐이고, 그건 `REVIEW-SYNC-CODE-7.md` 4절에 있다. **"두 사람이 각자 돌려봤다"고 착각하지 말 것.**

→ 그래서 남은 **실기기 확인이 더 중요해졌다.** 코드 리뷰로는 안 나오는 것(iOS 저장소 분리·타이밍)이 거기 있다.
