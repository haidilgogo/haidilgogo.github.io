# 코덱스 5차 검증 회신 (원문 보관) — 2026-07-31

> 🔴 **이 문서는 코덱스가 보낸 회신을 그대로 옮긴 것이다.** 클로드가 요약하거나 판단을 섞지 않는다.
> 남기는 이유: 1차 때 회신을 안 남겨서 **"7건 중 6건이 사실"이라고만 적혀 있고 어느 1건이 기각됐는지 알 수 없게 됐다.** 같은 일을 반복하지 않으려고 5차부터 원문을 보관한다.
> 대상: `4096316`(브랜치 `fix/sync-review4`, script v531) / 요청서: `REVIEW-SYNC-CODE-5.md`

---

## 판정 요약

**깨끗한 통과가 아니다.** 4차 지적 3건은 제대로 수정됐지만 **추가 결함 3건**을 확인했다. 파일은 수정하지 않았다.

### 요청서 항목 판정

| 항목 | 판정 |
|---|---|
| 4차 ① 빈 서버에서도 업로드 | 수정 완료 |
| 4차 ② 코드 변경 뒤 A의 늦은 응답 차단 | **한 탭 기준** 수정 완료 |
| 4차 ③ 변경된 카드만 캐시 삭제 | 수정 완료 |
| D1 데이터 보존 | 통과 |
| D3 해제 유지 | 통과 |
| D5 Firebase 실패 격리 | 통과 |
| **R2-a** 전환 중 A 데이터 유입 | **(가) 설계상 정상으로 판단한다.** B 불러오기가 성공하기 전까지 현재 코드는 A이고, 불러오기는 합치기이므로 A 동기화가 조금 먼저 끝난 것과 같은 결과다. 별도 전환 상태를 추가할 이익보다 복잡성이 크다 |
| `recFingerprint` | 현재 기록이 평면 구조이므로 키 순서와 `undefined` 처리 모두 문제없다 |

구문 검사, diff 검사와 합치기·삭제 tombstone·해제 이력·평면 기록 지문 모의 검증은 모두 통과했다.

또한 Firebase 공식 문서도 **트랜잭션의 최초 값이 원격 데이터가 있어도 `null`일 수 있으며, 충돌 시 최신 서버 값으로 콜백을 다시 실행**한다고 설명한다. 따라서 읽기 실패 뒤 시작한 트랜잭션이 원격 데이터를 단순히 빈 값으로 덮지는 않는다.
([트랜잭션 설명](https://firebase.google.com/docs/database/web/read-and-write) · [JavaScript Reference](https://firebase.google.com/docs/reference/js/v8/firebase.database.Reference#transaction))

---

## 발견한 문제 3건

### 1. 여러 탭에서 코드가 갈라져 두 코드 서버가 다시 섞일 수 있음 — 심각도: **보통**

- 위치: `script.js` 3802행(`storage` 이벤트), 4016행(`getSyncCode`)
- 증상: 한 탭은 코드 A, 다른 탭은 코드 B라고 인식할 수 있다.
- 원인: `storage` 이벤트가 `SYNC_CODE_KEY`를 처리하지 않고, 이미 코드가 메모리에 있으면 `localStorage`보다 `syncCodeMem`을 계속 우선한다.
- 재현:
  1. 탭 A에서 코드 A를 불러온다.
  2. 탭 B에서 코드 B를 불러온다.
  3. 탭 B의 데이터 변경은 공유 `localStorage`를 통해 탭 A에도 반영되지만, 탭 A의 코드는 계속 A다.
  4. 탭 A에서 이후 무엇이든 저장하면 **B가 섞인 데이터를 A 서버에 올릴 수 있다.**
- 결과적으로 사용자 의도와 달리 A·B 두 코드가 계속 살아서 합쳐질 수 있다.

### 2. 동기화 중 열려 있던 스티커 상세가 옛 기록을 계속 표시함 — 심각도: **보통**

- 위치: `script.js` 4156행(`applyPayload`), 4256행(`refreshAfterSync`)
- 증상: 목록은 새 내용으로 바뀌지만 **이미 열린 상세 화면은 옛 매장·날짜·메모**를 보여준다.
- 원인: `applyPayload()`는 목록 카드 캐시만 무효화하고, `refreshAfterSync()`는 열린 `stampViewOverlay`를 갱신하거나 닫지 않는다.
- 재현:
  1. 앱 시작 직후 기록 상세를 연다.
  2. 늦게 도착한 시작 동기화에 같은 기록의 수정 또는 삭제가 포함되게 한다.
  3. 뒤쪽 목록은 갱신되지만 상세는 옛 상태로 남는다.
  4. **삭제된 기록에서 `수정`을 누르면** 해당 id를 찾지 못해 3374행의 **새 기록 모드로 열릴 수 있다.**

### 3. 불러오기 네트워크 오류가 '저장된 데이터 없음'으로 표시됨 — 심각도: **낮음**

- 위치: `script.js` 4250행(`pullSync`), 4485행(불러오기 핸들러)
- 원인: `pullSync()`가 **모든 읽기 실패를 `null`로 바꾸고**, 수동 불러오기에서는 이를 빈 서버로 해석한다.
- 재현: Firebase 연결을 차단하고 실제 데이터가 있는 코드를 불러오면 "연결이 안 돼요"가 아니라 **"저장된 데이터가 없어요"**가 표시된다.
- 데이터 유실은 없지만 사용자가 **정상 코드를 틀린 코드로 오해**할 수 있다.

### 그 밖에

- `HANDOVER.md` 16행이 아직 "4차 지적 미수정" 상태로 적혀 있어 현재 작업 브랜치와 어긋난다.
  → ⚠️ **클로드 주: 이건 코덱스가 `5ddfe2a` 시점을 본 것이다. 그 뒤 `cef20d4`로 갱신했다.**

---

## 클로드 확인 결과 (2026-07-31, 회신 받은 뒤)

**3건 모두 실제로 재현했다.** 방법은 요청서 4절과 같다(Firebase CDN 두 줄만 가짜 스텁으로 교체, 앱 코드는 안 건드림).

| 건 | 재현 결과 |
|---|---|
| 1 | 탭1에서 A 불러오기 → 탭2에서 B 불러오기 → 탭1에서 즐겨찾기 한 번 → **저장된 코드는 B인데 전송은 A로 3회**, B의 기록(홍대점)이 **A 서버에 올라갔다** |
| 2 | 상세를 연 채 수정본 도착 → **목록 = 홍대점/7월25일, 상세 = 강남점/7월20일**. 삭제본 도착 → 목록은 "아직 스티커가 없어요"인데 **상세는 열린 채**, `수정`을 누르니 **날짜칸이 오늘로 채워진 새 기록 모드**로 열렸다 |
| 3 | 읽기를 실패시키고 불러오기 → **"입력하신 코드로 저장된 데이터가 없어요"** (원인은 네트워크 오류) |

🔴 **세 건 모두 v531이 만든 회귀가 아니다.** v531의 diff는 `syncOnStart`·`applyPayload` 두 곳뿐이고, 세 건이 사는 곳(`syncExternalState`·`refreshAfterSync`·`pullSync`·불러오기 핸들러)은 **손대지 않았다.** 즉 **셋 다 지금 라이브(v530)에도 있는 문제**이고, v531은 라이브보다 나쁘지 않다.
