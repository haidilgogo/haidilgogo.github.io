# 「내 데이터 코드」 3차 교차 검증 요청서 — 구조 변경분 재검증

> **읽는 사람: 코덱스(또는 다른 검토자).**
> 🔴 **저장소 파일을 읽기만 하고 수정하지 마세요.**
> 발견한 것은 **파일:줄번호 + 왜 문제인지 + 재현 방법 + 심각도**로 정리해 주세요.
>
> 작성 2026-07-31 / 대상 커밋 `77fea44`(main, 배포 완료) / 캐시버전 css v624 · **script v529**
> 앞선 요청서: `REVIEW-SYNC-CODE.md`(1차, **불변식 D1~D8 정의**) · `REVIEW-SYNC-CODE-2.md`(2차)

---

## 0. 2차 검토 결과 — 또 맞았다

2차 지적 6건 중 **5건을 고쳤고 1건은 의도적으로 남겼다.**

특히 ①「동시에 읽으면 여전히 마지막 쓰기가 이긴다」는 **1차 수정 방식 자체의 한계**를 정확히 짚은 것이었다. 읽고→합치고→쓰기는 그 사이가 원자적이지 않다. 그래서 이번엔 구조를 바꿨다.

**⑤(구버전에서 이력 없이 꺼진 즐겨찾기가 되살아남)는 고치지 않기로 했다.** 목록에도 없고 이력에도 없는 상태는 "껐다"와 "원래 없었다"를 구분할 정보가 없다. 구버전 자료에 한정되고 결과도 즐겨찾기가 하나 켜지는 정도(다시 끄면 끝)라, **잃는 쪽이 아니라 생기는 쪽**이므로 감수한다. 이 판단이 틀렸다고 보면 근거를 적어 달라.

---

## 1. 이번에 바뀐 구조 (재검증 대상)

| # | 2차 지적 | 수정 |
|---|---|---|
| ① | 동시 읽기 후 마지막 쓰기가 이김 | **Firebase `transaction()`** 으로 교체. 충돌 시 서버 최신값으로 콜백 재실행 |
| ③ | 쓰기 실패 시 화면·저장소 어긋남 | 합치기를 **순수 계산**(`mergePayloads`)으로 분리하고, **커밋된 뒤에만** `applyPayload`로 로컬 반영 |
| ② | 저장소 옛 코드가 메모리 새 코드보다 우선 | `getSyncCode`가 `syncCodeMem`을 먼저 본다 |
| ④ | 동일 시각이면 수렴하지 않음 | 스티커는 동점 시 `JSON.stringify` 큰 쪽, 이력은 동점 시 **끈 쪽(v:0)** 으로 통일 |
| ⑥ | 7자 이상 조용히 잘림 | `extractCode`의 앞 6자 절단 제거, 문장 추출은 뒤에 코드 글자가 없을 때만 |

**핵심 함수(전부 `script.js` 맨 아래 절):**
`marksOf` · `mergeMarkMaps` · `mergePayloads`(순수) · `setsFromMarks` · `applyPayload` · `mergeIntoLocal`(= apply∘merge) · `pushSync`(트랜잭션)

---

## 2. 🔴 이번 구조 변경이 새로 만들었을 위험 (이번 검토의 핵심)

앞선 두 번보다 **바뀐 폭이 크다.** 새 구멍을 찾아 달라.

### R1. 트랜잭션 콜백의 순수성
`ref.transaction((remote) => mergePayloads(mine, remote))`

- 콜백은 **여러 번, 그리고 `null`로도** 불린다. `mergePayloads`가 정말 부작용이 없는가? 바깥 상태(`stampData`·`favorites`·`favMarks`…)를 읽거나 쓰는 경로가 남아 있는가?
- `mine`은 트랜잭션 **전에 한 번** 찍은 스냅샷이다. 재시도가 여러 번 일어나는 동안 사용자가 새로 저장하면 그 변경은 이번 트랜잭션에 안 들어간다 — `pendingPush`로 반드시 이어지는가?
- `mergePayloads` 안의 `Date.now()`(updatedAt)는 재시도마다 달라진다. 이것이 트랜잭션 수렴을 방해하거나 무한 재시도를 만드는가?
- 트랜잭션 실패(재시도 한도 초과·권한 거부)는 어떤 경로로 끝나는가? `done()`이 반드시 불리는가?

### R2. 커밋 후 반영(`applyPayload`)의 부작용
- `applyPayload`가 `stampData.records`를 **통째로 새 배열로 교체**한다. 이때 `stampCardCache`(id→DOM 노드)가 **옛 내용을 그대로 들고 있는** 경우가 있는가? 합치기로 같은 id의 내용이 바뀌면 화면이 옛 메모를 보여줄 수 있다.
- `applyPayload`는 **스티커를 localStorage에 저장하지 않는다**(이력만 저장). 저장은 뒤따르는 `refreshAfterSync()`의 `saveStamps()`에 의존한다. `changed`가 false인데 실제로는 바뀐 조합이 있는가? 그러면 새로고침 시 되돌아간다.
- `favorites`/`likedByMe`가 이제 **이력(marks)에서만** 만들어진다. 이력이 비었거나 깨진 상태(`loadMarks`가 JSON 파싱에 실패해 `{}`)에서 이 경로를 타면 즐겨찾기가 사라지는가?

### R3. 재진입과 반복
`pushSync` → `refreshAfterSync()` → `save*` → `schedulePush()` → `pushSync`

- `pushing`/`pendingPush`로 끊었는데, 트랜잭션 재시도가 섞이면 **끝없는 왕복**이 가능한가?
- `refreshAfterSync`가 `save*`를 부르는데, 그 안의 `updateMarks`가 이력에 **새 시각을 찍는다.** 방금 합쳐 들어온 상태에 로컬 시각을 덧씌워, 다음 합치기에서 다른 기기 것을 이기게 만드는 경로가 있는가? (해제가 되살아나는 형태로 나타날 수 있다)

### R4. 동점 규칙의 타당성
- 스티커: 동점이면 `JSON.stringify(r) > JSON.stringify(old)`. 두 기기가 **같은 답**에 도달하는가? 키 순서가 기기마다 다를 수 있는 객체인가?
- 이력: 동점이면 **끈 쪽**을 남긴다. 이 때문에 **정상적으로 켠 것이 지워지는** 조합이 있는가? (특히 `t:0`끼리 만나는 구버전 자료)

### R5. `syncCodeMem` 우선 처리
- 메모리가 먼저이므로, **같은 페이지에서 코드를 두 번 바꾸는** 흐름에서 어긋남이 있는가?
- 저장소 쓰기는 성공했는데 값이 다른(외부에서 지워짐 등) 경우는?

### R6. 입력 처리
- `extractCode`가 최대 20자를 남긴다. 아주 긴 글을 붙여넣으면 입력칸에 20자가 남는데, 사용자가 이해할 수 있는 상태인가?
- 문장 추출 정규식 `HG[-\s]?([A-Z0-9]{6})(?![A-Z0-9])`이 놓치거나 잘못 잡는 실제 입력이 있는가?

---

## 3. 다시 판정해 줄 것

**D1(데이터 보존)** — 2차에서 실패였다. 트랜잭션 도입 후 어떤가? 특히 **동시 쓰기**와 **쓰기 실패** 두 경로.
**D3(해제 유지)** — 동점 규칙이 바뀌었다. 여전히 유지되는가?
**D5(Firebase 실패 격리)** — 트랜잭션 실패가 저장·기록 흐름을 멈추지 않는가?

---

## 4. 검증 시 주의 (앞 문서와 동일 — 실제로 겪은 함정)

1. 🔴 테스트용 코드에 **`O`·`I`·`0`·`1`을 쓰지 말 것.** 형식 검사에서 거부된다. **`A-H · J-N · P-Z · 2-9`** 로만(예: `HG-TESTA2`).
2. 🔴 붙여넣기는 **`input.value` 직접 대입으로 테스트하지 말 것.** 실제 경로(`document.execCommand('insertText', …)`)를 쓸 것.
3. ⚠️ **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 실제로 바뀐다. 눌렀으면 되돌리고 숫자 확인.
4. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것.
5. ⚠️ 브라우저 콘솔의 타이머가 늘어질 수 있으니 타이밍 항목은 넉넉히 기다릴 것.

---

## 5. 아직 아무도 확인 못 한 것

⬜ **실기기에서 사파리 → 홈 화면 복구.** 이 기능을 만든 이유인데 사용자가 아직 확인하지 못했다. 코드 리뷰로는 답이 안 나온다.

---

## 6. 참고
1차 요청서에 **기능의 목적·서버 구조·보안 규칙·의도된 동작 목록**이 있다. 앱 전체 규칙은 `CLAUDE.md`.
🔴 이 문서도 내부 문서다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 했다.
