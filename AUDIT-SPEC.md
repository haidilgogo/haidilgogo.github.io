# 하딜고고 전수조사 기준서 (공통)

> **클로드와 코덱스가 이 문서 하나만 보고 각자 독립으로 조사한다.**
> 🔴 **서로의 보고서를 보기 전에** 자기 보고서를 끝낼 것. 그래야 대조에 뜻이 있다.
> 🔴 **저장소 파일은 읽기만 하고 수정하지 않는다.**
>
> 작성 2026-07-31 / 대상 커밋 `3c28840`(main, 배포 완료) / 캐시버전 css v624 · script v530
> 결과 파일: 클로드 → `AUDIT-CLAUDE.md` · 코덱스 → `AUDIT-CODEX.md`

---

## 1. 왜 하는가

「내 데이터 코드」를 만들며 교차 검증을 세 번 했고, **세 번 다 실제 결함이 나왔다.** 매번 "고친 수정이 새 구멍을 만드는" 형태였다. 그래서 이번엔 **그 기능만이 아니라 앱 전체**를 같은 잣대로 훑는다.

목적은 **점수 매기기가 아니라 놓친 것 찾기**다. 두 조사가 겹치면 확신이 생기고, 갈리면 거기가 진짜 애매한 지점이다.

---

## 2. 조사 범위

라이브에 올라가 있는 것 전부. 아래 구역으로 나눠 **각 구역을 반드시 한 번은 훑는다.**

| 구역 | 내용 |
|---|---|
| **A. 데이터·저장** | `localStorage` 7개 키, Firebase `likes`·`sync`, 저장 실패·손상값·용량 초과 |
| **B. 내 데이터 코드** | 코드 생성·입력·트랜잭션·합치기·화면(띠·시트) |
| **C. 스티커 탭** | 기록 추가/수정/삭제, 목록·상세, 스티커 그림 매칭 |
| **D. 레시피 화면** | 홈·전체보기·검색·즐겨찾기·좋아요·정렬·순위 배지·상세 모달 |
| **E. 스토리 뷰어** | 셀럽 이동·자동재생·탭 판정·닫기 |
| **F. 매장·기타 탭** | 매장 목록, 메뉴(빈 화면), 가챠, 배너·칼럼 패널 |
| **G. 전역** | 탭 전환, 스크롤 잠금, 캐시 버전, 공유, 스플래시, 정적 아티클 페이지(guide/hotpot/cilantro) |

**범위 밖(지적하지 말 것)**
- 디자인 취향(색·간격·문구 어감)
- `REVIEW-SYNC-CODE.md` 3절의 **「의도된 동작」** 목록 — 다만 그 판단 자체가 틀렸다고 보면 근거를 적어 별도 표시
- 레시피 내용(재료·설명 값)의 사실 여부
- Firebase 콘솔 설정(저장소 밖)
- 성능 최적화 제안(실제 고장이 아닌 것)

---

## 3. 심각도 기준 (양쪽 동일하게 쓸 것)

| 등급 | 뜻 |
|---|---|
| **S1** | **데이터 유실·노출.** 사용자가 만든 것이 사라지거나 남에게 보인다 |
| **S2** | **기능 고장.** 눌러도 안 되거나 잘못된 결과가 나온다 |
| **S3** | **오작동·혼란.** 동작은 하지만 화면과 실제가 다르거나 오해를 부른다 |
| **S4** | **사소.** 낭비·죽은 코드·드문 조건에서만 어색함 |

---

## 4. 보고 형식 (반드시 이대로)

각 발견 항목:

```
### [S1] 한 줄 요약
- 파일: script.js:1234 (여러 곳이면 모두)
- 증상: 무엇이 잘못되는가
- 원인: 왜 그렇게 되는가
- 재현: 1) … 2) … 3) …
- 확인: 실제로 돌려봤는가 / 코드만 읽고 판단했는가
```

🔴 **재현 경로 없이 "~일 수 있습니다"만 적지 말 것.** 근거가 추정뿐이면 항목 끝에 `(추정)`을 붙이고 심각도를 한 단계 낮춘다.
🔴 **문제를 못 찾은 구역은 "없음"이라고 적을 것.** 빈칸으로 두지 말 것 — 안 본 것과 봤는데 없는 것은 다르다.
🔴 **억지로 만들지 말 것.** 전 구역 "없음"이어도 된다.

마지막에 구역별 요약표(구역 / 발견 수 / 최고 심각도 / 훑은 근거)를 붙인다.

---

## 5. 검증 시 주의 (실제로 당한 것들)

1. 🔴 테스트용 코드에 **`O`·`I`·`0`·`1`을 쓰지 말 것.** 형식 검사에서 거부된다. **`A-H · J-N · P-Z · 2-9`** 만(예: `HG-TESTA2`).
2. 🔴 붙여넣기는 **`input.value` 직접 대입으로 테스트하지 말 것.** `maxlength`·정리 로직을 우회해 **없는 버그를 없다고 판정하게 된다.** 실제 경로(`document.execCommand('insertText', …)`)를 쓸 것.
3. ⚠️ **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 실제로 바뀐다. 눌렀으면 되돌리고 숫자를 확인할 것.
4. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것(규칙상 통째 삭제는 막혀 있다).
5. ⚠️ 브라우저 콘솔은 배경에서 **타이머가 늘어진다.** 타이밍 항목은 넉넉히 기다리고, 시간 측정값은 믿지 말 것.
6. ⚠️ `localStorage`만 비우고 새로고침을 안 하면 **화면이 옛 상태를 그대로 들고 있다.** 상태를 바꿨으면 새로고침할 것 — 이것 때문에 없는 버그를 봤다고 오판한 적이 있다.
7. ⚠️ 화면에 뭐가 보인다/안 보인다를 말하기 전에 **그 클래스를 붙이는 코드가 있는지 grep**할 것. CSS 규칙이 있다고 화면에 나오는 게 아니다.

---

## 6. 배경 문서

- `REVIEW-SYNC-CODE.md` — 기능 목적·서버 구조·보안 규칙·**불변식 D1~D8**·의도된 동작
- `REVIEW-SYNC-CODE-2.md` / `-3.md` — 1·2차 수정 내역
- `CLAUDE.md` — 앱 전체 규칙(배포·스크롤바·이미지 규격·데이터 표기)
- `HANDOVER.md` — 진행 상황

---

## 7. 이후 절차

1. 클로드·코덱스가 **각자** 조사해 `AUDIT-CLAUDE.md` / `AUDIT-CODEX.md`를 낸다.
2. 둘 다 나온 뒤 **코덱스가 대조**한다: 둘 다 발견 / 클로드만 / 코덱스만 / 판단이 갈린 것 / 실제 재현으로 확정할 것.
3. 대조 결과를 보고 사용자가 무엇을 고칠지 정한다.

🔴 이 문서와 두 보고서 모두 **내부 문서**다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 한다.
