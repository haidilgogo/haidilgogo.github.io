# 「내 데이터 코드」 2차 교차 검증 요청서 — 수정본 재검증

> **읽는 사람: 코덱스(또는 다른 검토자).**
> 🔴 **저장소 파일을 읽기만 하고 수정하지 마세요.**
> 발견한 것은 **파일:줄번호 + 왜 문제인지 + 재현 방법 + 심각도**로 정리해 사용자에게 전달해 주세요.
>
> 작성 2026-07-31 / 대상 커밋 `4493eb6`(main, 배포 완료) / 캐시버전 css v624 · **script v528**
> 1차 요청서는 `REVIEW-SYNC-CODE.md` — **불변식 D1~D8의 정의는 거기 있고, 이 문서에서도 그대로 쓴다.**

---

## 0. 1차 검토 결과 — 지적이 전부 맞았다

1차에서 올라온 7건을 하나씩 코드로 확인한 결과 **6건이 사실**이었고 모두 고쳤다.
특히 ①은 **기존 사용자 전원**에게 해당하는 데이터 유실이었고 재현까지 됐다(즐겨찾기 `s1,s2` → 사라짐).

D4·D6·D7·D8이 보장된다는 판정도 정확했고, 「의도된 동작」에 대한 반론도 없었다. 좋은 리뷰였다.

---

## 1. 무엇을 어떻게 고쳤나 (재검증 대상)

| # | 1차 지적 | 수정 내용 | 볼 곳 |
|---|---|---|---|
| ① | 이력 없는 로컬 즐겨찾기·좋아요가 합치기에서 소실 | `mergeMarks`에 **로컬 집합**도 `{v:1, t:0}`으로 보완하는 단계 추가. 인자에 `localSet` 추가 | `mergeIntoLocal` 안 `mergeMarks` |
| ② | 동시 저장 시 서버를 통째로 덮어씀 | `pushSync`를 **읽고 → 합치고 → 쓰기**로 변경. 재진입 방지 `pushing`, 그 사이 저장을 기억하는 `pendingPush` 추가 | `pushSync` |
| ③ | 스티커 수정본이 서버의 옛 내용에 짐 | 수정 시 `rec.editedAt = Date.now()`. 합치기 비교를 `max(editedAt, addedAt)`으로, 비교 연산자를 `>=` → `>`(동점이면 **로컬 우선**) | `stampSubmitEl` edit 분기, `mergeIntoLocal`의 `touchedAt`/`put` |
| ④ | 저장소 가득 참 → 코드 소실 → 업로드도 안 됨 | 메모리 사본 `syncCodeMem` 도입. `getSyncCode`가 저장소 실패 시 이걸 쓴다 | `getSyncCode`/`setSyncCode` |
| ⑤ | 입력 검증이 실제 알파벳과 불일치 | `CODE_CHAR = /^[A-HJ-NP-Z2-9]{6}$/`로 정확히 검사(0·1·I·O 거부, 7자 이상 거부) | `normalizeCode` |
| ⑥ | 불러오기 후 `HG- HG-XXXXXX`로 보임 / `#syncMine` 안 열림 | 6자리만 넣고 `#syncMine.hidden = false` | 불러오기 버튼 핸들러 |
| ⑦ | 변경 감지가 항상 참 | 비교용 `fingerprint()`에서 `updatedAt` 제외 | `fingerprint`, `syncOnStart` |

---

## 2. 🔴 이번 수정이 새로 만들었을 수 있는 위험 (여기가 이번 검토의 핵심)

고치면서 구조가 바뀐 곳이 있다. **새로 생긴 구멍이 없는지 집중해서 봐 달라.**

### R1. `pushSync`의 읽고-합치고-쓰기 (②의 부작용)
가장 크게 바뀐 곳이다. 다음을 확인해 달라.

- **재진입 고리**: `pushSync` → `refreshAfterSync()` → `saveStamps/saveFavorites/saveLikes` → `schedulePush()` → `pushSync`. `pushing` 플래그와 `pendingPush`로 끊었는데, **무한 반복이나 끝없는 왕복(핑퐁)** 이 생길 수 있는 입력이 있는가?
- **`pendingPush` 유실**: `pushing` 중에 들어온 저장은 `pendingPush = true`로만 기억한다. 그 사이 저장이 여러 번이면 한 번으로 뭉쳐지는데, 마지막 상태가 반드시 올라가는가? `catch` 경로에서도 `done()`이 불려 `pendingPush`가 처리되는가?
- **`syncOnStart`와의 경쟁**: 앱 시작 시 `syncOnStart`(pull → merge → push)와 사용자의 저장으로 인한 `pushSync`가 겹칠 때, **늦게 도착한 응답이 새 데이터를 되돌리는** 경로가 있는가?
- **UI 정합성**: `pushSync` 안에서 `mergeIntoLocal`이 로컬 상태를 바꾼다. `changed`일 때만 `refreshAfterSync()`를 부르는데, 화면과 저장값이 어긋나는 순간이 생기는가?
- **에러 경로**: `once('value')`가 실패하면 `set`도 안 한다 → 그 저장은 서버에 영영 안 올라가는가? (다음 저장이나 다음 앱 시작 때 복구되는지)

### R2. `mergeMarks`의 로컬 보완이 과하지 않은가 (①의 부작용)
로컬 집합에 있는데 이력이 없는 id를 `{v:1, t:0}`으로 넣는다.

- 이 보완이 **다른 기기에서 명시적으로 끈 항목을 되살리는** 경우가 있는가? (원칙: `t:0`이라 remote의 어떤 명시적 변경에도 져야 한다)
- 반대로, 로컬에서 껐는데 이력이 없는 상태는 애초에 만들어질 수 있는가? (그렇다면 그건 `v:1`로 되살아난다)

### R3. `editedAt` 도입 (③의 부작용)
- `put`의 비교가 `>`로 바뀌어 **동점이면 로컬이 이긴다.** 다른 기기의 최신 수정이 밀리는 경우가 있는가?
- `editedAt`은 **수정 경로에만** 찍힌다. 삭제·생성 경로와 섞였을 때 이상한 조합이 나오는가?
- 옛 기록에는 `editedAt`이 없다(`undefined`). `clean()`이 이를 털어내는데, 합치기 비교에서 문제가 되는가?

### R4. `syncCodeMem` (④의 부작용)
- 저장소가 막힌 상태에서 코드가 메모리에만 있을 때, **다른 코드를 불러오면** 두 값이 어긋나는가?
- 새로고침하면 메모리 사본은 사라진다. 그때 서버에는 데이터가 있는데 코드를 모르는 상태가 되는데, 사용자에게 알릴 방법이 없는 것은 알고 있다 — **더 나쁜 결과(예: 다른 코드로 새로 만들어 서버를 덮어씀)** 가 생기는 경로가 있는가?

### R5. `CODE_CHAR` 강화 (⑤의 부작용)
- 생성기(`CODE_ALPHABET`)와 검증기(`CODE_CHAR`)가 **정말 같은 집합**인가? 한쪽만 바뀌면 멀쩡한 코드가 거부된다.
- 입력 중 다듬는 `extractCode`는 여전히 `[A-Z0-9]` 기준이다. 검증기와 달라서 생기는 어색한 동작이 있는가? (예: 0을 친 순간 지워지지 않고 남았다가 누를 때 거부)

### R6. `fingerprint` (⑦의 부작용)
- `delete p.updatedAt`이 `syncPayload()`가 만든 객체를 건드린다. 매번 새 객체를 만드는 게 맞는지, 어딘가에서 공유되는 참조가 아닌지.
- 변경 감지가 이제 **덜 민감**해졌다. 실제로 바뀌었는데 못 잡는 경우가 있는가?

---

## 3. 다시 확인해 줬으면 하는 불변식

1차 요청서의 **D1~D8을 그대로** 다시 봐 달라. 특히:

- **D1(데이터 보존)** — 1차에서 실패 판정이었다. 지금은 어떤가? `mergeIntoLocal`·`pushSync` 어느 경로에서도 로컬 데이터가 사라지지 않는지.
- **D3(해제 유지)** — ①을 고치면서 `mergeMarks`가 바뀌었다. 해제가 여전히 유지되는가?
- **D5(Firebase 실패 격리)** — `pushSync`에 `once('value')`가 늘었다. 그 실패가 저장·기록 흐름을 멈추지 않는가?

---

## 4. 검증 시 주의 (실제로 겪은 함정)

1. 🔴 **테스트용 코드에 `O`·`I`·`0`·`1`을 쓰지 말 것.** 실제 코드에 안 쓰는 글자라 이제 형식 검사에서 거부된다. 내가 이걸 몰라 "고치다 망가뜨렸나" 하고 한참 헤맸다. **`A-H · J-N · P-Z · 2-9`로만** 만들 것 (예: `HG-TESTA2`).
2. 🔴 **`input.value`에 직접 대입해서 붙여넣기를 테스트하지 말 것.** 브라우저의 실제 붙여넣기 경로를 우회한다. `document.execCommand('insertText', …)`처럼 실제 입력 경로를 쓸 것.
3. ⚠️ **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 실제로 바뀐다. 눌렀으면 반드시 되돌리고 숫자를 확인할 것.
4. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것(규칙상 통째 삭제는 막혀 있다).
5. ⚠️ 타이밍이 걸린 항목은 브라우저 콘솔의 타이머가 늘어질 수 있으니 넉넉히 기다릴 것.

---

## 5. 아직 아무도 확인 못 한 것

⬜ **실기기에서 사파리 → 홈 화면 복구.** 이 기능을 만든 진짜 이유인데 사용자가 아직 확인하지 못했다. 코드 리뷰로는 답이 안 나온다.

---

## 6. 참고

- 1차 요청서 `REVIEW-SYNC-CODE.md`에 **기능의 목적·서버 구조·보안 규칙·의도된 동작 목록**이 있다. 이 문서는 그 위에 얹는 것이다.
- 앱 전체 규칙은 `CLAUDE.md`, 진행 상황은 `HANDOVER.md`.
- 🔴 이 문서도 **내부 문서**다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 했다.
