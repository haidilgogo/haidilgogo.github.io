# 하딜고고(Haidilgogo)

## 프로젝트 개요
하이디라오(훠궈) 소스/레시피를 공유하는 개인 프로젝트 웹사이트. 데스크톱/모바일 대응.
빌드 도구나 프레임워크 없이 순수 HTML/CSS/JS로 작성됨. 라이브 사이트: https://haidilgogo.github.io/

## 배포
- GitHub Pages로 배포 (조직 haidilgogo / 저장소 haidilgogo.github.io — 루트 주소로 서빙)
- 별도 빌드 과정 없음 — **`main`에** 반영되면 그대로 라이브
- **배포 브랜치: `main` 하나뿐.**

### 🔴 푸시(백업) ≠ 배포 (2026-07-25 사용자 확정)
- **작업 브랜치(`feat/bottom-nav` 등)는 커밋할 때마다 원격에 푸시한다.** 따로 물어볼 필요 없음.
  GitHub Pages는 `main`만 서빙하므로 **작업 브랜치를 푸시해도 라이브 사이트는 안 바뀐다** — 푸시는 백업일 뿐이다.
- **`main`은 사용자가 배포를 지시할 때만 건드린다.** 임의 병합·푸시 금지.
- 이전에 있던 "🔴푸시금지" 규칙은 푸시와 배포를 같은 것으로 오해해서 생긴 것이고 **폐기됐다**.
  (그 규칙 탓에 3일치 26커밋이 로컬에만 존재한 적 있음.)
- 현재 배포 계획: **메뉴 탭까지 완성되면 `main`에 한 번에 배포**.

## 🔴 발도장 스티커 처리 규칙 (반드시 지킬 것)
발도장 탭(`#view-stamp`)의 지점 스티커는 **정해진 파이프라인으로만** 만든다. 어기면 카드마다 모양이 달라진다(실제로 그런 적 있음 — 흰 다이컷 테두리·둥근 모서리가 빠져 다른 카드와 어긋남).

- **새 스티커 추가·수정 = 무조건 `.claude/make_stickers.py` 사용.** PIL 등으로 직접 webp 변환 **절대 금지**.
  1. 사용자가 만든 완성 카드 원본(테두리+풍경+빨간밴드+구운 이름 PNG)을 `../../02. Data/Haidilgogo/sticker/complete/<지점명>.png`에 둔다.
  2. `.claude/make_stickers.py`의 `STAMP_MAP`에 `('<지점명>.png', '<지점명>.webp')` 한 줄 추가.
  3. `python3 .claude/make_stickers.py` 실행 → 흰 다이컷 테두리 + 둥근 모서리 + 검은 테두리 재드로우 + 600×1034(0.58)로 자동 통일해 `assets/stickers/`에 저장.
  4. `script.js`의 `STAMP_IMGS`에 `'<지점명>': 'assets/stickers/<지점명>.webp'` 한 줄 추가, `index.html`의 `script.js?v=` 숫자 올림.
- 스티커 규격(고정): **0.58 비율(600×1034), 배민 주아체, 빨간 밴드 `#D0402E` + 크림 글자 `#FBF5EA`, 흰 다이컷 테두리(앱은 CSS drop-shadow만).** 이름은 카드에 구워져 있으므로 앱 오버레이 안 함(`buildStampCard`: STAMP_IMGS 있으면 밴드 X).
- 자세한 배경·진행현황은 유저 메모리 `[[haidilgogo-nav-features-plan]]` 참고.

## 🔴 스크롤바 규칙 (절대 — 2026-07-24 사용자 확정)
**이 사이트는 어느 화면에서도 스크롤바를 보이지 않는다.** 스크롤 기능은 정상이고 **막대만** 안 보인다.

- 구현은 `styles.css` 최상단 `*` 규칙 **한 곳뿐**이다: `scrollbar-width: none`(Firefox) + `-ms-overflow-style: none`(구 Edge) + `*::-webkit-scrollbar { display: none }`(Safari·Chrome).
- **새 스크롤 영역(모달·시트·가로 레일 등)을 만들 때 스크롤바 CSS를 따로 쓰지 말 것.** 전역 규칙이 이미 덮는다. 개별 요소에 `scrollbar-width`나 `::-webkit-scrollbar`를 다시 선언하면 규칙이 또 흩어진다.
- 전역 규칙 도입 때 개별 요소에 흩어져 있던 옛 선언 6곳(`.tabs` / `.mf-scroll` / `.modal-card` / `.store-region-tabs` / `.celeb-rail` / `.hp-rail`)을 **전부 제거**했다. 되살리지 말 것.
- **`html`은 `overflow-y: auto`여야 한다. `scroll`로 되돌리지 말 것.** 예전의 `overflow-y: scroll` + `scrollbar-gutter: stable`은 "막대가 생겼다 사라지며 15px씩 폭이 밀리는 것" 방지용이었는데, 막대 폭이 늘 0이 된 지금은 목적이 사라졌고 부작용만 남았다 — `scroll`은 내용이 화면에 다 들어와도 html을 스크롤 영역으로 만들어서, 발도장·메뉴 **빈 화면처럼 내릴 게 없는 탭에서도 폰이 고무줄처럼 딸려 움직인다**. `auto`로 바꾼 뒤 상단바 sticky 정상·탭 전환 시 폭 흔들림 없음을 확인했다(2026-07-24).
- 참고: 막대가 안 보이면 "더 내릴 수 있다"는 힌트도 사라진다. 스크롤이 긴 화면을 새로 만들 땐 다른 신호(잘린 카드 빼꼼, 하단 고정 버튼 등)로 알 수 있게 할 것.

## 폴더/파일 구조
```
Haidilgogo/
├── index.html      # 페이지 구조 (탭, 검색창, 카드 그리드, 모달)
├── styles.css       # 전체 스타일
├── script.js        # 데이터 + 렌더링 로직 (아래 참고)
├── manifest.json    # PWA 매니페스트
└── assets/
    ├── cards/       # 레시피 카드 썸네일 (파일명 = 소스명.jpg, 버전 있으면 소스명_연도.jpg. 예: 건희소스[단]_2021.jpg, 영지소스_2025.jpg — 'recipe card_' 접두어 없음)
    ├── icons/       # 앱 아이콘·og (apple-touch-icon, icon-192, icon-512, logo-mark, og-image)
    └── fonts/       # 웹폰트 woff2
```

### 원본 데이터는 저장소 밖에 있다 (중요)
카드 이미지·아이콘의 **원본은 이 저장소에 없다.** 사용자 아카이브인 `../../02. Data/Haidilgogo/`에 따로 보관된다 — 프로젝트 폴더만 뒤지면 "이미지가 없다"고 잘못 결론 내리게 된다(실제로 그런 적 있음).
🔴 **2026-08-01 정정 — `thumbnail/` 아래가 용도별로 한 단계 나뉘었다.** 옛 문서에는 `thumbnail/original/`로 적혀 있었는데 지금은 **`thumbnail/recipe/original/`**이다. 옛 경로로 찾아보고 "원본이 없다"고 잘못 보고한 적이 있다(2026-08-01).

```
02. Data/Haidilgogo/
├── thumbnail/
│   ├── recipe/                     # 레시피 카드용
│   │   ├── original/               # 손대지 않은 원본 (대기분)
│   │   │   └── complete/           # 카드로 만들어 사이트에 올린 것의 원본
│   │   └── 1080x1080/complete/     # 정사각 가공본 (카드로 올린 것)
│   ├── menu/                       # 메뉴 탭 일러스트 (2026-08-01 신설)
│   │   ├── 탕/ · 고기/             # 완성본 = 1080 정사각·배경 투명. 파일명 = 메뉴 이름
│   │   └── original/               # 뽑아내기 전 시트 (archive·final·reference·review)
│   └── main-banner/                # 배너·아티클 히어로
│       ├── columns/{guide,hotpot-trivia,cilantro}
│       └── monthly-sauce/<연월>/
├── sticker/complete/               # 발도장 스티커 완성 카드 (위 스티커 규칙 참고)
├── menu/                           # 코덱스 메뉴 조사 자료(md·스크립트) — 저장소 아님
└── icon/{logo, src}/               # 로고·출처 플랫폼 아이콘 원본
```
- **메뉴 일러스트는 `.claude/make_menu_images.py`로 변환**한다(1080 정사각 → 540 webp, 하위 폴더까지 훑음, `original/`은 건너뜀). 결과는 `assets/menu/<메뉴 이름>.webp`로 **평평하게** 나온다 — 메뉴의 분류는 지점마다 다르고 바뀔 수 있어서 경로에 분류를 넣지 않는다.
- ⚠️ 한글 파일명이 자모 분리(NFD)로 저장된 게 섞여 있어 **`find -name "*토마토*"`·`grep`이 파일을 못 찾을 수 있다**. 안 나오면 없는 게 아니라 `ls`로 직접 확인할 것. 또 `find | head`로 자르면 `complete/`를 통째로 놓친다.
- **새 카드 이미지는 `assets/cards/`에** 넣고, `RECIPES`의 `img`는 `'assets/cards/<소스명>.jpg'`로 참조. 파일명에 소괄호((단)/(짠) 등) 있어도 브라우저에서 정상 로드됨(확인 완료). 파일명은 표시 이름과 맞춰 유지.

## 데이터 구조 (script.js 안에 하드코딩, 별도 JSON 없음)
- `CATS`: 카테고리 목록 (`전체, 소스, 탕, 히든메뉴`). 2026-07-21 개편: 구 `육수`→`탕`, 구 `밥·면·기타`→`히든메뉴`로 통합. CSS `[data-cat="..."]` 색상 세트(소스=로즈/탕=골드/히든메뉴=초록)와 값이 일치해야 함
- `RECIPES`: 레시피 배열. 각 항목 필드:
  - `id`, `cat`(카테고리), `emoji`(썸네일 이미지 없는 카드의 배경 아이콘), `name`, `desc`(설명/유래)
  - `img`(이미지 경로, 없으면 emoji+tint 배경 사용), `imgBg`, `imgFit`(항상 `'cover'`), `tint`(그라데이션)
  - `order`: 주문 항목 (선택, 예: 밥 메뉴의 "공깃밥 1공기"). 이름은 `ORDER_ITEMS` 목록 안에서만 사용
  - `ings`: 재료 배열 `[이름, 양, 단위]`. 이름은 `SAUCE_BAR`, 단위는 `UNITS` 목록 안에서만 사용
  - `steps`: 조리 단계 배열 (없으면 `[]`)
  - `tip`: 팁 문구 (없으면 `''`)
  - `source`: 출처 표기 (선택, 예: "버블 건희(ONEUS)", "네이버블로그 sjsilver23"). `img`가 있는 카드에서만 썸네일 하단 그라데이션 위에 표시됨 — 출처가 불분명한 레시피는 생략 가능

## 썸네일 규격 (2026-07-11 규격화)
- 모든 카드 썸네일은 **1:1 정사각형**으로 통일(데스크탑·모바일 동일). `.recipe-thumb`가 `aspect-ratio: 1/1`, 이미지는 `object-fit: cover`로 꽉 채움.
- **카드 이미지는 반드시 정사각형 원본으로 준비할 것**(권장 1080×1080). 정사각형이 아닌 이미지는 `cover`로 가운데가 잘려 내용(특히 옆쪽 레시피 글씨)이 사라짐.
- 이미지 최적화: `sips -s format jpeg -s formatOptions 82 --resampleWidth 1080 --resampleHeight 1080 "원본" --out "결과.jpg"` (정사각형 원본 전제).
- ⚠️ **원본 PNG는 절대 삭제하지 말 것.** 원본은 이 저장소 밖 `../../02. Data/Haidilgogo/thumbnail/`에 있는 사용자 아카이브다(`original/` = 원본, `1080x1080/` = 정사각 가공본). 변환이 끝나면 **삭제가 아니라 `1080x1080/complete/`로 이동**한다 — `complete/` 안 = 카드로 만들어 사이트에 올린 것, 밖 = 아직 카드 없는 대기분. 파일명은 카드 이름과 맞춘다.
- **개별 위치 조정(`imgPosition`)·카드별 scale 땜빵 금지.** 과거 영지소스 전용 scale 1.3 땜빵이 데스크탑 hover 규칙과 충돌해 "좁은 폭에서 hover 시 축소" 버그를 유발했음 → 규격화로 제거함. 크롭이 안 맞으면 코드로 때우지 말고 **정사각형 이미지 자체를 다시 준비**한다.
- hover/active 반응은 전 카드 공통(이미지 `scale(1.06)` + 카드 살짝 올라옴 + 테두리 브랜드빨강 1px). 카드별로 다르게 주지 않는다.
- **hover 확대 안전여백**: 이미지가 1.06배(가운데 기준) 확대되며 각 변 약 31px가 잘림. 핵심 내용(글씨·얼굴)은 1080 기준 **상하좌우 40px 여백 안(중앙 ~1000×1000)** 에 배치. 하단 ~15%는 어두운 그라데이션+출처 글씨 영역이라 핵심 배치 지양.

## 메인 배너 문구 길이 (2026-07-28 측정)
홈 상단 배너의 제목·부제는 **폭이 정해져 있어 길면 감기거나 잘린다.** 가장 좁은 기기(320px) 기준 안전선:

- **제목(`.mf-name`, 30px)**: 한글 **9자**까지 한 줄. 넘으면 두 줄로 감김.
- **부제(`.mf-desc`, 14px)**: 한글 **20자**까지. `white-space: nowrap`이라 **줄바꿈이 아니라 뒤가 `…`로 잘린다** — 화면에서 티가 잘 안 나니 주의.

**이달의 소스 배너는 매달 내용이 바뀌므로** 새 레시피의 `heroDesc`를 쓸 때 20자 안쪽으로 잡는다(다른 세 배너는 문구 고정이라 안전). 실제로 `하얼빈 정통파, 중식 여신 박은영 셰프의 참기름 소스`가 잘려서 앞머리를 덜어낸 적 있음.
- 이름이 13자쯤 되는 레시피(예: `라젤(이 아는 동생)소스`)가 이달의 소스로 뽑히면 제목이 두 줄이 된다. 폰트 크기로는 해결이 안 되니 그 달에 따로 판단한다.
- **제목 크기는 배너 공통 30px**이고 예외는 가이드 배너(26px) 하나다. 가이드는 제목이 두 줄(`titleHtml`)이라 30px이면 320px에서 세 줄이 된다.
- 길이 확인은 눈대중 대신 브라우저에서 `Range.getBoundingClientRect()`로 실제 글자 폭을 잰다. `scrollWidth`는 블록 요소라 여유를 알 수 없다.

## 정식 재료·단위 목록 (표기 일관성용)
`script.js` 상단에 `SAUCE_BAR`(소스바 재료), `ORDER_ITEMS`(주문 항목), `UNITS`(단위)를 상수로 고정해둠.
페이지 로딩 시 모든 레시피를 검사해 **목록에 없는 이름/단위가 있으면 브라우저 콘솔에 경고**를 띄움 → 오타·표기 불일치(같은 재료 다른 이름) 방지.
- `매운소고기소스`와 `매운소고기소스(건더기만)`은 **의도적으로 구분**한 별개 항목 (통일하지 말 것)
- 진짜 새 재료라면 → 먼저 해당 목록에 추가한 뒤 레시피에 사용

### 재료 표시 순서 (자동 정렬, 2026-07-11)
`SAUCE_BAR` 배열의 **나열 순서 = 재료 서빙 순서**다. 레시피 상세를 렌더할 때 `sortIngs()`가 `ings`를 이 순서로 **자동 정렬**하므로, `RECIPES`의 `ings`는 아무 순서로 넣어도 화면엔 항상 같은 순서로 나온다(원본 데이터는 안 바뀜).
- 순서 그룹: **1) 베이스 소스 → 2) 액체 양념(굴소스·간장소스·중국식초) → 3) 고기·건더기 → 4) 채소·향신 → 5) 가루류 → 6) 기름류 → 7) 간·기타(설탕·소금·미원)**
- 그래서 **새 재료를 SAUCE_BAR에 추가할 땐 알맞은 그룹 위치에 넣을 것** (그 위치가 곧 표시 순서가 됨). 같은 그룹 안 순서는 SAUCE_BAR에 적힌 순서를 따름.
- 레시피 추가 시 `ings` 순서는 신경 쓸 필요 없음 — 자동 정렬됨.

### 레시피 추가 시 재료 검증 절차 (필수)
새 레시피 카드를 추가할 때는 **항상** `RECIPES`에 넣기 전에 모든 재료·단위·주문항목 이름을 정식 목록(`SAUCE_BAR` / `ORDER_ITEMS` / `UNITS`)과 대조 검증한다. 각 이름에 대해:

1. **정식 목록에 정확히 있으면** → 그대로 사용.
2. **목록에 없는데 기존 항목의 변형처럼 보이면** (띄어쓰기·동의어·줄임말·표기 차이 등, 예: `다진 파` vs `다진파`, `고춧가루` vs `마라시즈닝/고춧가루`) → 임의로 합치거나 추가하지 말고 **사용자에게 먼저 확인**한다: "이거 기존 `○○`와 같은 건가요, 다른 건가요?"
   - 같음 → 기존 정식 이름으로 통일해서 사용.
   - 다름 → 3번으로.
3. **처음 보는(진짜 새) 재료면** → 정식 명칭을 사용자에게 확인한 뒤, 해당 목록(`SAUCE_BAR`/`ORDER_ITEMS`/`UNITS`)에 **먼저 추가**하고 나서 레시피에 사용한다.
4. **어떤 경우에도 말없이 새 이름을 만들거나 임의로 합치지 않는다.** 비슷해 보여도 의도적으로 구분된 항목이 있음 (예: `매운소고기소스` vs `매운소고기소스(건더기만)`). 애매하면 반드시 사용자에게 물어본다.

**표기 규칙(2026-07-12 확정, 두 구분자):**
- **(소괄호) = 변형·옵션** (맛 버전, 재료 사용 옵션 모두 포함): `건희소스(단)`, `건희소스(짠)`, `매운소고기소스(건더기만)`, `청유훠궈소스(건더기만)`.
- **정식명/부연 = 정식명이 낯설 때 슬래시로 붙이는 고정 부연**: `마라시즈닝/고춧가루`, `오향우육/다진 고기`.

부연(/)은 재료마다 한 번 정하는 이름의 일부이며, 원작자가 레시피별로 다르게 부른 이름은 병기하지 않는다(카드 이미지에 원본이 있음). ※ 이력: 병기가 소괄호→**슬래시(/)** 로 옮겨가면서, 소괄호는 **변형·옵션 전용**으로 통합(대괄호는 더 이상 쓰지 않음, 2026-07-12). 앞으로 새 변형·옵션은 `()`, 새 병기는 `/`.
- **띄어쓰기(2026-07-12 확정)**: 관형형+명사는 어법대로 띄어 씀 — `다진 마늘`, `다진 파`, `다진 양파`, `다진 고기`. `고춧가루`(사이시옷)가 정답(고추가루 아님).

즉 기본 원칙: **애매하거나 목록에 없으면 → 추측하지 말고 사용자에게 확인.**

### 단위(양) 표기 방침 (2026-07-11 확정: 원본 충실)
하이디라오 소스바 도구는 **지점마다 다르므로**(스푼/국자 등) "진짜 정답 도구"는 없음 → **원작자(썸네일 원본) 표기를 그대로 따르는 것**이 유일하게 검증 가능한 기준이다.
- **레시피마다 단위가 달라도 정상**이다(사용자는 한 번에 소스 하나만 만들며, 그 레시피 안 비율만 맞으면 됨). 재료별로 단위를 강제 통일하지 말 것 — 원본을 훼손함.
- 허용되는 정규화는 **동의어 도구명 통일(국자·숟갈 → 스푼)** 과 **애매어 정규화(많이→넉넉하게, 조금→적당히, 반→0.5)** 뿐. `티스푼`은 스푼과 다른 크기이므로 원본대로 유지. `바퀴`(두르기)도 원본이 그리 적었으면 유지.
- **스푼을 집게로 바꾸는 식의 도구 재해석 금지**(뜻이 달라짐). 과거 다진파를 임의로 `집게`로 적었던 것을 2026-07-11 전부 원본 `스푼`으로 되돌림(양은 원본과 동일했음).

## 주요 기능 (script.js)
- 카테고리 탭 필터링 + 검색 (이름/재료명 매칭, 설명은 검색 대상 아님 — 설명 문구의 단어로 걸려서 원치 않는 레시피가 나오는 걸 막기 위해 의도적으로 제외)
- 카드 클릭 → 상세(주문/재료/단계/팁) 표시. 데스크톱=가운데 모달 / 모바일(≤640px)=상단바 아래 전체화면 패널(상단바는 그대로 동작하며, 탭·검색 누르면 상세가 자동으로 닫히고 그 동작 실행. X버튼으로 닫음, history 처리 없음)
- 즐겨찾기는 `localStorage`에 저장되어 새로고침/재방문 후에도 유지됨
- 커스텀 pull-to-refresh는 제거됨(e5807a6). 로고 클릭 시 `?_r=timestamp` 쿼리로 강제 새로고침
- 좋아요는 Firebase Realtime DB(`haidilao-gogo`, asia-southeast1)의 `likes`에 저장되어 전체 방문자가 공유. `localStorage`는 캐시 겸 "내가 누른 것" 표시용이라 기기별로 남음. DB 연결 실패 시 로컬 캐시로만 동작(a1cfd07)

### script.js의 Firebase apiKey는 시크릿이 아니다 (지우지 말 것)
`script.js`의 `firebaseConfig` 안에 `apiKey: 'AIza...'`가 그대로 박혀 있다. 이름 때문에 비밀키로 오해하기 쉽지만 Firebase 웹 apiKey는 **클라이언트에 공개되는 게 정상인 프로젝트 식별자**다(구글 공식 입장). 브라우저가 로드하는 파일에 있어야 동작하므로 저장소를 비공개로 돌려도 사이트 방문자 누구나 볼 수 있다.

- 이걸 "유출"로 판단해 삭제·환경변수화·키 회전 하지 말 것 → **좋아요 기능이 죽는다**
- 실제 방어선은 apiKey가 아니라 Realtime DB 보안 규칙(콘솔에서 관리, 저장소 밖). 2026-07-17 확인 결과 `likes/$id`만 숫자·0 이상 조건으로 쓰기 허용, 그 외 경로는 전면 차단 — 적절한 상태다
- 남는 리스크는 "누군가 좋아요 숫자를 임의로 조작 가능" 하나뿐인데, 로그인 없는 좋아요의 구조적 한계라 규칙으로는 못 막는다. App Check가 유일한 해법이나 이 사이트 규모엔 과함 — 의도적으로 안 함

## 작업 시 유의사항
- 사용자는 코딩 초보 — 변경사항은 단계별로, 스크린샷/결과 확인 요청 가능성 있음
- 디자인 변경 시 기존 tint/이모지 톤과 일관성 유지
- 프레임워크 도입 등 구조를 바꾸는 제안은 먼저 설명 후 진행
- 진짜 비밀키(서버 키·토큰·비밀번호)는 커밋 금지. 단 이 저장소는 서버 없는 정적 사이트라 **숨길 곳 자체가 없다** — 코드에 넣든 안 넣든 브라우저에 다 노출된다. 비밀키가 필요한 기능이 나오면 "어디에 숨길까"가 아니라 "이 구조로 가능한가"부터 사용자와 상의할 것
