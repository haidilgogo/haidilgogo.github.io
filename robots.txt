# 하딜고고 — 검색엔진 크롤러 안내 (2026-07-30 신설)
# 전체 공개. 막을 페이지가 없다(사이트가 index.html 한 장이다).
#
# ⚠️ 내부 문서(CLAUDE.md·HANDOVER.md)는 여기서 막는 게 아니라 _config.yml의 exclude로
#    배포 자체에서 뺐다. robots.txt는 "부탁"일 뿐이라 주소를 아는 사람은 그대로 읽을 수 있다.
#    아래 Disallow는 혹시 파일이 다시 배포되는 사고가 났을 때를 위한 이중 안전장치다.

# 🔴 셀럽 사진은 검색 이미지에서 뺀다 (2026-08-04)
#    구글이 하딜고고 검색결과의 대표 썸네일로 **셀럽 얼굴 사진**을 골라 쓰고 있었다.
#    그것도 이미 교체한 옛 사진이었다 — 사진을 바꿔도 주소가 같아서(assets/people/세훈.jpg)
#    구글 이미지 색인에 옛 내용이 남아 있었다.
#    사용자님 판단: 옛 사진도 싫고 다른 셀럽으로 바뀌는 것도 싫다 → 폴더째 후보에서 뺀다.
#
#    ⚠️ noimageindex 를 쓰지 않은 이유: 그건 레시피 카드·메뉴 그림까지 전부 색인에서 뺀다.
#       여기 한 줄이면 셀럽 사진만 빠지고 나머지는 그대로다.
#    ⚠️ 사이트 화면에는 아무 영향이 없다. 셀럽 사진은 지금처럼 그대로 보인다.
#    ⚠️ 이미 색인된 옛 사진은 이 줄만으로 즉시 사라지지 않는다 — 2026-08-04 Search Console
#       「삭제 → 임시 삭제 항목」으로 따로 내렸다(약 6개월). 이 줄이 그 뒤를 잇는 영구 조치다.

User-agent: *
Allow: /
Disallow: /CLAUDE.md
Disallow: /HANDOVER.md
Disallow: /assets/people/

Sitemap: https://haidilgogo.com/sitemap.xml
