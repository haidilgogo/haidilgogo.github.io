# 전수조사 보고서 — 클로드

> 기준서 `AUDIT-SPEC.md`에 따름 / 대상 커밋 `3c28840` · css v624 · script v530
> 🔴 코덱스 보고서를 보지 않고 작성했다.
> 조사 방식: 구역별로 `script.js`·`index.html`·`styles.css`를 읽고, 의심 지점은 실제 브라우저에서 확인.

---

## A. 데이터·저장

### [S3] 다른 탭에서 즐겨찾기·좋아요를 바꿔도 **이력(marks)은 따라오지 않는다**
- 파일: `script.js:3802`(storage 리스너), `script.js:3762`(`syncExternalState`)
- 증상: 같은 사이트를 두 탭에 열어두고 A탭에서 즐겨찾기를 바꾸면, B탭은 `favorites`/`likedByMe`만 다시 읽고 **`favMarks`/`likedMarks`는 옛 값을 그대로 들고 있다.**
- 원인: storage 리스너가 `FAVORITES_KEY`·`LIKED_KEY`·`LIKE_COUNTS_KEY`·`STAMPS_KEY`만 처리한다. 마크 두 키(`haidilao_fav_marks`·`haidilao_liked_marks`)와 `haidilao_sync_code`는 목록에 없다.
- 재현: 1) 두 탭에 사이트를 연다 2) A탭에서 즐겨찾기를 켠다 3) B탭에서 `JSON.parse(localStorage.haidilao_fav_marks)`와 B탭 메모리 상태를 비교하면 어긋난다 4) B탭에서 뭔가 저장하면 서버로 옛 시각이 올라갈 수 있다
- 완화 요인: 저장할 때마다 `updateMarks`가 현재 집합 기준으로 이력을 다시 맞춰서 **값 자체는 자가 치유**된다. 어긋나는 것은 시각(t)뿐이라, 결과는 "다른 기기의 최근 변경이 밀릴 수 있다" 수준이다.
- 확인: 코드 읽고 판단 + 리스너 키 목록 실측. 실제 두 탭 재현은 안 함 **(추정)**

### [S4] 저장 실패를 전부 조용히 삼킨다
- 파일: `script.js:337`(`saveFavorites`), `:3198`(`saveStamps`), `:4123`(마크 저장), 그 밖 `catch (e) {}` 다수
- 증상: `localStorage`가 가득 차거나 막히면 저장이 안 되는데 **사용자에게 아무 표시가 없다.** 화면에는 남은 것처럼 보이다가 새로고침하면 사라진다.
- 원인: 의도적으로 삼키고 있다(시크릿 모드 대응). 다만 사용자에게 알리는 경로가 없다.
- 재현: 저장소를 가득 채운 뒤 스티커를 추가 → 화면엔 추가되지만 새로고침하면 없음
- 참고: 코드 자체는 메모리 사본(`syncCodeMem`)으로 서버 업로드까지는 살렸다(3차 수정). 로컬 유실은 남는다.
- 확인: 코드 읽고 판단 **(추정 — 용량 초과 실제 재현은 안 함)**

---

## B. 내 데이터 코드

### [S3] 동기화로 들어온 기록의 매장 이름이 이 기기에 없으면 **깨진 이미지**가 뜬다
- 파일: `script.js:3235`(`buildStampCard`), `script.js:3228`의 주석
- 증상: `STAMP_IMGS[name]`이 `undefined`인 채로 `<img src="undefined">`가 만들어진다. 카드 자리에 깨진 이미지가 남는다.
- 원인: 이 함수의 주석은 **"여기 들어오는 이름은 항상 STAMP_IMGS에 있다"**고 단언한다. 기록 드롭다운이 그림 있는 매장만 보여주므로 예전엔 참이었다. **동기화가 그 전제를 깼다** — 다른 기기에서 온 기록은 이 기기가 모르는 매장일 수 있다.
- 재현: 1) 서버 `sync/<코드>`의 기록에 `name: '없는점'`을 넣는다 2) 그 코드를 불러온다 3) 스티커 탭 목록에 깨진 이미지 카드가 뜬다
- 현실적 경로: 새 매장 스티커가 추가된 뒤, **옛 `script.js`를 캐시로 들고 있는 기기**가 그 기록을 받는 경우. 사파리가 옛 HTML을 들고 있는 상황은 이 프로젝트에서 실제로 겪었다.
- 확인: 코드 읽고 판단(가드 부재는 확실). 실제 렌더는 재현 안 함

### [S4] `stamps.deleted`(지운 id 목록)가 무한히 자란다
- 파일: `script.js:3721`, `script.js:4069`, `script.js:4154`
- 증상: 기록을 지울 때마다 id가 쌓이고 **절대 줄지 않는다.** 서버·로컬 양쪽에 계속 남는다.
- 원인: 되살아남 방지를 위해 필요한 구조인데, 오래된 tombstone을 정리하는 규칙이 없다.
- 크기: id 하나 약 15바이트. 1000번 지워도 15KB — **당장 문제는 아니다.**
- 확인: 코드 읽고 판단

---

## C. 스티커 탭

### [S4] 수정 시 `editedAt`을 찍지만 **삭제·생성에는 시각 개념이 없다**
- 파일: `script.js:3562`(수정), `script.js:3721`(삭제)
- 증상: 삭제는 tombstone(영구 우선)이라 시각이 필요 없고 생성은 `addedAt`이 있어 지금은 맞는다. 다만 앞으로 "삭제 취소" 같은 기능이 생기면 이 비대칭이 바로 문제가 된다.
- 확인: 코드 읽고 판단. **현재 동작에 결함 없음** — 구조상 주의점으로만 기록

---

## D. 레시피 화면

**발견 없음.**

훑은 근거: 좋아요의 실제분/표시분 분리(`getRealLikeCount` `script.js:469` / `getLikeCount` `:473`)가 유지되고, 서버 반영은 `likesRef.child(id).transaction(...)`(`:629`)으로 실제분만 증감한다. `BASE_LIKES`는 표시·정렬에만 쓰인다. 순위 배지는 매 렌더 재계산(`sauceRankMap`), 정렬은 `byPopular` 한 곳. 이전 세션에서 잡힌 결함들(필터 상태 순위 오류·하트 초기화)이 되돌아온 흔적 없음.

---

## E. 스토리 뷰어

**발견 없음.**

훑은 근거: 사진 요소는 슬라이드마다 새로 만들고(`storyBody.innerHTML`), 넘김 시 흰 배경 위에 카드 색을 인라인으로 덮는다. 2026-07-31에 시도했던 "요소 재사용 + decode 후 교체"는 되돌려서 흔적이 없다(`storySwapNow`·`renderStoryBody` 없음 확인). 탭 판정은 좌표 기준이라 `.story-img` 선택자 문제 없음.

---

## F. 매장·기타 탭

**발견 없음.**

훑은 근거: 매장 목록은 `STORES` 정적 배열, `STORE_CATCH[name] === 'soon'`으로 오픈 예정 처리. 메뉴 탭은 빈 화면 하나. 가챠는 `haidilao_gacha_daily`로 하루 1회 제한(동기화 대상 아님 — 의도된 것).

---

## G. 전역

### [S3] 정적 아티클 3개가 앱 본문과 **어긋날 수 있다**
- 파일: `guide.html`·`hotpot.html`·`cilantro.html`(생성 시각 7/30 23:36) vs `script.js`(7/31 19:08)
- 증상: 아티클 본문은 `script.js`에서 뽑아 만든 정적 사본이다. `script.js`의 글을 고치고 **생성기(`.claude/make_article_pages.mjs`)를 다시 안 돌리면** 검색으로 들어온 사람은 옛 글을 본다.
- 원인: 생성이 수동이고 강제 장치가 없다.
- 재현: `script.js`의 가이드 문단을 고치고 배포한 뒤 `haidilgogo.com/guide.html`을 열면 옛 문장이 나온다
- 현재 상태: 오늘 바꾼 것은 전부 동기화 기능이라 **아티클 본문은 실제로 안 바뀌었다.** 지금은 어긋나 있지 않다.
- 확인: 파일 시각 실측 + 구조 확인

### [S4] 캐시 버전을 사람이 손으로 올린다
- 파일: `index.html`의 `?v=` 두 곳
- 증상: CSS/JS를 고치고 숫자를 안 올리면 모바일이 옛 파일을 계속 쓴다. 이 프로젝트에서 실제로 겪은 사고다.
- 확인: 규칙 존재(`CLAUDE.md`)하나 자동 장치는 없음. **현재 값은 맞다**(css 624 / script 530 = 파일 상태와 일치)

---

## 구역별 요약

| 구역 | 발견 | 최고 심각도 | 훑은 근거 |
|---|---|---|---|
| A. 데이터·저장 | 2 | S3 | 저장 키 17개 전수, `JSON.parse` 8곳 방어 확인, storage 리스너 키 목록 |
| B. 내 데이터 코드 | 2 | S3 | 코드 생성·트랜잭션·합치기·적용 경로 정독, 3차 수정분 재현 확인 |
| C. 스티커 탭 | 1 | S4 | 추가·수정·삭제 3경로, 카드 캐시 무효화 |
| D. 레시피 화면 | 0 | — | 좋아요 분리·정렬·배지 경로 확인 |
| E. 스토리 뷰어 | 0 | — | 되돌린 수정의 잔재 없음 확인 |
| F. 매장·기타 | 0 | — | 정적 데이터·빈 화면 확인 |
| G. 전역 | 2 | S3 | 아티클 생성 시각 대조, 캐시 버전 실측 |

**합계 7건 (S1 0 · S2 0 · S3 4 · S4 3)**

---

## 스스로 밝히는 한계

1. **내가 만든 코드를 내가 본다.** 세 번의 교차 검증에서 매번 내가 놓친 것이 나왔다 — 이 보고서도 같은 편향이 있다고 봐야 한다. 특히 **B 구역(동기화)** 은 내가 "이미 세 번 봤다"고 생각하는 만큼 눈이 무뎌져 있을 가능성이 높다.
2. **실제 재현을 다 하지는 못했다.** `(추정)` 표시한 항목은 코드 근거만 있다.
3. **동시성·타이밍**은 브라우저 콘솔의 타이머가 늘어져 정확히 재현하기 어렵다. 이 범주는 코드 독해에 의존했다.
4. **CSS는 결함 기준으로 훑지 않았다.** 디자인 취향이 범위 밖이라 레이아웃 깨짐만 봤고, 그것도 폭 375·800에서만 봤다.
