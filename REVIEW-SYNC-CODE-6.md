# 「내 데이터 코드」 6차 교차 검증 요청서 — 5차 수정분 재검증

> **읽는 사람: 코덱스(또는 다른 검토자).**
> 🔴 **저장소 파일을 읽기만 하고 수정하지 마세요.**
> 발견한 것은 **파일:줄번호 + 증상 + 원인 + 재현 + 심각도**로 정리해 주세요.
>
> 작성 2026-07-31 / 대상 커밋 **`72f85fa`** / 캐시버전 css v624 · **script v532**
> 앞선 요청서: `REVIEW-SYNC-CODE.md`(1차, **불변식 D1~D8 정의**) · `-2.md` · `-3.md` · `-4.md` · `-5.md`
> 5차 회신 원문: **`REVIEW-SYNC-CODE-5-REPLY.md`**

## 🔴 배포 전 검증이다 (5차와 동일)

- 대상은 **작업 브랜치 `fix/sync-review4`**다. **라이브는 아직 `3c28840`(script v530)**이다.
- ⚠️ **라이브(haidilgogo.com)로 검증하면 안 된다.** 이 브랜치를 로컬에서 띄워 볼 것.

```bash
git fetch origin && git checkout fix/sync-review4
```

## 🔴 회신은 원문 그대로 받고 싶다

5차부터 **회신 원문을 저장소에 보관**하기 시작했다(`REVIEW-SYNC-CODE-5-REPLY.md`). 이유: 1차 때 안 남겨서 *"7건 중 6건이 사실"*이라고만 적혀 있고 **어느 1건이 기각됐는지 알 수 없게 됐다.** 6차 회신도 요약하지 말고 **판정 표 + 각 건의 위치·증상·원인·재현**을 그대로 적어 달라.

---

## 0. 5차 검토 결과 — 다섯 번째도 맞았다

**추가 결함 3건이 전부 사실이었다.** 지적을 그대로 믿지 않고 **셋 다 먼저 재현한 뒤** 고쳤다(재현 결과는 `-5-REPLY.md` 아래쪽).

**`R2-a`는 (가) 설계상 정상으로 확정했다** — 코덱스 판단과 사용자 결정이 같았다. 별도 전환 상태를 두지 않는다. **다시 제안하지 말 것.**

🔴 **5차 3건은 v531이 만든 회귀가 아니라 라이브(v530)에도 있던 것**이다. v531의 diff는 `syncOnStart`·`applyPayload` 두 곳뿐이고, 세 건이 사는 곳은 손대지 않았다.

---

## 1. 이번에 무엇을 바꿨나 (재검증 대상)

| # | 5차 지적 | 수정 | 위치 |
|---|---|---|---|
| ① | 여러 탭에서 코드가 갈라짐 | `storage` 이벤트 목록에 **`SYNC_CODE_KEY` 추가**. 다른 탭이 코드를 바꾸면 `syncCodeMem`도 따라가고 `renderCodeNotice()`로 띠를 다시 그린다. ⚠️ **빈 값이면 메모리 사본을 안 지운다**(저장소가 막혔을 때 코드를 잃지 않으려고 둔 것 — 4차 ④) | `syncExternalState` · `storage` 리스너 |
| ② | 열린 스티커 상세가 옛 내용 | `openStampView`에서 **그리는 부분만 `renderStampView`로 분리**하고, `refreshStampView()`(없으면 닫기)를 **`refreshAfterSync`와 탭 간 동기화 양쪽**에서 부른다 | `openStampView` · `renderStampView` · `refreshStampView` · `refreshAfterSync` · `syncExternalState` |
| ③ | 읽기 실패가 '데이터 없음'으로 표시 | **`pullSyncResult`** 신설 — `{ok:false}`(못 읽음) / `{ok:true, value:null}`(빈 서버)를 구분. `pullSync`는 그 위의 얇은 껍데기로 남겨 시작 동기화는 종전대로 | `pullSyncResult` · `pullSync` · 불러오기 핸들러 |

**곁들여 고친 것(코덱스 지적 범위 밖 — 판단해 달라):** 탭 간 동기화 경로(`syncExternalState`의 `STAMPS_KEY` 분기)는 **지워졌을 때 닫기만 하고 내용이 바뀐 경우는 그대로 뒀다.** 동기화 경로와 같은 결함이라 `refreshStampView()` 한 줄로 함께 고쳤다. **범위를 넘은 것인지 봐 달라.**

🔴 **`openStampView`를 그대로 다시 부르면 안 된다** — `stampViewReturnFocus = document.activeElement`가 **대화상자 안 버튼으로 덮여** 닫을 때 원래 자리로 못 돌아간다. 그래서 여는 것(포커스·`open` 클래스)은 `openStampView`에만 남기고 내용만 `renderStampView`가 바꾼다.

---

## 2. 🔴 이번 수정이 새로 만들었을 위험

다섯 번 연속 "내 수정이 새 구멍을 만드는" 형태였다. 이번에도 그럴 것으로 보고 봐 달라.

### R1. `storage`로 코드를 갈아끼우는 것 (①의 부작용)

- **진행 중이던 전송이 버려진다.** `pushSync`·`syncOnStart`는 `getSyncCode() === code`를 확인하는데, 다른 탭이 코드를 바꾸면 그 확인이 이제 **실패**한다. 커밋은 이미 됐고 로컬 반영만 건너뛴다 — **"아무도 안 올리는 창"**이 생기는가? (4차 ①로 막으려던 것이 여기로 옮겨가지 않았는가?)
- **`renderCodeNotice()`를 `storage` 콜백에서 부른다.** 이 함수는 접힘 상태(`CODE_FOLD_KEY`)·버튼 문구·`hidden`을 모두 다시 계산한다. 다른 탭의 저장 때문에 **이 탭의 화면 상태가 되돌아가는** 경우가 있는가? (특히 사용자가 방금 접었는데 펴지는 등)
- **`pageshow(persisted)`에서도 `syncExternalState()`가 인자 없이 불린다**(`script.js` 1610행 근처, bfcache 복원). 이제 그 경로에서도 코드가 저장소 값으로 맞춰진다. iOS 사파리 뒤로가기 복원에서 **이상한 조합**이 나오는가?
- **빈 값이면 메모리 사본을 유지**한다. 다른 탭이 코드를 **의도적으로 없애는** 경로가 존재하는가? 있다면 이 탭만 옛 코드로 계속 올리게 된다.
- **남겨둔 것(판단 요청)**: 탭 두 개에서 **「내 코드」 시트를 동시에 열어둔 채** 다른 탭이 코드를 바꾸면, **시트에 보이는 코드 글자(`syncCodeText`)는 옛것으로 남는다.** 전송은 새 코드로 간다. 아주 드물다고 보고 손대지 않았다 — 맞는 판단인가?

### R2. 상세 다시 그리기 (②의 부작용)

- **사용자가 읽고 있는 화면이 갑자기 바뀐다.** 목록과 어긋나는 것보다는 낫다고 봤는데, **닫아버리는 편**이 나은가? 아니면 지금이 맞는가?
- **삭제 확인 상태가 풀린다.** `renderStampView`가 `resetStampViewDelete()`를 부르므로, 사용자가 「한 번 더 누르면 삭제돼요」 상태에서 동기화가 도착하면 **무장이 해제**된다. 의도한 것이지만(내용이 바뀐 기록을 옛 확인 상태로 지우면 안 된다) 놓친 부작용이 있는가?
- **`closeStampView()`는 `stampViewId`를 `null`로 만들지 않는다.** `refreshStampView`는 `open` 클래스도 함께 보므로 닫힌 뒤에는 안 돈다고 봤다. 맞는가? 닫힌 상태에서 `stampViewId`가 남아 문제가 되는 다른 경로가 있는가?
- **입력 시트와 겹칠 때**: 상세에서 `수정`을 누르면 상세가 닫히고 입력 시트가 열린다. 그 시트가 열려 있는 동안 **그 기록이 동기화로 바뀌거나 지워지면** 어떻게 되는가? (시트는 이 수정 범위 밖이다 — 새 결함이면 알려 달라.)
- `refreshAfterSync`는 `try` 안에서 여러 렌더를 연달아 부른다. `refreshStampView`가 던지면 **뒤의 렌더가 통째로 안 돈다.** 던질 수 있는 입력이 있는가?

### R3. 읽기 실패와 빈 서버 구분 (③의 부작용)

- **`syncOnStart`는 여전히 실패를 `null`로 받고 그대로 `pushSync`한다**(4차 ①). 트랜잭션이 서버 최신값을 다시 읽으므로 유실이 없다는 5차 판정을 그대로 따랐다. 이번 변경으로 이 전제가 흔들리는가?
- **규칙 거부(permission denied)도 `{ok:false}`로 뭉친다** → "연결이 안 돼요"라고 말한다. 형식이 틀린 코드는 화면 검사에서 먼저 걸리므로 여기 오는 건 사실상 네트워크·규칙 문제뿐이라고 봤다. 맞는가? 사용자를 오해시키는 조합이 있는가?
- **버튼 잠금 해제**(`syncLoadBtn.disabled = false`)가 **모든 분기**에서 도는가? 실패 분기가 하나 늘었다.

### R4. 여전히 남아 있는 것 (고치지 않았다)

- 트랜잭션·읽기 실패에 **자동 재시도가 없다.**
- `stamps.deleted`(지운 id)는 **영원히 쌓인다.**
- 즐겨찾기·좋아요는 구버전에서 이력 없이 꺼진 것이 되살아날 수 있다(3차에서 합의).

---

## 3. 다시 판정해 줄 것

- **D1(데이터 보존)** · **D3(해제 유지)** · **D5(Firebase 실패 격리)** — 5차에서 전부 통과였다. 이번 변경으로 깨지지 않았는가?
- **5차 3건이 제대로 고쳐졌는가** — 특히 ①이 "코드만 바꾸고 진행 중 전송은 방치"하는 형태가 아닌지.
- **곁들여 고친 탭 간 경로**가 범위를 넘었는지, 혹은 부족한지.

---

## 4. 이번에 내가(클로드) 실제로 돌려본 것

🔴 **그대로 믿지 말고 다시 확인해 달라.** 방법: `index.html`의 **Firebase CDN 두 줄만 가짜 스텁으로 교체**한 임시 페이지 + **직전 커밋(v531) 사본**을 나란히 띄워 대조. **앱 코드는 한 글자도 안 바꿨다.** ①은 **브라우저 탭 두 개**로 실제 `storage` 이벤트를 발생시켜 확인했다.

| 시나리오 | 고치기 전(v531) | 고친 뒤(v532) |
|---|---|---|
| 탭1에서 A 불러오기 → 탭2에서 B 불러오기 → 탭1에서 즐겨찾기 1회 | 저장된 코드는 B인데 **A로 3회 전송**, B 기록이 **A 서버로** 올라감 | **B로 전송**, A 서버는 원래 기록만 그대로 |
| 상세를 연 채 그 기록의 **수정본**이 동기화로 도착 | 목록 `홍대점/7월25일` ↔ 상세 `강남점/7월20일` | **양쪽 일치** |
| 상세를 연 채 그 기록의 **삭제**가 도착 → `수정` 클릭 | 상세 열린 채 남고, **날짜가 오늘로 채워진 빈 새 기록 화면**이 뜸 | **상세가 닫힘** |
| 읽기 실패 상태로 불러오기 | "입력하신 코드로 저장된 데이터가 없어요" | **"연결이 안 돼요. 잠시 뒤 다시 해주세요"** |
| 진짜 빈 서버 코드로 불러오기 | "…저장된 데이터가 없어요" | **종전 문구 유지**(오탐 아님) |
| 평소 합치기(전송 2회·다른 기기 기록 따라오기·tombstone·즐겨찾기/좋아요·카드 2개) | — | **전/후 완전히 동일** |
| 상세 열기 → X로 닫기 (포커스 복귀) | — | **전/후 동일** |

콘솔 오류 0. 실제 `index.html`도 열어 카드 37개·인기소스 1위 17 정상 확인.

**안 해본 것**: 실제 Firebase(가짜 스텁이라 네트워크·규칙·재시도가 안 걸린다) · 트랜잭션 실패/재시도 한도 · iOS bfcache 복원 · 저장소 용량 초과 · **실기기 사파리↔홈 화면**.

---

## 5. 검증 시 주의 (전부 실제로 당한 것)

1. 🔴 테스트용 코드에 **`O`·`I`·`0`·`1`을 쓰지 말 것.** 형식 검사에서 거부된다. **`A-H · J-N · P-Z · 2-9`** 만(예: `HG-TESTA2`).
2. 🔴 붙여넣기는 **`input.value` 직접 대입으로 테스트하지 말 것.** 실제 경로(`document.execCommand('insertText', …)`)를 쓸 것.
3. 🔴 **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 바뀐다.
4. 🔴 `localStorage`만 비우고 **새로고침을 안 하면** 화면이 옛 상태를 들고 있다.
5. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것(통째 삭제는 규칙이 막는다).
6. ⚠️ 브라우저 콘솔은 배경에서 타이머가 늘어진다. **시간 측정값을 믿지 말 것.**
7. ⚠️ **①은 탭 두 개가 있어야 재현된다.** `storage` 이벤트는 **다른 탭에서 쓸 때만** 발생하고, 같은 탭에서 `localStorage.setItem`을 해도 안 뜬다.

---

## 6. 아직 아무도 확인 못 한 것

⬜ **실기기에서 사파리 → 홈 화면 복구.** 이 기능을 만든 이유인데 아직 확인하지 못했다. 코드 리뷰로는 답이 안 나온다. **배포 없이 터널 주소로 된다.**

---

## 7. 참고

1차 요청서에 **기능 목적·서버 구조·보안 규칙·의도된 동작 목록**이 있다. 앱 전체 규칙은 `CLAUDE.md`, 현재 상태는 `HANDOVER.md`.
🔴 이 문서도 내부 문서다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 했다.
