# 「내 데이터 코드」 7차 교차 검증 요청서 — 6차 수정분 재검증

> **읽는 사람: 코덱스(또는 다른 검토자).**
> 🔴 **저장소 파일을 읽기만 하고 수정하지 마세요.**
> 발견한 것은 **파일:줄번호 + 증상 + 원인 + 재현 + 심각도**로 정리해 주세요.
> 🔴 **회신은 요약하지 말고 판정 표와 각 건의 원문 그대로** 주세요 — 저장소에 그대로 보관합니다(`-5-REPLY.md`·`-6-REPLY.md`).
>
> 작성 2026-07-31 / 대상 커밋 **`1bd0933`** / 캐시버전 css v624 · **script v533**
> 앞선 문서: `REVIEW-SYNC-CODE.md`(1차, **불변식 D1~D8 정의**) · `-2`~`-6` 요청서 · `-5-REPLY` · `-6-REPLY`

## 🔴 배포 전 검증이다

- 대상은 **작업 브랜치 `fix/sync-review4`**, **라이브는 아직 `3c28840`(script v530)**이다.
- ⚠️ **라이브(haidilgogo.com)로 검증하면 안 된다.** 이 브랜치를 로컬에서 띄워 볼 것.

```bash
git fetch origin && git checkout fix/sync-review4
```

---

## 0. 6차 검토 결과 — 여섯 번째도 맞았다

**2건 다 사실이었고 재현한 뒤 고쳤다.** 나머지 위험 항목 9개는 전부 "현재 방식이 적절"로 판정받아 손대지 않았다.

🔴 **발견 1은 내 판단을 뒤집은 것이다.** 나는 5차 때 "열린 시트의 코드 표시는 아주 드무니 그대로 둔다"고 적었는데, **복사·보내기가 화면과 다른 값을 준다**는 근거가 결정적이었다. 그 화면의 존재 이유가 복구 코드를 정확히 적어두게 하는 것이다.

**확정되어 더 안 볼 것**(다시 제안하지 말 것): `R2-a`(전환 중 A 데이터 유입) = 설계상 정상 / 상세는 닫기보다 다시 그리기 / 빈 코드일 때 메모리 사본 유지 / 권한 거부를 "연결이 안 돼요"로 묶기.

---

## 1. 이번에 무엇을 바꿨나 (재검증 대상)

| # | 6차 지적 | 수정 | 위치 |
|---|---|---|---|
| 1 | 열린 「내 코드」 시트에 옛 코드가 남음 | **`renderSyncCode()` 신설** — 코드 글자와 '내 코드' 덩어리 표시를 한 곳에서 그린다. **세 경로**(시트 열기 · 불러오기 성공 · 탭 간 코드 변경)에서 부른다 | `renderSyncCode` · `openSyncSheet` · 불러오기 핸들러 · `syncExternalState` |
| 2 | 수정 시트가 열린 동안 기록이 삭제되면 저장이 조용히 무시됨 | 저장 시 그 id가 없으면 **방금 쓴 내용을 새 기록으로 남긴다**(새 `id`·`addedAt = Date.now()`) | `stampSubmitEl` 클릭 핸들러의 `edit` 분기 |

🔴 **2번은 사용자가 고른 방향이다.** 후보는 셋이었다 — ⓐ 새 기록으로 살리기(선택) / ⓑ 시트를 그 자리에서 닫기(입력 중이던 글이 사라진다) / ⓒ 안내 문구 띄우기(새 문안이 필요해 보류). **'지운 건 되살아나지 않는다'는 규칙과 충돌하지 않는다**는 것이 선택 근거다: 새 `id`라 tombstone(`deleted`)에 걸리지 않고, 지워진 그 기록은 지워진 채 남는다. 되살아나는 건 **사용자가 지금 쓴 내용**뿐이다.

---

## 2. 🔴 이번 수정이 새로 만들었을 위험

여섯 번 연속 결함이 나왔다. 이번에도 그럴 것으로 보고 봐 달라.

### R1. `renderSyncCode` 통합 (1의 부작용)

- **시트가 닫혀 있을 때도 불린다**(탭 간 경로). 숨은 요소의 글자와 `syncMine.hidden`을 바꾸는 것뿐이라 무해하다고 봤다 — 다음에 시트를 열면 `openSyncSheet`가 어차피 다시 그린다. 맞는가?
- **사용자가 보고 있는 코드 글자가 소리 없이 바뀐다.** 안내 없이 바뀌는 것이 맞는가? (안내 문구를 넣으려면 사용자 확정이 필요해 넣지 않았다.) 사용자가 **옛 코드를 이미 종이에 적은 뒤**라면 어떤가 — 이 화면이 할 수 있는 일이 더 있는가?
- **복사 버튼과 코드 변경이 겹치는 순간**: 복사는 여전히 `getSyncCode()`를 읽는다. 화면 갱신과 클립보드 쓰기 사이에 어긋나는 창이 남아 있는가?
- 불러오기 성공 경로는 `setSyncCode(code)` **뒤에** `renderSyncCode()`를 부른다. 이 순서에 의존하는데, 앞뒤가 바뀌면 빈 값이 그려진다. 지금 코드에서 그 순서가 보장되는가?
- 세 경로가 같은 함수를 부르면서 **각 경로가 원래 하던 다른 일**(예: 불러오기 성공 시 덩어리 열기)이 빠지지 않았는가?

### R2. 삭제된 기록의 수정 저장 → 새 기록 (2의 부작용)

- **다른 기기 눈에는 "지운 게 되살아난 것"처럼 보인다.** 매장·날짜가 같은 기록이 다시 나타나기 때문이다. 실제로는 새 id지만 **사용자가 구분할 수 있는가?** 혼란이 유실보다 나쁜 조합이 있는가?
- **중복이 생기는 조합**: 두 기기가 같은 기록의 수정 시트를 열어둔 채 세 번째 기기가 그 기록을 지우면, 둘 다 저장할 때 **비슷한 기록이 두 개** 생긴다. 실제로 그렇게 되는가? 막을 값어치가 있는가?
- **정렬이 튄다**: 새 기록의 `addedAt`이 지금 시각이라 **같은 날짜 안에서 맨 위로** 온다(정렬 규칙: 날짜 내림차순 → `addedAt` 내림차순). 원래 자리에 있던 기록이 위로 뛰는 셈인데 사용자가 놀라는가? `addedAt`을 옛 값으로 살릴 방법이 없다(기록이 이미 사라져 읽을 수 없다) — 맞는가?
- **`editedAt`이 없다**(새로 만든 기록이라). 합치기의 `touchedAt = max(editedAt, addedAt)` 비교에서 이상한 조합이 나오는가?
- `stampCardCache.delete(stampEditId)`가 **이제 없는 id를 지운다.** 무해하다고 봤다(Map.delete는 없는 키에 안전). 새 id의 카드는 `renderStamps`가 새로 만든다. 맞는가?
- **일반 흐름이 오염되지 않았는가**: 기록이 멀쩡히 있는 평소 수정에서 새 기록이 생기는 경로가 조금이라도 있는가? (`stampMode === 'edit'`인데 `find`가 실패하는 다른 원인이 있는가 — 예: id가 바뀌는 경로)

### R3. 여전히 남아 있는 것 (고치지 않았다)

- 트랜잭션·읽기 실패에 **자동 재시도가 없다.**
- `stamps.deleted`(지운 id)는 **영원히 쌓인다.**
- 즐겨찾기·좋아요는 구버전에서 이력 없이 꺼진 것이 되살아날 수 있다(3차에서 합의).
- **수정 시트 자체는 동기화를 안 본다.** 이번엔 '저장할 때'만 막았다 — 시트가 열려 있는 동안 그 기록이 **다른 내용으로 바뀌면** 사용자는 옛 값을 편집하고 저장해 다른 기기의 수정을 덮는다. 고칠 값어치가 있는가?

---

## 3. 다시 판정해 줄 것

- **D1(데이터 보존)** · **D3(해제 유지)** · **D5(Firebase 실패 격리)** — 세 회차 연속 통과였다. 이번 변경으로 깨지지 않았는가?
- **6차 2건이 제대로 고쳐졌는가.**
- 🔴 **이제 그만해도 되는가?** 여섯 회차 동안 매번 결함이 나왔지만 최근 두 회차는 **경계 상황**이었고 핵심 동작은 계속 통과다. **배포해도 되는 상태인지 의견을 달라** — 남은 것이 "실기기 확인"뿐이라고 보는가, 아니면 코드에서 더 볼 곳이 있는가.

---

## 4. 이번에 내가(클로드) 실제로 돌려본 것

🔴 **그대로 믿지 말고 다시 확인해 달라.** 방법: `index.html`의 **Firebase CDN 두 줄만 가짜 스텁으로 교체**한 임시 페이지 + **직전 커밋(v532) 사본**을 나란히 대조. **앱 코드는 한 글자도 안 바꿨다.** 1번은 **브라우저 탭 두 개**로 실제 `storage` 이벤트를 발생시켜 확인했다.

| 시나리오 | 고치기 전(v532) | 고친 뒤(v533) |
|---|---|---|
| 탭1에 코드 A 시트를 열어둔 채 탭2에서 B 불러오기 | 시트 표시 `TESTA2` / 실제 `HG-TESTB3` — **어긋남** | **둘 다 `TESTB3`**, 시트는 열린 채 유지 |
| 수정 시트를 연 사이 삭제 도착 → 메모 고치고 저장 | **기록 0개**, 시트만 닫힘 | **새 기록으로 저장**(메모 '고친 메모'), 목록 표시, 서버 업로드 확인 |
| 〃 — tombstone | — | **`deleted`에 `st1` 그대로**(지운 기록은 안 살아남) |
| 평소 수정(기록이 있을 때) | — | 기록 2개 유지 · 그 기록만 변경 · `editedAt` 찍힘 · **새 기록 안 늘어남** |
| 코드 없는 기기에서 불러오기 | — | 덩어리 열림 · 6자리만 표시 — **전/후 동일** |

콘솔 오류 0. 실제 `index.html` v533·카드 37개 정상.

**안 해본 것**: 실제 Firebase(가짜 스텁이라 네트워크·규칙·재시도가 안 걸린다) · 세 기기 동시 조작 · iOS bfcache 복원 · 저장소 용량 초과 · **실기기 사파리↔홈 화면**.

---

## 5. 검증 시 주의 (전부 실제로 당한 것)

1. 🔴 테스트용 코드에 **`O`·`I`·`0`·`1`을 쓰지 말 것.** **`A-H · J-N · P-Z · 2-9`** 만(예: `HG-TESTA2`).
2. 🔴 붙여넣기는 **`input.value` 직접 대입으로 테스트하지 말 것.** 실제 경로(`document.execCommand('insertText', …)`)를 쓸 것.
3. 🔴 **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 바뀐다.
4. 🔴 `localStorage`만 비우고 **새로고침을 안 하면** 화면이 옛 상태를 들고 있다.
5. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것(통째 삭제는 규칙이 막는다).
6. ⚠️ 브라우저 콘솔은 배경에서 타이머가 늘어진다. **시간 측정값을 믿지 말 것.**
7. ⚠️ **탭 간 항목은 탭 두 개가 있어야 재현된다.** `storage` 이벤트는 **다른 탭에서 쓸 때만** 발생한다.
8. ⚠️ 서버 전송은 **800ms 묶음(debounce)** 뒤에 나간다. 저장 직후에 세면 "안 올라갔다"는 오판이 난다.

---

## 6. 아직 아무도 확인 못 한 것

⬜ **실기기에서 사파리 → 홈 화면 복구.** 이 기능을 만든 이유인데 아직 확인하지 못했다. 코드 리뷰로는 답이 안 나온다. **배포 없이 터널 주소로 된다.**

---

## 7. 참고

1차 요청서에 **기능 목적·서버 구조·보안 규칙·의도된 동작 목록**이 있다. 앱 전체 규칙은 `CLAUDE.md`, 현재 상태는 `HANDOVER.md`.
🔴 이 문서도 내부 문서다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 했다.
