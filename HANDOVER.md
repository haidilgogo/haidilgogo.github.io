# 하딜고고 인계 문서

작성: 2026-07-25 / 작성자: Claude (Opus 5) / 인계 대상: Codex

> **먼저 `CLAUDE.md`를 읽을 것.** 프로젝트 규칙(스크롤바·캐시 버전·스티커 파이프라인·재료 표기)이 거기 있고, 이 문서는 그 위에 "지금 어디까지 왔는지"만 얹은 것이다.

> **⚠️ 현재 Codex는 읽기 권한만 부여받은 상태다(사용자 지시).** 파일 수정·커밋·푸시를 하지 말 것. 발견한 것은 보고만 하고, 고칠지는 사용자가 정한다. 권한이 바뀌면 아래 "다음 작업 순서"를 따르면 된다.

---

## 1. 현재 작업 목적

`feat/bottom-nav` 브랜치에서 **모바일 앱 형태로의 전면 개편**을 진행 중이다. 하단 탭바(레시피·메뉴·매장·스티커) 구조를 도입하고, 화면을 하나씩 옛 "TCG 카드" 문법에서 **클린 카드 문법**으로 옮기고 있다.

2026-07-25 이 세션의 목표는 **레시피 상세(모달) 개편**이었고, 그 과정에서 파생된 버그 수정과 아이콘·정렬 정비까지 진행했다.

**배포 계획: 메뉴 탭까지 완성되면 `main`에 한 번에 배포한다.** 그전까지 `main`은 건드리지 않는다.

---

## 2. 완료한 기능 (이 세션)

### 레시피 상세(모달) 전면 개편
사이트에서 마지막까지 남아 있던 옛 TCG 문법(크림 액자 + 카테고리 틴트 본체 + 상단 50px 밴드)을 걷어냈다.

- 액자·틴트 제거 → 크림 바탕(`--bg-card`) 한 겹. `.modal-card`가 스크롤 컨테이너.
- **상단 줄(`.modal-topline`)**: 왼쪽 = 카테고리 텍스트(알약 없음), 오른쪽 = 즐겨찾기 → 닫기. 셋 다 `#4D4139`.
- 이미지: 좌우 margin 16px, 둥근 모서리 12px, `--card-border` 1px 유지, 위에 아무것도 안 얹음.
- 출처: 이미지 안 좌하단.
- 이름: 이미지 아래 좌측 23px/800, 부제(`ver`)는 아랫줄. 별은 첫 글자 좌상단 대각선.
- 설명: 박스 없이 문단.
- 섹션(주문·소스바·만드는 법·팁): 전부 흰 박스, **팁만 로즈 톤**(`#FDF1EE` / 테두리 `#F1D5CD`).
- 섹션 아이콘 없음(이모지 전부 제거). 팁의 💡도 제거.
- 좋아요: 최하단 가운데 알약 유지.
- 배지: 소스 상위 5위면 순위 배지, 이달의 소스면 '이달의 소스' 배지. 둘 다면 가로로 나란히.

### 순위 배지 체계
- **쿠팡이츠 방식**: 알약 5개는 같은 검정 알약, **1~3위만 안에 메달 아이콘**(금·은·동), 4~5위는 글자만.
- 메달 = `assets/icons/medal/{gold,silver,bronze}.svg` (원본은 `02. Data/Haidilgogo/icon/svg/medal/` 세트 1).
- 폭은 내용에 맞게 자동(1~3위 45~47px / 4~5위 30~31px), **높이는 5개 전부 17.75px 동일**.
- 노출 범위: **브라우즈 소스 탭 + 홈 인기소스 레일 + 모달**. '전체' 탭·검색·즐겨찾기·인물 필터에서는 숨김.

### 셀럽 별
- 홈 **"셀럽 레시피" 섹션 제목**에 별을 달아 **범례** 역할(별 = 셀럽 소스임을 여기서 학습).
- 홈 인기소스 카드에서는 별 제거(순위 배지와 겹쳐 산만).
- 브라우즈 그리드·모달에는 별 유지. 위치는 첫 글자 좌상단 대각선.

### 탭바 아이콘
- 매장 = `store-1`(채움 아이콘), 스티커 = `1_sticker-smile-circle`(선 아이콘). 둘 다 인라인 SVG + `currentColor`.
- 네 아이콘 글리프 높이 15.6~16.8px로 정렬.

### 가챠(오늘의 소스)
- 결과 카드를 **브라우즈 카드 스타일**로 교체(좋아요 없음 / 즐겨찾기 있음 / 배지 없음 / 별 있음).
- 결과 카드를 `.gacha-stage` 안으로 옮기고 `inset:0` — 매직 넘버 제거.
- 닫기 버튼을 우상단으로 되돌리고 다른 닫기 버튼과 크기·굵기 통일.

### 아이콘 액션 버튼 통일
상단바(즐겨찾기·공유·설치), 모달(즐겨찾기·닫기), 브라우즈 닫기, 스티커 시트/기록보기 닫기를 **전부 18px 박스 / 글리프 높이 15px / 화면 획 굵기 1.7**로 통일. 터치 타겟은 44px 유지(스티커 시트 2개는 예외, 아래 참고).

---

## 3. 수정한 파일

`main`(`6e6ec03`) 대비 이 브랜치의 누적 변경:

| 파일 | 변경 |
|---|---|
| `script.js` | +2425줄 — 데이터·렌더링 로직 전반 |
| `styles.css` | +3478줄 — 전 화면 스타일 |
| `index.html` | +485줄 — 탭바·모달·가챠·매장·스티커 마크업 |
| `manifest.json` | PWA 매니페스트 소폭 |
| `assets/` | 스티커 webp, 메달 SVG, 셀럽 사진, 배너 이미지 등 신규 |

총 37개 파일, +5666 / -802.

**이 세션에서 실제로 손댄 것은 `index.html` · `styles.css` · `script.js` 세 개뿐이다**(+ `assets/icons/medal/` 3개 신규, `CLAUDE.md` 문서 보강).

---

## 4. 아직 완료하지 못한 작업

| 작업 | 상태 | 막고 있는 것 |
|---|---|---|
| **메뉴 탭** | 빈 화면(준비 중) | Codex가 `02. Data`에서 사진·목록 정리 중 |
| **baseLikes** | 미구현 | 사용자가 집에서 숫자 확정 예정 |
| **준비중 배너**(메인 배너 4개 중 3번) | 임시 콘텐츠(무지 배경 + 🚧) | 콘텐츠·이미지 결정 |
| **모달 뒤로가기로 닫기** | 미착수, 방향만 합의됨 | 사용자 착수 지시 |
| **실기기 확인 → main 배포** | 대기 | 메뉴 탭 완성 |
| 죽은 코드 정리 | 미착수 | 아래 6번 참고 |
| 스티커 시트/기록보기 닫기 터치 타겟 34px | 미해결 | — |
| 모달·브라우즈 닫기 버튼 위치 | ✅ 해결(아이콘 x좌표까지 일치) | — |

---

## 5. 확정된 사용자 결정 (뒤집지 말 것)

이미 논의를 거쳐 확정된 것들이다. 다시 제안하지 말 것.

1. **정렬 = 인기순 > 동점이면 최신순**(`byPopular`). 가나다순 아님 — 'ㄱ' 소스가 영구히 유리해져 새 레시피가 계속 밀리기 때문.
2. **순위 배지는 소스 탭·홈 인기소스·모달에서만.** 카테고리가 섞이는 화면('전체' 탭·검색·즐겨찾기)에서는 숨긴다 — '2위'가 무엇의 2위인지 맥락이 없어 혼란스럽다.
3. **"좋아요 0이면 배지 숨기기"는 하지 않는다.** baseLikes가 들어가면 동점 자체가 사라져 무의미하다.
4. **이달의 소스 배지는 모달 + 브라우즈 카드에만.** 홈은 최상단 배너에 이미 크게 떠 있어 중복.
5. **모달 상단 줄을 sticky로 고정하지 않는다.** 화면 위에 고정되는 줄은 상단바 하나만 유지하고 싶다는 사용자 방침.
6. **섹션 아이콘은 쓰지 않는다**(글자만). 17px에서 선 아이콘 획이 뭉개져 "안 예쁘다"는 판단.
7. **좋아요 알약(999px) 유지.** 아래쪽에서 유일하게 터치 가능한 요소라 알약이 안정적으로 보인다.
8. **소스 카드 이미지는 실제 캡쳐를 유지한다. Gemini 일러스트화 금지.**
9. **작업 브랜치는 커밋할 때마다 푸시한다**(백업). GitHub Pages는 `main`만 서빙하므로 라이브에 영향 없음. **`main`은 사용자 지시가 있을 때만.**
10. 화면에 보이는 이름은 **'스티커'**, 코드 내부 식별자는 `stamp` 유지(바꾸면 연쇄 회귀).

---

## 6. 발견된 버그와 주의사항

### 이 세션에서 고친 버그 (원인 유형이 재발 가능하므로 기록)

1. **순위 배지가 필터 상태에서 틀림** — `renderGrid()`가 필터된 배열의 인덱스를 순위로 썼다. → `sauceRankMap()`(소스 전체 절대 순위)을 매 렌더 재계산.
2. **표시 순서와 배지가 서로 다른 기준** — `getFiltered()`만 동점 타이브레이크가 가나다순이었다. → `byPopular`로 통일. **정렬 기준은 코드에 한 곳뿐이어야 한다.**
3. **모달에서 공유 버튼 누르면 화면 전환** — 상단바에 캡처 단계 클릭 리스너가 있어 `.topbar` 안 아무 클릭이나 모달을 닫았다. → `#topShareBtn` 예외 처리. **상단바에 새 버튼을 추가할 때마다 이 리스너를 확인할 것.**
4. **호버 시 순위 메달이 카드만큼 부풀어 오름** — `.hp-thumb img`가 후손 선택자라 배지 안의 메달 `<img>`까지 잡았다. → 전부 `> img` 자식 선택자로. **썸네일 안에 새 `<img>`를 넣을 때 재발한다.**
5. **가챠 결과 카드가 제목을 덮음** — `top:67px` 하드코딩이 실제 스테이지 위치(94px)와 어긋났다. → 스테이지 안으로 옮기고 `inset:0`.
6. **하트 활성 상태가 화면마다 따로 놈** — 모달 핸들러가 없는 선택자(`.recipe-grid .like-btn`)를 찾고 있었고, `refreshLikeCounts()`는 숫자만 갱신한다. → `syncLikeUI(id)` 헬퍼로 통일. **새 하트 UI를 추가하면 이 선택자에도 넣을 것.**
7. **즐겨찾기를 눌러도 하트가 통통 튐** — `.pop` 클래스가 애니메이션 후에도 남아, 그리드 재렌더로 DOM에 재삽입될 때 애니메이션이 재생됐다. → `popHeart()`가 타이머로 클래스를 뗀다.
8. **새로고침하면 하트만 초기화** — Firebase에서 숫자가 올 때마다 `saveLikes()`가 "내가 누른 것"까지 덮어썼다. → `saveLikeCounts()`(집계 전용) 분리. **집계 갱신은 절대 `likedByMe`를 건드리면 안 된다.**
9. **즐겨찾기 켤 때 스크롤이 안 올라감** — 토글 핸들러에만 `window.scrollTo({top:0})`가 빠져 있었다.
10. **만드는 법 숫자와 글자가 3.5px 어긋남** — `line-height`가 `normal` vs `1.55`로 달랐다.

### 알려진 미해결 사항

- **`buildCard()`와 `.recipe-card` CSS는 죽은 코드다.** 가챠까지 브라우즈 카드로 바뀌면서 호출부가 사라졌다. 삭제하면 검증 범위가 커져서 이번엔 남겨뒀다.
- **`.recipe-name { font-size: 24px }`는 죽은 값** — 아래 `@media all`의 15px에 항상 덮인다.
- **`@media (min-width: 100000px)`** 는 절대 적용 안 되는 옛 데스크탑 흔적이다.
- **스티커 시트·기록보기 닫기 버튼의 터치 타겟이 34px**(44px 기준 미달).
- **모달·브라우즈·시트·가챠에 history 처리가 없다.** 안드로이드 뒤로가기나 iOS 엣지 스와이프를 하면 오버레이가 닫히는 게 아니라 **사이트를 떠난다.** 모달의 유일한 출구가 X 버튼인데 그게 본문과 함께 스크롤돼 사라지므로, 긴 레시피에서는 맨 위로 올라가야 닫을 수 있다.
- **소스 20개 중 17개가 좋아요 0으로 동점.** 4·5위 배지는 사실상 임의로 뽑힌 둘이다(날짜까지 같으면 `RECIPES` 배열 순서가 3단계 타이브레이크).

### 🔴 작업 시 함정 (실제로 여러 번 걸린 것들)

- **`el.hidden = true`만으로는 안 숨겨진다.** author CSS가 `display:flex/block`이면 UA의 `[hidden]{display:none}`을 이긴다 → `.foo[hidden]{display:none}`을 명시적으로 재선언할 것. (3번 걸림)
- **CSS 특이성.** `.hp-thumb img`(0,1,1)가 `.hp-medal`(0,1,0)을 이겨 메달이 썸네일 크기로 부풀었다. 카드 안에 새 요소를 넣을 때 기존 후손 선택자에 걸리는지 확인할 것.
- **아이콘 크기는 박스가 아니라 글리프로 맞춘다.** 같은 18px 박스여도 글리프가 viewBox를 채우는 비율이 달라 크기가 달라 보인다. `getBBox()` × (표시크기/viewBox)로 실측하고, `<g transform="translate(12 12) scale(N) translate(-12 -12)">`로 보정하되 **stroke-width를 N으로 나눌 것**.
- **`getBoundingClientRect()`로 박스만 맞추면 안 된다.** 눈에 보이는 건 아이콘이다. 모달 닫기 버튼이 박스는 같은데 패딩이 비대칭이라 아이콘만 3px 어긋난 적이 있다.
- **Bash로 한글 문자열을 치환하지 말 것**(자모 분리로 깨진 적 있음). 파일 수정은 에디터 도구로.
- **좋아요는 Firebase에 전체 공유된다.** 로컬에서 테스트로 눌러도 **실제 사이트 숫자가 올라간다.** 테스트 전에 `firebase.database().ref('likes').off()`로 리스너를 끊거나, 누른 뒤 반드시 되돌릴 것. (이번 세션에서 건희소스 3→4, 화령소스 2→4로 오염됐고, 사용자가 baseLikes 때 정리하기로 해서 그대로 뒀다.)

---

## 7. 실행 및 테스트 방법

**자동화된 테스트 스위트는 없다.** 빌드 과정도 없다(순수 HTML/CSS/JS).

### 로컬 실행
```bash
cd "/Users/Macmini_J/ClaudeCode/03. Projects/Haidilgogo"
python3 -m http.server 8931
# → http://localhost:8931
```

### 실기기(폰) 확인
cloudflared 터널을 쓴다. ⚠️ 터널 주소가 바뀌면 브라우저가 다른 사이트로 취급해 `localStorage`(좋아요·즐겨찾기·스티커)가 초기화된 것처럼 보인다.

### 확인해야 할 화면
모바일 뷰포트(375×812) 기준. 데스크탑에서도 폭 440px 단일 레이아웃이다.

1. 홈 — 배너 캐러셀 4개 / 셀럽 레일 8명 / 인기소스 5개(1~3위 메달) / 히든메뉴 리스트 / 탕 그리드
2. 브라우즈 — 전체·소스·히든메뉴·탕 탭, 검색, 즐겨찾기
3. 레시피 상세 모달 — 배지·별·섹션·좋아요
4. 가챠 — 저장된 결과 / 새로 뽑는 연출 둘 다
5. 매장·스티커·메뉴 탭

### 가챠 테스트 (하루 한 번 제한 우회)
```js
localStorage.setItem('haidilao_gacha_daily', JSON.stringify({date:'2026-07-25', id:'s5'}));
// 새로 뽑는 연출: localStorage.removeItem('haidilao_gacha_daily')
// ⚠️ 테스트 후 반드시 removeItem
```

### 이 세션 스모크 테스트 결과 (2026-07-25, `7e9d5e9`)
| 항목 | 결과 |
|---|---|
| 홈 배너 / 셀럽 / 인기소스 | 4 / 8 / 5 ✅ |
| 홈 순위 배지 5개 중 메달 3개 | ✅ |
| 홈 인기소스 별 0개, 셀럽 제목 별 1개 | ✅ |
| 브라우즈 소스 탭 카드 20개, 배지 5개, 이달의 소스 1개, 별 14개 | ✅ |
| 모달 열림·닫힘, 카테고리·섹션 렌더 | ✅ |
| 탭 4개 전환(레시피·메뉴·매장·스티커) | ✅ |
| 콘솔 에러 | 0건 ✅ |
| 깨진 이미지 | 0건 ✅ |

### ⚠️ 브라우저 자동화 측정 함정
미리보기 창이 백그라운드 탭이면(`document.hidden === true`):
- `window.innerWidth`가 **0**으로 나온다 → 가로 스크롤 검사가 전부 거짓 양성이 된다
- **CSS 애니메이션이 멈춰 `animationend`가 영영 안 온다**
- `scroll-behavior: smooth` 때문에 `scrollTo` 직후 위치를 읽으면 옛 값이 나온다 → `behavior:'instant'`를 쓸 것

---

## 8. 다음 작업 순서

권한을 받으면 이 순서를 권한다.

1. **전수조사 보고**(현재 지시받은 작업) — 읽기만 하고 발견 사항을 정리. 위 6번에 적힌 것은 이미 아는 것이니 중복 보고하지 말 것.
2. **모달 뒤로가기로 닫기** — 방향 합의됨. 레시피 상세 모달만 먼저(브라우즈·시트·가챠는 나중에 한 번에). ⚠️ 모달은 셀럽 스토리 위에서도 열린다(스토리 → "레시피 보기"). 그 상태에서 뒤로가기는 **모달만 닫고 스토리는 남아야 한다.**
3. **메뉴 탭 구현** — `02. Data`의 정리 결과가 나오면. 데이터는 `script.js` 안 배열로 넣는다(별도 JSON 없음). 카드 이미지는 1:1 정사각 1080×1080.
4. **baseLikes** — 사용자가 숫자를 주면.
5. **준비중 배너 콘텐츠**.
6. **죽은 코드 정리** — `buildCard()`, `.recipe-card` CSS, `.recipe-name` 죽은 값 등. 한 번에 지우고 전 화면 회귀 확인.
7. **실기기 확인 → `main` 배포**(사용자 지시 필요).

### 작업 시 반드시
- 고친 파일만 `index.html`의 `?v=` 숫자를 올린다(현재 **css v563 / script v401**). `index.html`만 고쳤으면 올리지 않는다.
- 스크롤바 CSS를 새로 쓰지 않는다(전역 `*` 규칙이 처리).
- 커밋 후 `git push origin feat/bottom-nav`. **`main` 금지.**

---

## 9. 현재 브랜치와 커밋 상태

```
브랜치: feat/bottom-nav
HEAD:   7e9d5e9  (원격 origin/feat/bottom-nav 와 동일)
main:   6e6ec03  (로컬·원격 동일, 이 브랜치 작업 반영 안 됨 = 라이브 사이트 그대로)
작업 트리: 깨끗
main 대비: 이 브랜치가 앞섬 (미배포)
```

### 이 세션 커밋 21개 (오래된 순)

```
311fa45 feat(전체보기): 소스/히든메뉴/탕 브라우즈 화면 개편 (css v550/script v388)
302364f feat(레시피 상세): 액자 제거 + 클린 레이아웃 개편 (css v551)
fb6ecc0 fix(순위 배지·모달): 절대 순위 배지 버그 수정 + 모달 배지·별·버튼 정비 (css v552/script v389)
8edd811 fix(리포트 7건): 배지 노출 축소·별 확대·정렬·닫기버튼 정비 (css v553/script v390)
a94d1a0 fix(순위 배지·팁 박스): 1위 배지 리본→금색 알약 통일, 팁 💡 제거 (css v554/script v391)
ea6c667 fix(순위 배지): 금색 알약+왕관 되돌리고 쿠팡이츠식 메달 아이콘으로 교체 (css v555/script v392)
2341220 fix(카드 호버): 썸네일 확대 규칙이 순위 메달까지 잡던 문제 (css v556)
e657bd3 fix(아이콘 액션 버튼): 상단바·모달·브라우즈·발도장 닫기 18px 시각 통일 (css v557)
f8eda6d fix(탭바 아이콘·별 위치): 매장/스티커 아이콘 교체, 인기소스 별 제거, 셀럽 별 범례, 별·닫기버튼 위치 재조정 (css v558/script v393)
76a7660 docs(CLAUDE.md): 푸시(백업)와 배포(main)를 명확히 구분
e44f70f style(탭바): 매장·스티커 아이콘 살짝 확대 (글리프 16.5px로)
510be10 style(셀럽 별): 첫 글자 좌상단 대각선에 밀착 + 브라우즈 별 확대 (css v559)
0ae2be1 fix(브라우즈 정렬): 표시 순서와 순위 배지가 서로 다른 기준을 쓰던 문제 (script v394)
4cd7ca7 feat(브라우즈 카드): 그리드 카드에 '이달의 소스' 배지 추가 (css v560/script v395)
f87504f fix(즐겨찾기): 켤 때 스크롤이 맨 위로 안 가던 문제 (script v396)
fc63494 fix(모달): 만드는 법 세로 정렬 + 닫기 아이콘 위치 (css v561)
0e558c2 fix(가챠): 결과 카드가 '오늘의 소스' 제목을 덮던 문제 (css v562)
d9ee3ab fix(가챠): 닫기 위치 회귀 + 결과 카드를 브라우즈 카드 스타일로 (css v563/script v397)
c0757c5 fix(좋아요): 하트 활성 상태가 화면마다 따로 놀던 문제 (script v398)
a31be97 fix(하트 팝): 즐겨찾기를 눌러도 하트가 통통 튀던 문제 (script v400)
7e9d5e9 fix(좋아요): 새로고침/재진입 시 하트만 초기화되던 문제 (script v401)
```

---

## 10. 동시 작업 규칙

지금 `02. Data/Haidilgogo`(메뉴 탭 사진·목록 정리)와 `03. Projects/Haidilgogo`(저장소) 양쪽에서 작업이 돌 수 있다.

- **저장소를 고치는 주체는 한 번에 하나만.** 두 도구가 같은 `script.js`를 고치면 서로의 변경을 덮어쓴다(실제로 한 번 위험했다).
- 데이터 폴더(`02. Data`)는 **원본 아카이브**다. 읽기는 자유롭게, 수정·삭제는 하지 말 것.
- 이 문서(`HANDOVER.md`)는 저장소 루트에 있어 `main` 배포 시 공개된다. 비밀 정보는 없지만, 거슬리면 배포 전에 지우면 된다.
