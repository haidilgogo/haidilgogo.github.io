# 하딜고고 인계 문서

최종 갱신: 2026-07-30 / 인계 대상: 새 대화(Claude·Codex 공통)

> 2026-07-28 작업 분담이 바뀌었다. **가이드 문안 작성도 Claude에서 진행 중**이며, 코드 수정은 Claude만 한다.
> Codex에 문안을 맡길 때는 "저장소 파일 읽기만, 수정 금지"를 명시한다.

> **가장 먼저 `CLAUDE.md`를 끝까지 읽을 것.** 프로젝트의 배포, 스크롤바, 이미지 원본, 캐시 버전, 데이터 표기 규칙은 `CLAUDE.md`가 기준이다.
>
> **현재 최우선 작업은 「하이디라오 첫 방문 가이드」다.** STEP 1·2 확정 완료, STEP 3부터. 이 문서 맨 앞의 `최신 작업`을 먼저 읽고 마지막 지점부터 이어간다. 뒤쪽의 잡학 아티클(옛 이름 알쓸신잡) 기록은 완료된 이전 작업 이력이다 — 단, **제목·부제·배너 이미지는 다시 열려 있다.**
>
> 사용자가 제공한 PDF 기획초안보다 이 문서에 적힌 **이후 확정 사항과 현재 커밋 상태가 우선**한다. 사용자가 요청하지 않은 문구를 임의로 추가하거나 코드를 먼저 수정하지 않는다.

---

# 최신 작업: 하이디라오 첫 방문 가이드

## A. 현재 저장소 상태

```text
경로: /Users/Macmini_J/ClaudeCode/03. Projects/Haidilgogo
브랜치: main (2026-07-30 배포 후 그대로 체크아웃돼 있음)
작업 브랜치: feat/base-likes (= main과 같은 위치). feat/bottom-nav도 남아 있으나 내용은 전부 병합됨
최신 커밋: faa0add docs: 본문 HTML화 옵션 분석 기록 — 다음 대화 시작점
main: faa0add — 🚀 배포됨 (코드 최신은 8bfc023, 이후는 문서만) (로컬·원격·작업브랜치 전부 동일)
작업 트리: 깨끗함
캐시 버전: styles.css?v=605 / script.js?v=502
라이브 주소: **https://haidilgogo.com** (2026-07-30 커스텀 도메인 연결)
```

### 🌐 2026-07-30 커스텀 도메인 연결 — `haidilgogo.com`

가비아에서 구매(19,800원/1년, **만료 2027-07-30**) → GitHub Pages에 연결. HTTPS는 Let's Encrypt
자동 발급·자동 갱신. `Enforce HTTPS` 켜져 있다.

- **옛 주소 `haidilgogo.github.io`는 깃허브가 새 주소로 자동 전달**한다(301). 이미 공유된 링크는
  전부 살아 있어서 아무에게도 다시 알릴 필요가 없었다. `www`도 본주소로 전달된다.
- 저장소의 `CNAME` 파일은 깃허브가 자동 생성한 것이다(내용: `haidilgogo.com`). **지우지 말 것.**
- `manifest.json`은 `start_url`·`scope`가 상대경로(`./`)라 손댈 것이 없었다. 주소가 박혀 있던
  곳은 `og:url` 한 줄뿐이었다.
- 곁들여 `og:image`·`twitter:image`를 절대 주소로 바꿨다 — 상대 경로는 카카오톡 같은 일부
  미리보기 수집기가 해석하지 못해 이미지가 안 뜬다. `link rel=canonical`도 신설했다(주소가
  셋이 되었으므로 원본을 못박는다).
- 🔴 **아이폰 홈화면에 추가한 사람**은 시작 주소가 다른 도메인으로 전달되면서 전체화면(standalone)이
  깨질 수 있다(확인 못 함). 그 경우 홈화면 아이콘을 지우고 다시 추가해야 한다. 아이콘이 옛 로고인
  것도 같은 이유로 원격에서 못 고친다 — iOS가 추가 시점에 아이콘을 복사하기 때문이다.

### 🔴 2026-07-30 보안 — 내부 문서가 웹에 공개돼 있었다 (`66f3a49`)

**`https://haidilgogo.com/HANDOVER.md`가 HTTP 200으로 61KB를 그대로 내주고 있었다.** `CLAUDE.md`도
18KB 공개. 이 저장소는 루트가 그대로 서빙되는데 두 문서가 루트에 있어서다. 검색엔진에 막 등록한
직후여서 크롤러가 긁어가기 전에 막았다.

- 비밀번호·키는 없지만 미공개 계획·판단 근거·커밋 이력, 그리고 **좋아요 숫자가 더미라는 사실**까지
  들어 있다.
- 🔴 **`robots.txt`로는 못 막는다.** 그건 크롤러에게 부탁하는 것뿐이고 주소를 아는 사람은 그대로
  읽을 수 있다. **`_config.yml`의 `exclude`로 파일을 아예 안 내보내야 404가 된다.**
- ⚠️ **새 내부 문서를 저장소 루트에 추가하면 `_config.yml`의 `exclude`에도 반드시 추가할 것.**
  점(`.`)으로 시작하는 파일·폴더(`.claude` 등)는 Jekyll이 기본 제외하므로 안 적어도 된다.
- `_config.yml` 도입 전에 **Jekyll이 켜져 있고**(`.nojekyll` 없음) `index.html`·`script.js`에
  **Liquid 문법(`{{`, `{%`)이 0건**임을 확인했다. 있으면 Jekyll이 내용을 망가뜨린다.
- 배포 후 검증: 두 문서 404, 사이트 200, `script.js`·`styles.css`·`manifest.json`·아이콘 전부 200,
  화면(인기소스 17·11·10, 카드 37, 셀럽 8) 정상, 콘솔 오류 0.

### 🔍 2026-07-30 SEO — 등록 완료, 본작업은 진행 중

- ✅ **구글 Search Console 등록** — **도메인 속성**(`haidilgogo.com`)으로 소유확인 완료.
  🔴 확인 방식이 **가비아 DNS의 TXT 레코드**(`google-site-verification=Jcmz…`)다.
  **그 TXT를 지우면 등록이 풀린다.**
- ✅ **네이버 서치어드바이저 등록** — `https://haidilgogo.com/`으로 소유확인 완료.
  네이버는 DNS 방식을 지원하지 않아 **`index.html`의 `naver-site-verification` 메타태그**를 쓴다.
  🔴 **그 태그를 지우면 등록이 풀린다.**
- ✅ `robots.txt`(전체 허용 + 사이트맵 위치) · `sitemap.xml`(주소 하나 — SPA라 개별 주소가 없다)
- ✅ **사이트맵 제출 완료** — 구글은 상태 `성공`·발견된 페이지 `1`로 즉시 읽어갔다.
  네이버도 등록됨(26.07.30 19:00). 🔴 **구글은 도메인 속성이라 사이트맵 주소를 전체(`https://…`)로
  넣어야 한다** — `sitemap.xml`만 넣으면 "사이트맵 주소가 잘못됨"이 뜬다(짧은 경로는 URL 접두어
  속성에서만 통한다).
- ✅ **`<title>`·`description`·`<h1>`·`og:site_name`·JSON-LD 완료** (아래 절 참고)
- ⬜ 남은 것: **본문을 HTML에 넣기** 하나뿐이다(구조 변경, 상의 필요).

### 🔍 2026-07-30 SEO 메타·구조 (`d038ead`·`9c451d6`·`04f2715`)

| 항목 | 전 | 후 |
|---|---|---|
| `<title>` | `하딜고고` (검색어 0개) | `하이디라오 소스 레시피 모음 · 하딜고고` (22자) |
| `description` | 22자 | 74자 (아래 원문) |
| `<h1>` | 사이트 전체 **0개** | 1개 (`.sr-only`로 화면에서만 감춤) |
| `og:site_name` | 없음 | `하딜고고` |
| JSON-LD | 없음 | `WebSite` + `CollectionPage`(레시피 33개 `ItemList`) |

확정된 `description`(사용자가 직접 쓴 문장):
> 건희소스·영지소스 등 하이디라오 소스 레시피와 마라훠궈탕·히든메뉴까지. 셀프소스바 이용법과 첫 방문 주문 가이드도 한곳에 정리했어요.

🔴 **지키거나 알아둘 것:**

- **`마라탕`이라 쓰지 말 것.** 마라탕과 마라훠궈탕은 다른 음식이고 이 사이트엔 마라탕이 없다.
  검색량은 마라탕이 훨씬 많지만 찾던 게 없어 바로 이탈하므로 도움이 안 된다(사용자 원안이
  `마라탕`이었고 이 이유로 고쳤다).
- **`<title>`에 과하게 공들일 값어치는 낮다.** 구글은 **홈페이지** 제목을 브랜드명으로 갈아끼우는
  경향이 있다 — 29CM 실측: `<title>`이 `감도 깊은 취향 셀렉트샵 29CM`인데 검색결과 파란 제목은
  `29CM`만 나온다(브랜드 검색·설명형 검색 둘 다). 하위 페이지(`29Magazine - 감도 깊은 …`)는
  적은 대로 나온다. **반면 `description`은 적은 대로 쓴다**(29CM 41자 그대로 노출) → 통제력이 큰
  쪽에 공을 들일 것.
- **`og:title`·`og:description`은 `<title>`·`description`과 일부러 다르다.** 전자는 카톡·트위터
  공유 카드용이고 카톡은 두 줄쯤에서 자른다. 검색용은 길게(74자), 공유 카드용은 짧게(19자).
  통일하지 말 것. 현재 값: `og:title` = `하딜고고`, `og:description` = `하이디라오(훠궈) 소스·레시피 모음`.

🔴 **화면 문구에 `꿀팁`·`발도장`을 쓰지 말 것 (사용자 지시).**
  - `발도장`은 화면에서 이미 빠졌다. 탭 라벨은 **`스티커`**다. 남은 발도장 표기는 전부 코드
    주석·내부 이름(`#view-stamp`·`openStampSheet`·`stampShareBtn` 등)이라 그대로 둔다.
  - 🔴 `꿀팁`은 2026-07-30에야 완전히 뺐다(`8bfc023`). 검색용 `description`만 새 문구로 바꾸고
    **`og:description`과 JSON-LD의 `WebSite.description`에 옛 문구가 복사된 채 남아 있었다.**
    ⚠️ **설명 문구를 고칠 때는 세 곳을 함께 확인할 것** — `description` / `og:description` /
    JSON-LD `WebSite.description`.

**카톡 미리보기 캐시** — 문구·이미지를 바꾸면 카톡은 옛 정보를 계속 보여준다.
[카카오 디버거](https://developers.kakao.com/tool/debugger/sharing)에서 🔴 **`캐시 초기화`를 먼저,
그다음 `디버그`** 순서로 해야 새 값이 보인다(`디버그`는 카카오가 들고 있는 값을 보여줄 뿐이다).
2026-07-30에 이 순서를 거꾸로 안내해서 "아직 옛 문구가 나온다"는 혼선이 있었다. 서버는 정상이었다
(쿼리 없는 주소·www·옛 github.io·카카오봇 위장 요청 네 경우 모두 새 값 응답 확인).
- **`.sr-only`**(styles.css 최상단) — 화면에는 안 보이지만 스크린리더·검색엔진은 읽는 유틸.
  `display:none`은 스크린리더도 못 읽어 목적이 사라지므로 쓰지 말 것. **검색어를 채워 넣는
  자리가 아니다** — 화면과 어긋나는 숨은 텍스트는 구글이 속임수로 본다.
- **JSON-LD에 `Recipe` 스키마를 넣지 않은 것은 의도다.** 구글은 "레시피가 그 주소의 주된 내용"일
  때만 사진+재료 카드를 준다. 주소가 루트 하나뿐인데 33개를 `Recipe`로 선언하면 내용과 어긋나
  스팸으로 취급될 수 있다. **레시피별 개별 주소를 만들면 그때 바꾼다.**
- ⚠️ **레시피를 추가·삭제·개명하면 `index.html`의 JSON-LD `itemListElement`·`numberOfItems`도
  갱신할 것.**

### 🔴 남은 SEO 핵심 — 본문을 HTML에 넣기 (미착수, 구조 변경)

크롤러가 보는 본문이 **657자**뿐이고 `마라`가 **0회**다. 레시피 33개·가이드·아티클이 전부
`script.js` 안에서 그려지기 때문이다. 주소도 루트 하나뿐이라 "건희소스"를 검색해도 들어올
페이지가 없다.

**왜 이게 결정적인지 실증 자료**(2026-07-30 구글 `하이디라오 소스 레시피` 검색 결과):
1위 네이버 블로그의 스니펫에 **재료와 분량이 그대로** 나왔다(`땅콩소스1스푼(핵심)+스위트칠리소스
2.5+…`). 블로그가 쓴 설명문이 아니라 **구글이 본문에서 긁어온 것**이다. 하딜고고는 긁어갈 본문이
없어서 제목·설명을 아무리 잘 써도 저런 스니펫이 안 나온다.

**기회**: 그 검색 1페이지가 전부 블로그·인스타다. 전용 사이트가 하나도 없다.

#### 📊 콘텐츠 분량 실측 (2026-07-30) — 우선순위가 예상과 달랐다

| 대상 | 글자 수 |
|---|---|
| **가이드**(첫 방문 가이드) | **10,633자** |
| **안물안궁**(훠궈 잡학) | 4,728자 |
| **고수 빼주세요!** | 2,995자 |
| **아티클 합계** | **18,356자** |
| 레시피 33개 전부 | **5,912자** (평균 179자) |

🔴 **아티클 셋이 레시피 33개의 3배다.** 가이드 하나만으로 레시피 전부의 두 배에 가깝다.
그리고 **레시피는 평균 179자로 얇고 조리단계가 있는 건 33개 중 13개뿐**이다(재료는 32개).
구글은 얇은 페이지를 잘 안 올려준다.
→ **순서는 아티클 먼저, 레시피는 나중.** "레시피 33개 = SEO 금광"이라는 직관은 틀렸다.
   아티클은 이미 긴 글이고 원래부터 "한 편의 글"이라 개별 주소를 갖는 게 자연스럽다.

#### 방법 세 가지 (2026-07-30 사용자에게 제시, **아직 미결정**)

**① 손으로 HTML 파일 4개 만들기** — 본문이 이미 `script.js`에 HTML 문자열로 있어 그대로 옮기면 된다.
빌드 도구 0이고 4개뿐이라 감당 가능. 🔴 **단점: 같은 글이 두 곳에 존재한다.** `script.js`를 고치면
정적 페이지가 낡는다 — 이 프로젝트가 반복해서 당한 함정이다.

**② 생성 스크립트 (권장)** — `.claude/`에 스크립트를 두고 `script.js`에서 본문을 읽어 정적 페이지를
자동 생성. **`script.js`가 유일한 원본이라 이중화가 없다.** 🔴 **선례가 있다 — 발도장 스티커도
`.claude/make_stickers.py`로 생성한다.** "빌드 도구 없는 사이트" 원칙과 충돌하지 않는다(로컬 도구다).

**③ Jekyll에 맡기기** — GitHub이 배포 때 생성. 로컬에서 돌릴 게 없지만 데이터를 `_data/`에 따로
둬야 해서 **①과 같은 이중화 문제**가 생긴다. 피하려면 결국 ②가 필요하다.

#### 페이지 생김새도 결정해야 한다

- **(가) 단순 읽기 페이지 (권장)** — 로고 + 본문 + 맨 아래 "하딜고고에서 더 보기" 버튼.
  만들기 쉽고 검색엔진에 최적. 앱과 모양이 다르다는 점은 감수. 검색으로 들어온 사람은 글을
  읽으러 온 것이고, 다 읽으면 버튼으로 앱에 들어오게 하면 된다.
- **(나) 앱과 같은 껍데기** — 상단바·탭바까지 그대로. 일관돼 보이지만 만들 게 훨씬 많다.

#### 부수 효과

아티클에 개별 주소가 생기면 **앱에서 그 주소로 링크**를 걸 수 있다. "내부 링크가 하나도 없다"는
SEO 약점(SPA라 페이지가 루트 하나뿐)이 같이 풀린다.

**🔴 다음 대화에서 할 일: 위 ①②③ 중 방법과 (가)/(나) 중 생김새를 사용자와 확정한 뒤 계획을 세운다.**
큰 작업이라 시간을 따로 잡는 게 맞다.

🔴 **`main`에 체크아웃된 상태로 시작할 수 있다.** 코드를 고치기 전에 반드시 작업 브랜치를 떼라 —
`main`은 GitHub Pages가 서빙하는 라이브 브랜치라 푸시하는 순간 배포된다. 2026-07-30에 실제로
`main`에 있는 채로 작업이 시작됐고, 사용자가 "브랜치로 작업하는 거죠?"라고 물어 잡았다.
(이 문서의 옛 A항이 `브랜치: feat/bottom-nav`라고 적어둔 탓에 확인 없이 믿을 위험이 있었다.)

### 🚀 2026-07-30 배포 — 옛 TCG 그리드 카드 죽은 코드 삭제 (`0bf09e4`)

2026-07-25에 그리드 카드를 `buildBrowseGridCard`(클린 카드)로 교체하면서 남겨둔 옛 코드를 지웠다.
`script.js` −64줄 / `styles.css` −335줄. **화면 동작은 하나도 안 바뀐다**(호출처가 없던 코드다).

지운 것: `buildCard()` 43줄 · `fitCardTitles()`(`.recipe-name`을 찾는데 요소가 0개라 무동작) ·
`refreshLikeCounts` 선택자의 `.like-btn` · `.recipe-thumb-img`용 `contextmenu` 리스너(중복) ·
`.recipe-card`/`.fav-star`/`.like-btn` 계열 CSS 전부(`@media` 안쪽 포함).

🔴 **이름이 비슷해도 남긴 것 — 지우지 말 것:** `.recipe-thumb-overlay`·`.recipe-thumb-source`(모달
썸네일 출처) · `.like-count`(모달 좋아요, `index.html`에 정적) · `.name-sub`(`s15 라젤(이 아는
동생)소스`의 `nameHtml`이 사용) · `.recipe-grid`(전체보기 컨테이너) · `.empty-state` ·
`.browse-fav` · `fitCardTitle()`(인기소스·전체보기 제목 폭 맞추기).

🔴 **이미지 저장 방지는 두 겹이고 둘 다 살아 있다.** `styles.css`의 `img { -webkit-touch-callout:
none }`(모바일 꾹 누르기)과 `script.js`의 **모든 `<img>`** `contextmenu` 차단(데스크톱 우클릭).
지운 건 후자와 겹치던 낡은 잔재(`.recipe-thumb-img`만 노리던 것)다. 클래스로 좁힌 선택자는 카드
마크업이 바뀌면 조용히 풀리므로, 다시 좁히지 말 것.

**검증(이 방법을 다음에도 쓰면 된다):** 삭제 전 버전을 `git show <이전커밋>:파일`로 `_tmp-before/`에
꺼내 같은 로컬 서버의 하위 경로로 띄우고(assets는 심볼릭 링크), **같은 브라우저·같은 조작 순서**로
양쪽을 훑어 요소 개수·계산된 스타일·화면 텍스트를 기계적으로 비교했다. 24개 화면 1,248개 항목 +
매장·발도장·기록시트·스토리뷰어·가챠 결과카드 + 320/390/1280px 전부 **차이 0**, 콘솔 에러 0.
Codex 교차검증도 이상 없음. 🔴 **비교 전 두 버전을 같은 상태로 맞춰야 한다** — 안정화 조건은
인기소스 1위 숫자가 실제값(당시 17)이 되고 `.hc-card`가 37개 이상일 때다. 이걸 안 맞춰서 처음에
"차이 65건"이라는 오진을 냈다.

🔴 **`.gacha-btn`은 가챠 버튼이 아니다.** `index.html`에서 발도장 **기록하기** 버튼이
`class="gacha-btn stamp-write-btn"`로 스타일만 공유한다. 진짜 가챠는 홈의 `#homeRandomBtn`이 연다
(이걸 착각해 "가챠 확인했다"고 잘못 보고한 적 있음).

### 🚀 2026-07-30 배포 — 기본 좋아요(BASE_LIKES)로 0을 없앴다

소스 20개 중 좋아요가 있는 건 4개뿐이고 16개가 0이라 **"사람 없는 커뮤니티"처럼 보인다**는
사용자 판단으로, 33개 전부에 기본 좋아요(더미)를 심었다. `main` `70a6167` → `d0cd886` (커밋 4개).

**화면 숫자 = `BASE_LIKES` + 실제 방문자 좋아요.** Firebase 콘솔에서 직접 고치는 방식은 쓰지 않는다
(모바일에서 지옥이고 버전관리가 안 된다). `script.js`의 `BASE_LIKES` 한 곳만 고치면 숫자·순위가 바뀐다.

확정된 숫자(= 화면에 뜨는 값). **아래는 `d0cd886`에서 전면 재조정한 최종값**이다 —
첫 배포(`4b75865`)의 옛 값(소스 38 24 22 … / 탕 5 4 3 2 1 / 히든 12 11 10 …)은
**아래쪽이 한 칸씩 떨어져 "심어놓은 티가 난다"고 사용자가 지적**해 버렸다. 되살리지 말 것.

```text
      순위| 이름                     표시
소스     1| 건희소스 오리지널 · 2021    17
         2| 쑨디2호소스 2022          11
         3| 영지소스 2025             10
         4| 박은영소스 참기름           9
         5| 장하오소스                 8   ┐ 동점
         6| 건희소스 짭짤 · 2021        8   ┘
         7| 마크소스                   7
         8| 영지소스 악마 · 2023        6
         9| 세훈소스 마장               5   ┐
        10| 화령소스                   5   │ 동점 4개
        11| 건희소스 2024              5   │
        12| 쑨디2호소스 2024           5   ┘
        13| 세훈소스 간장               4   ┐ 동점
        14| 수코소스                   4   ┘
        15| 박은영소스 마장             3   ┐
        16| 라젤소스                   3   │ 동점 3개
        17| 김풍소스                   3   ┘
        18| 건희소스 리뉴얼 · 2025      2   ┐ 동점
        19| 우기소스                   2   ┘
        20| 라젤(이 아는 동생)소스       0
탕       1| 혼자햐탕                   3
         2| 지새기탕                   1   ┐ 동점
         3| 토마토탕                   1   ┘
         4| 스키야키탕                 0   ┐ 동점
         5| 마라훠궈탕                 0   ┘
히든     1| 유부 새우완자               5
         2| 메기살덮밥                 3   (실제분만 — 사용자가 "실제 데이터 그대로" 지정)
         3| 희수국밥                   2   ┐ 동점
         4| 토마토에그누들              2   ┘
         5| 유부 새우완자(업그레이드)     1   ┐
         6| 토마토달걀밥                1   │ 동점 3개
         7| 토마토달걀만두              1   ┘
         8| 탄탄면                     0
```

🔴 **위 순위는 화면에 실제로 나오는 순서이고, 사용자가 준 목록과 일부 다르다.** 동점이 많아
최신순 타이브레이크가 순서를 정하기 때문이다. 어긋나는 자리를 전부 계산해 보여준 뒤
**사용자가 "순서 안 지켜도 된다"고 확정**했다. 순서를 맞추려고 숫자를 건드리지 말 것.
어긋난 자리는 **소스 13~19위**(세훈간장>수코 / 박은영마장>라젤>김풍 / 건희리뉴얼>우기)와
**탕 2~3위**(지새기>토마토 — 사용자 목록은 토마토가 위였다)다.

🔴 **표시 0이 넷 있다**(라젤(이 아는 동생)소스·탄탄면·스키야키탕·마라훠궈탕). "0을 없애자"고
시작한 작업이지만 숫자를 크게 낮추면서 사용자가 직접 0으로 지정한 것이다. 임의로 1 이상으로
올리지 말 것.

🔴 **되돌리지 말 것 / 새 레시피 추가 때 지킬 것:**

- **순서는 전부 사용자가 직접 지정한 것이다.** 인기순처럼 보이지만 실제 좋아요 순이 아니다
  (예: 화령소스는 실제 좋아요 5개로 실제 2위인데 사용자가 10위로 지정했다).
  임의로 "인기순에 맞게" 재배치하지 말 것.
- **`BASE_LIKES`는 더미이고 표시값이 아니다.** 실제 좋아요가 있는 6개(`s1`·`s2`·`s4`·`s19`·`r1`·`b7`)는
  표시값에서 실제분을 뺀 수가 들어 있다. 새 레시피엔 더미를 **반드시 추가**할 것 — 빠뜨리면 0이 된다.
- 🔴 **동점이 아주 많다**(소스 5쌍·탕 2쌍·히든 2쌍). 실제 좋아요는 원래 겹치므로 의도한 것이다.
  동점끼리는 `byPopular`의 타이브레이크(**최신순**, 날짜가 같거나 없으면 `RECIPES` 배열 순서)가
  순서를 정한다. **먼저 오길 원하는 쪽이 더 최신 레시피인 자리에만 동점을 둘 수 있다.**
  숫자를 바꿀 때는 **바꾸기 전에 정렬을 계산해서 어떤 순서가 나오는지 사용자에게 보여줄 것.**
- 🔴 **숫자가 작아서 순위가 쉽게 흔들린다.** 화령소스는 더미가 0이라 **한 명만 더 누르면 9위 위로**
  올라온다. 1↔2위 차이도 6뿐이다(17 vs 11). 순위를 고정하고 싶다는 요청이 나오면 숫자를 다시
  벌리는 것 말고는 방법이 없다고 설명할 것.
- **`getRealLikeCount`(실제분)와 `getLikeCount`(더미+실제)를 절대 합치지 말 것.** 저장(`likeCounts`·
  Firebase)에는 언제나 실제분만 들어가야 한다. 표시값으로 계산하면 좋아요를 취소할 때 숫자가
  거꾸로 튀어오른다(더미+실제 38에서 취소 → 69로 뛴 뒤 Firebase 응답이 와서야 37).
- **홈 탕 카드(`.hc-card`)에는 하트가 없다.** 카드는 보이지만(이미지+이름) **좋아요 숫자만 안 보인다.**
  그래서 탕의 더미는 **홈 그리드 순서만** 정한다(전체보기·상세에선 숫자도 보인다). 홈은
  `renderHomeCatGrid`가 `slice(0,4)`로 상위 4개만 그리므로 **5위 마라훠궈탕은 홈에 카드 자체가 안 뜬다**
  (0점이어서가 아니라 5위여서다). 히든메뉴 리스트는 상위 3개만 뜬다.

🔴 **배포 직후 라이브에서 잡은 버그(`ee5feeb`) — 같은 실수 반복 금지:**

홈 탕·히든메뉴도 `byPopular`로 정렬하는데, **좋아요 첫 도착 시 재정렬 대상에서 빠져 있었다.**
첫 방문(localStorage 캐시가 빈 상태)이면 좋아요 0인 순서가 그대로 남아, 실제 좋아요가 있는
메기살덮밥이 홈 히든메뉴 상위 3칸에서 밀려 **안 보였다.** `refreshLikeCounts()`는 숫자만 갱신하고
순서는 안 건드리므로 그것만으론 안 된다 — `renderHomeCatList`·`renderHomeCatGrid`를 첫 도착 분기에
추가했다. **좋아요로 정렬하는 목록을 새로 만들면 이 분기에도 등록할 것.**

🔴 **글자 폭은 `Range`로 재지 말 것(2026-07-30 실수).** `Range.getBoundingClientRect()`가
letter-spacing 때문에 `쑨디2호소스`를 77px로 재서 "두 자리 숫자를 넣으면 이름이 잘린다"고 두 번
잘못 보고했다. 실제는 74.98px이고 **안 잘린다.** 정확히 재려면 **폭 제한 없는 곳에 복제한 span**으로
재거나, 두 자리 숫자 10~99를 전부 넣어보고 `scrollWidth > clientWidth`를 확인할 것.
(390·375·320px 세 화면 모두 이번 변경으로 새로 잘리는 항목은 없다. 320px에서 인기소스 이름 5개가
잘리는 것은 변경 전에도 그랬다.)

검증 방법 참고: 좋아요 토글은 **Firebase 스크립트 태그만 제거한 사본**을 만들어 확인했다
(`likesRef = null`로 떨어져 localStorage만 쓴다). 라이브 하트를 클릭하면 공유 DB 숫자가 실제로
바뀌므로 **클릭으로 테스트하지 말 것.**

### 🚀 2026-07-29 배포 완료 — 11일 만에 라이브가 최신이 됐다

`main`이 `6e6ec03` → `bfa056a`로 올라갔다. **252커밋 / 11일치 / 86파일 / 7,532줄 추가.**
홈 개편·전체보기·매장·발도장·가이드·아티클이 전부 라이브에 반영됐다.

- 배포 전 사용자가 실기기로 확인함. 배포 후 라이브에서 탭바·배너·셀럽 레일·가이드 전체·
  콘솔 오류 없음까지 확인함.
- **되돌리려면**: `git push origin 6e6ec03:main --force` (배포 직전 지점)
- 계획이 바뀐 점: 원래는 "메뉴 탭까지 완성하고 한 번에 배포"였으나, 메뉴 탭(Codex 작업)이
  언제 끝날지 모르는데 252커밋을 더 묵히는 게 위험이 커서 **먼저 배포하고 메뉴 탭은 나중에**로
  바꿨다. 메뉴 탭은 `전체 메뉴를 미리 볼 수 있는 화면이 곧 준비될 예정이에요` 안내가 떠 있다.

### 🔴 배포 후 잡은 것 — 스플래시 시간차 (bfa056a)

실기기에서 **워드마크가 먼저 뜨고 로고가 뒤늦게** 나타났다(사용자 발견).

- 원인은 로고 교체가 아니었다. 동기화 로직(`2f259d4`)은 멀쩡했고 그림도 52KB → 12KB로
  가벼워졌다. **글꼴만 base64로 HTML에 내장돼 즉시 준비되고 로고는 네트워크였던 것**이 원인 —
  실기기에서 700ms를 넘기면 `setTimeout(reveal, 700)` 폴백이 먼저 터진다.
- **로고도 data URI로 내장**했다(HTML 77KB → 94KB). 스플래시 로고 네트워크 요청 0건 확인.
- 곁들여 `document.fonts.load`에 `'하딜고고'`를 넘기게 고쳤다. 글자를 생략하면 공백(U+0020)으로
  찾는데 이 글꼴은 `하·딜·고`만 담은 unicode-range 서브셋 둘이라 **매칭 0개로 아무것도 안
  기다리던 코드**였다(라이브에서 확인). 글꼴이 이미 내장이라 눈에 보이는 변화는 없다.
- 🔴 **이런 문제는 로컬에서 재현이 안 된다.** 12KB가 즉시 오기 때문이다. 스플래시·이미지
  타이밍 문제는 반드시 배포 후 실기기로 확인할 것.

### 🔴 Codex와 동시 작업 중 — 저장소는 Claude만 (2026-07-29 확인)

- **Codex는 메뉴 탭 구성을 작업 중이고, 파일은 전부 `02. Data/Haidilgogo/menu/` 안에 있다.**
  거긴 git 저장소가 아니라서 이 저장소와 안 겹친다(2026-07-29 양쪽에서 확인, 손실 없음).
- 저장소 안 md는 `HANDOVER.md`·`CLAUDE.md` **둘뿐**이고, Codex는 이 둘을 **읽기만** 하기로 했다.
- 🔴 **커밋할 때 `git add -A`를 쓰지 말 것.** 고친 파일만 이름으로 지정한다.
  같은 작업 트리를 다른 작업이 쓰고 있으면 남의 미완성 파일까지 쓸어 담는다.
- 메뉴 탭을 실제로 코딩하는 단계가 오면 `git worktree`로 폴더를 나눈다:
  `git worktree add "../Haidilgogo-menu" -b feat/menu-tab`
- 메뉴 표기는 **매장 태블릿 표기를 따른다**(`쭈꾸미`·`백미 새우` 등). 표준어보다 화면에 뜨는 이름이 우선 —
  이 저장소의 `셀프소스바`·`육류·야채류`와 같은 원칙이다. 확정 목록은 `02. Data/.../menu/menu-list.md`.

### 2026-07-29 커밋 (전부 작업 브랜치 백업, 배포 아님)

```text
bfa056a fix(스플래시): 로고를 HTML에 내장 — 워드마크와 시간차 제거   🚀 배포
5047f43 CTA 위 여백 20px → 28px                                    🚀 배포(1차)
d8985b8 CTA 있는 칼럼만 본문 끝~닫기 간격 좁힘
557c2e4 가이드에 STEP 구분선 + 레시피 CTA 버튼 + 하딜고고 로고체
45c4430 팁 박스 바로 뒤 문단에 여백 20px
5b81528 칼럼 마무리 문단 줄 간격을 본문과 맞춤
e82da82 가이드 마무리 문단 추가
bca428a 가이드 STEP 3 팁을 거름망으로 교체
7318b27 docs: 아티클 공통 글쓰기 기준(B2항) 신설
77f5f18 아티클 교정 2차 — 줄표 여백 + 문체 규칙 정리
d26bb79 아티클 3개 교정 1차 — 표기·문맥·문단 나누기
8ae40f7 docs: STEP 3 확정 + BONUS 분리 결정 반영
12155a7 셀럽 레일 정렬을 팔로워순 → 이름 가나다순으로
f297c0b 가이드 STEP 3에 '재료를 찾기 어렵다'는 이유 추가
e0ad828 가이드 STEP 3 '재료 넣고 익혀 먹기' 추가
0f70d21 docs: 인계 문서 D항의 오래된 서술 두 곳 정정
cfa1603 docs: STEP 2 확정 반영 — 인계 문서 갱신
ea79c91 가이드 STEP1↔STEP2 이음매 정리 (검토 반영)
3825412 ver에 연도 복원
8e972c5 건희소스 2025를 '리뉴얼'로 + 인기소스 부제 한 줄 고정
4846c53 맛 이름이 있는 ver에서 연도 제거
3d8588b 가이드 STEP 2 재료 소개 문단을 '셀프소스바' → '소스바'로
188cd51 가이드 STEP 2 팁 박스 문안 확정
2179c39 가이드 STEP 2 팁 제목을 원래 문장으로 되돌림
1f2da53 가이드 STEP 2에 큐레이션 소스 2개 붙임 + 팁 박스 본문
c1fb998 건희소스 버전 표기 정리 + STEP 2 팁 박스 제목
1120121 가이드 STEP 2 아래 팁 박스 제거
14ff61b 가이드 STEP 2 '하딜고고를 열고 레시피대로' 소제목을 팁 박스로
c535bb0 가이드 STEP 2 도입부 문안 수정
c42a410 docs: STEP 2 초안 반영 — 인계 문서 갱신
94b404e 가이드 STEP 2 '소스바에서 소스 만들기' 초안 추가
7f81b88 docs: 2026-07-29 작업 반영 — 인계 문서 갱신
ae9ebe8 가이드 이미지 2장 교체본 반영
d3e4927 안물안궁 썸네일 교체 + 밝은 그림에 맞춰 X 색 복구
1164360 안물안궁 제목을 그림 글씨와 같은 말로 맞춤
b3f88f5 안물안궁 제목에 예능 자막 밈 화살표(↘ ↗)
ea937c8 docs: 화살표는 의도한 변주라고 못박음
0acce39 탕·히든메뉴 눌림을 '틀이 눌리는' 방식으로 통일
bb6e1f0 docs: 카드 호버·누름 규칙을 사이트 공통 규칙으로
290c298 칼럼 사진 아래 여백 4px → 14px
7406f0f 고수 아티클 타투·후기 사진에 캡션 추가
1aa8544 → 8909fbb → e9175b3 → ab4844d  가이드 소스바 인원 안내(문안 확정까지)
3fdd155 → adef850  전골 맛 선택 이미지 교체(1맛 추가)·표기 수정
4ca28d9 가이드에 재료 주문 안내 추가
4d57193 → 119ec17  '소스바 → 셀프소스바' 전체 변경 후 되돌림(사용자 지시)
8959c85 → 0b2268f  셀프소스바 인원 선택 그림 추가 + 폭 60%(.col-figure--narrow)
7b73d8b 가이드 01 순서 교체(재료 → 소스바) + 소스바 문안 확정
0549e98 → 44c3afa  'STEP 1' 라벨 방식 + 번호 색을 요약 그림 색으로
ad8df21 '처음이라면 전골 4가지 맛' 추천을 팁 박스로
8e66138 재료 주문 대목 문안 다듬기
34a378f 가이드에 재료 주문 화면 그림 추가
19894ee → 28fb18d  '첫 주문이 고민된다면'을 팁 박스로 + 문안 다듬기
938e7fa docs: 닫기 X는 그대로 두기로
1245afb 안물안궁 도입부 끝맺음 수정
```

## B. 가이드의 목적과 작업 방식

- 독자는 하이디라오 첫 방문자이며, 입장 대기 중에 가이드를 읽는다.
- 자리에 앉은 뒤에는 태블릿 주문과 소스바 이용으로 바쁘므로, 미리 읽고 바로 따라 할 수 있어야 한다.
- 문구와 구성을 하나씩 확정한 뒤 구현한다. 미확정 내용을 임의로 쓰거나 한꺼번에 완성하지 않는다.
- 고수·알쓸신잡의 칼럼 패널을 공통 서식으로 쓰되, 가이드에 필요한 별도 표현은 그때마다 논의한다.
- 친근하고 편한 하딜고고 말투를 사용한다.
- 사실 확인이 필요한 내용은 추측하지 않는다.
- 감자 안내 캐릭터 이미지는 완전히 보류 상태이므로 다시 제안하지 않는다.

## B2. 🔴 아티클 공통 글쓰기 기준 (2026-07-29 확립)

가이드뿐 아니라 **고수·안물안궁 등 모든 칼럼에 적용**된다. 아티클을 새로 쓰거나 고칠 때 이 기준을 따른다.

### 줄바꿈 — 마침표마다 나누지 않는다

**한 문단 = 하나의 생각.** 실측(320px): 본문 폭 276px · 15px · **한 줄 약 26자**.

- **상한은 5줄(약 130자).** 넘으면 나눈다. 모바일에서 6줄이 넘으면 글 덩어리로 보여 눈이 미끄러진다.
- **나눌 때**: ① 화제가 바뀔 때 ② 앞을 뒤집거나 결론 낼 때 ③ 병렬 항목일 때
- **붙일 때**: ① 앞 문장의 이유일 때 ② 부연·예시일 때 ③ 한 호흡에 읽히는 짧은 문장 둘일 때
- **한 문장짜리 문단은 강조 장치**다. 남발하면 힘이 빠진다.
- **도입부는 `훅 한 문장 + 설명 한 문단`** 꼴로 끊는다(아티클 셋 모두 이 형식으로 통일함).

### 문체 — 한 아티클 안에서 둘을 나눠 쓴다

- **독자에게 말 거는 문장 = 해요체** (`…들어 있어요`, `…차이가 있어요`)
- **사실·근거를 대는 문장 = 합쇼체** (`…사용됩니다`, `…느낍니다`)
- 🔴 **출처 번호가 붙은 문장은 합쇼체로 통일한다.** 근거를 대는 자리에서 말투를 한 단계 올려
  신뢰를 준다. (2026-07-29 검사: 출처 문장 9개 중 안물안궁 `[4]` 하나만 해요체라 맞춰 고침.)
- **가이드는 해요체 전용이다.** 출처를 대는 글이 아니라 따라 하라고 안내하는 글이라서다.
  이 차이는 의도된 것이니 통일하지 말 것.

### 표기

- **밈 말투는 지킨다.** `은근 재밌는`은 표준(`은근히`)이 아니지만, 제목
  `물어본 사람~ 궁금한 사람~`과 문장 톤이 이미 능청스러워 표준 표기가 오히려 튄다(사용자 확정).
- **줄표(—)는 앞뒤에 공백을 준다.** 화면에 나오는 줄표는 안물안궁 도입부 한 곳뿐이다.
- **화면에 나오는 영문 라벨은 `STEP`이 유일하다.** 늘리지 않는다 —
  `DIY 탕`→`나만의 탕`으로 바꾼 전례가 있다.

## C. PDF 기획초안

원본 기획자료:

```text
/Users/Macmini_J/ClaudeCode/02. Data/Haidilgogo/main-banner/guide/TalkFile_하딜고고_하이디라오_첫방문가이드_기획초안.pdf
```

- 2026-07-26 제작, 6페이지.
- PDF의 문구는 최초 기획안이며 자동 확정본이 아니다.
- 이후 대화에서 교체·삭제·확정한 내용이 PDF보다 우선한다.
- PDF 자체도 소스바 이용료·인원 선택 기준, 거름망, 서비스 제공 여부 등을 출시 전에 다시 확인하라고 명시한다.

## D. 현재 구현된 범위

가이드는 기존 `openColumn()`과 `#columnOverlay`를 사용한다. 옛 `#guideOverlay` 자리표시 패널은 제거됐다. 홈 가이드 배너와 히어로 마지막 가이드 배너가 같은 `GUIDE_BANNER` 아티클을 연다.

현재 `script.js`의 `GUIDE_BANNER.body`에 들어 있는 **STEP 1 확정본**(2026-07-28 문장 단위로 확정, 커밋 `83287f0`):

```text
제목
어서와~ 하이디라오는 처음이지?      ← titleHtml로 두 줄(배너·아티클 모두)

부제
주문부터 소스바 사용까지 · 가이드

도입부
하이디라오 방문이 처음이라면, 기다리는 동안 미리 알아보세요.
태블릿 주문부터 소스 만들기까지, 하딜고고가 하이디라오를 편하게 즐길 수 있도록 도와드릴게요.

[한눈에 보는 이용 순서 이미지]

01 태블릿으로 주문하기

자리를 안내받으면, 태블릿에서 가장 먼저 먹고 싶은 탕을 골라요.

[태블릿으로 주문하기 이미지]

전골 1맛·2가지 맛·4가지 맛 중에서 냄비를 몇 칸으로 나눌지 고르고, 각 칸에 먹고 싶은 탕을
선택하면 돼요. 탕 위치는 냄비 그림에서 드래그해 원하는 자리로 옮길 수 있어요.

[전골 맛 선택 이미지]

전골 구성에 따라 한 칸의 크기와 탕 가격이 달라요. 2가지 맛은 한 칸의 양이 더 많아 같은 탕도
가격이 더 높고, 4가지 맛은 한 칸의 양이 적어 가격도 더 낮아요.

인원수만큼 탕을 고르거나 모든 칸을 탕으로 채울 필요는 없어요. 일행의 취향과 맛보고 싶은
탕의 개수에 맞게 고르면 돼요.

처음이라면 전골 4가지 맛을 추천해요

여러 탕을 조금씩 맛볼 수 있고, 먹고 싶은 탕만 고른 뒤 나머지 칸은 0원인 맑은 탕(물)으로
선택할 수도 있어요.

맑은 탕(물)은 소스바 재료를 넣어 나만의 탕을 만들거나, 국자를 헹구는 용도로 활용할 수 있어요.
```

### 🔴 위 본문은 2026-07-28 시점이다. 2026-07-29에 STEP 1이 크게 바뀌었다

현재 화면에 나오는 STEP 1의 실제 구조는 다음과 같다. **본문 원문은 `script.js`의 `GUIDE_BANNER.body`가 기준**이고,
바꾼 이유는 전부 그 자리 주석에 적어뒀다(여기 요약보다 주석이 더 자세하다).

```text
STEP 1  ← 12px 주황 라벨(.column-step--1)
태블릿으로 주문하기  ← 19px

  자리를 안내받으면… + [태블릿으로 주문하기]
  전골 1맛·2가지 맛·4가지 맛… + [전골 맛 선택]
  전골 구성에 따라… / 인원수만큼 탕을…
  💡 처음이라면 전골 4가지 맛을 추천해요        ← 팁 박스(.col-tip)

육류·야채류 등 먹을 재료를 주문해요            ← 16px 소제목
  이제 화면을 내려… + [태블릿으로 재료 주문하기] + 처음부터 많이 시키지 않아도…
  💡 첫 주문이 고민된다면 이렇게 골라보세요      ← 팁 박스

셀프소스바 이용 인원도 선택해요                ← 16px 소제목
  재료까지 골랐다면… + [셀프소스바 인원 선택] + 같은 탕에 같은 고기를 먹어도…
```

2026-07-29에 확정된 것들(되돌리지 말 것):

- **순서는 탕 → 재료 → 소스바.** 기획초안(소스바가 STEP 2, 재료가 STEP 3)과 반대다.
  재료를 담아야 소스를 만들 이유가 생기고, 다음이 `02 소스바에서 소스 만들기`라 매장 동선과 맞는다.
- **기획초안의 STEP 2·3을 별도 섹션으로 세우지 않고 01 안에 넣었다.** 둘 다 태블릿에서 하는 일이고,
  별도 `02`를 만들면 맨 위 요약 그림의 `02 소스바에서 소스 만들기`와 번호가 어긋난다.
- **추천·조언은 소제목이 아니라 팁 박스(`.col-tip`)로 뺀다.** 소제목에는 절차만 남긴다 —
  안 그러면 한 STEP 안에서 소제목이 4개씩 평평하게 늘어선다.
- **`STEP 1`은 큰 단계 제목(`.column-step`, 19px + 12px 색 라벨)**, 그 아래 절차는 16px 소제목.
  번호 색은 요약 그림의 01·02·03 색 그대로(대비는 낮지만 그림과의 일치를 우선 — CSS 주석 참고).
- **소제목 `육류·야채류`는 태블릿 분류명, 본문 `고기와 채소`는 우리 문장.** 일부러 다르다. 통일하지 말 것.
- **`셀프소스바`는 가이드의 소스바 대목·팁 박스에만 쓴다.** 사이트의 다른 `소스바` 30여 곳은 그대로.
  (2026-07-29에 전체 변경을 시도했다가 사용자 지시로 되돌림 — 커밋 `4d57193` → `119ec17`.)
- **소스바 요금은 "있다"고 확정해 쓰고 금액은 안 쓴다.** 태블릿 3,000원 / 제주·부산 메뉴 데이터 3,500원으로
  실제로 다르다. 소스바 이용이 자율인지 강제인지는 아예 건드리지 않는다.
- ~~`무엇을 넣어야 할지 모르겠다면 하딜고고에서 취향에 맞는 레시피를 찾아보세요!`는 STEP 2에서 쓸 문장이다.~~
  → **2026-07-29 최종: STEP 2에서도 안 쓰기로 했다.** 지금은 사이트 어디에도 안 쓰이는 문장이다(G항 참고).
- **소스바 대목의 마지막 문단에서 `다양한 재료를 원하는 대로 조합해 나만의 소스를 만들 수 있고`를 뺐다**
  (2026-07-29). STEP 2가 같은 말을 두 번 해서다. 이 자리는 요금을 납득시키는 역할만 맡는다.

STEP 1에서 확정된 편집 판단(되돌리지 말 것):

- **전골/탕 관계를 한 문장에 드러냄.** 첫 방문자가 둘을 헷갈린다("탕을 고르랬는데 왜 전골을 고르지?"). `중 하나를 고른 다음` → `중에서 냄비를 몇 칸으로 나눌지 고르고`.
- **드래그 문장은 전골 문장에 붙임.** 필수가 아니라 부가 정보라 독립 문단으로 세우지 않는다. 다만 **드래그는 보이지 않는 동작이라 알려줄 값어치는 있어** 삭제하지 않았다.
- **`DIY 탕` → `나만의 탕`.** 영어 약어가 하딜고고 말투에서 튄다.
- **가격 설명·인원수 안내는 각각 한 문단.** 글자는 안 바꾸고 문단만 합쳤다.
- **`비싸다/저렴하다`는 쓰지 않는다.** 가치판단이라 사용자가 거부했다. `가격이 더 높다/낮다`가 정답.
- **이미지 배치**: 태블릿 이미지는 전골 종류 문단 위, 전골 맛 선택 이미지는 가격 문단 위. (구 코드는 전골 맛 이미지가 가격 문단 아래에 있었고 인원수 문단이 아예 없었다 — `83287f0`에서 바로잡음)
- **`1맛` 표기는 태블릿 실제 화면 그대로**(사용자 확인 완료 — 기획초안 6페이지의 출시 전 확인 항목 하나 해소).
- **국자 헹구는 이유는 설명하지 않는다.** 처음 방문자도 알아들을 수 있다는 사용자 판단.

‘한눈에 보는 이용 순서’는 제목·번호·문구를 웹 글자로 중복 표시하지 않고 완성 이미지 한 장으로 넣었다.

## E. 이미지

저장소 밖 원본:

```text
/Users/Macmini_J/ClaudeCode/02. Data/Haidilgogo/main-banner/guide/
├── 한눈에 보는 이용 순서.png        850×1400
├── 태블릿으로 주문하기.png          1281×902
├── 전골 맛 선택.png                 1500×650   (1맛 추가되며 교체)
├── 태블릿으로 재료 주문하기.png     1287×904   (2026-07-29 추가)
├── 셀프소스바 인원 선택.png          826×1340   (2026-07-29 추가)
├── 셀프소스바.png                  1448×1086   (2026-07-29 추가 · STEP 2 대표 그림)
└── 식탁 전경.png                   1448×1086   (2026-07-29 추가 · STEP 3 대표 그림)
```

커밋된 웹용 사본:

```text
assets/columns/guide/order-overview.webp         850×1400
assets/columns/guide/tablet-order.webp            880×620
assets/columns/guide/hotpot-flavor-select.webp    880×382
assets/columns/guide/tablet-ingredients.webp      880×619
assets/columns/guide/sauce-bar-qty.webp           826×1340   ← 세로로 길어 폭 60%로 표시
assets/columns/guide/sauce-bar.webp               880×660    ← 가로 그림이라 --narrow 없이 폭 100%
assets/columns/guide/table-setting.webp           880×660    ← STEP 3, 마찬가지로 폭 100%
```

🔴 **세로로 긴 그림은 `col-figure--narrow`를 붙여 폭을 60%로 줄인다.** 셀프소스바 카드가 폭 100%일 때
320px 폰에서 448px(화면의 55%)로, 항목 하나짜리 그림이 전체 요약 그림(56%)과 같은 크기였다.
60%면 269px(33%)이고 카드 제목이 13px이라 본문 14px과 비슷해 잘 읽힌다. **55% 아래로는 내리지 말 것**(실측).

원본은 삭제·덮어쓰지 않는다. 웹 사본은 원본 비율을 유지한다.
변환은 `cwebp -q 82`(가로 880 초과면 `-resize 880 0`)로 한다. **880보다 작은 원본은 확대하지 않는다** —
칼럼 본문 폭이 375px 화면에서 331px이라 2배(≈662px)면 충분하다. `한눈에 보는 이용 순서`가 그래서 850 그대로다.
교체할 때는 `script.js`의 `width`/`height` 속성을 새 크기로 고치고 `?v=` 숫자를 올린다 —
속성이 옛 비율이면 이미지가 뜨기 전 자리가 어긋난다.

## F. 검증·표시 결정

- 데스크톱과 실제 모바일 화면에서 확인했다.
- 가로 넘침과 콘솔 오류가 없고 이미지 세 장은 원본 비율로 표시된다.
- 칼럼 열기 시 `html` 스크롤 잠금, 상단 X, 하단 닫기가 정상이다.
- 고수·알쓸신잡 회귀도 확인했다.
- 줄바꿈은 기존 칼럼 공통 동작을 유지한다. `word-break: keep-all`을 새로 넣지 않는다.
- `01 태블릿으로 주문하기`의 현재 제목 간격도 유지한다.
- ‘처음이라면 전골 4가지 맛을 추천해요’는 눈에 띄는 `h3` 소제목으로 유지한다.

## G. PDF와 대조해 남은 항목

**STEP 1은 확정·구현 완료.** 결론이 난 두 건은 다음과 같다.

- **인원별 숫자 예시(`4명: 탕 3개 + 물 1칸`, `3명: 탕 2개 + 물 2칸`)는 넣지 않는다.**
  "정해진 공식이 아니다"라면서 공식을 주는 모순이고, 3명인데 물이 절반인 것도 앞선 물 칸 설명과 어긋난다.
  대신 “인원수만큼 탕을 고르거나 모든 칸을 탕으로 채울 필요는 없어요”라는 일반 안내로 확정했다.
- **거름망 팁은 STEP이 아니라 BONUS로 내린다.** "STEP에는 필수 과정만 둔다"는 원칙을 사용자가 세웠다.
  거름망은 탕을 고르는 일이 아니라 재료를 익혀 먹는 일이고, 성격상 기획초안의
  `BONUS 2 · 알아두면 좋은 서비스 이용 팁`과 같다. 문구는 BONUS 작업 때 다시 잡는다.
  - 참고: 이름 `거름망`은 매장에서 통하는 말이 맞다(사용자 확인). 다만 **제공 여부는 지점·상황에 따라 다를 수 있어
    단정하면 안 된다.** "직원에게 거름망이 있는지 물어보고 요청해보세요" 형태로 hedge를 문장 안에 녹인다.

**기획초안의 STEP 2(소스바 인원)·STEP 3(재료 주문)도 2026-07-29에 구현 완료**됐다.
다만 **별도 STEP이 아니라 `STEP 1 태블릿으로 주문하기` 안의 소제목**으로 들어갔다(위 D항 참고).
기획초안의 STEP 번호와 화면의 구조는 더 이상 1:1로 대응하지 않는다.

아직 미구현인 이후 범위:

- ~~STEP 2 소스바에서 소스 만들기~~ ← **✅ 확정 완료**
- ~~STEP 3 재료 넣고 익혀 먹기~~ ← **✅ 확정 완료(f297c0b까지)**
- ~~가이드 마무리 문장~~ ← **✅ 완료.** 입장 대기 여부로 두 갈래를 챙기고
  `소스바 앞에서 헤매지 않도록, 하딜고고가 함께할게요`로 맺는다 + `레시피 보러 가기` 버튼.
- **별도 아티클 「처음이라면 이 조합!」** ← 나중에(사용자가 미룸).
  여기에 **소스팀/주문팀 팁**과 **서비스 팁**(앞치마·머리끈·케이크 등)이 들어간다.
  거름망 팁은 2026-07-29에 STEP 3으로 옮겼으니 빼고 잡을 것.
  🔴 아티클이 4개가 되면 **메인 배너 캐러셀이 5칸**이 된다. 그때 배너 구성을 한 번 볼 것.

### 가이드 마무리에서 새로 생긴 것

- **`.col-divider`** — STEP 앞마다·마무리 앞에 넣는 구분선(네이버 블로그식). 색 `#ECE5DC`.
- **`.col-cta`** — 칼럼 본문 안 버튼. `data-go="browse"`를 주면 칼럼을 닫고 전체보기를 연다.
  소스 카드의 `data-rid`와 같은 방식이라 다른 아티클에서도 그대로 쓸 수 있다.
  `:has(.col-cta)`로 CTA가 있는 칼럼만 본문 끝~닫기 간격을 좁힌다(34 → 14px).
- **`.col-outro--plain` + `.col-brand`** — 마무리 줄 전체는 본문색, `하딜고고`만
  푸터(`.footer-brand`)와 같은 로고체 Paperlogy + 브랜드 빨강.
- **`.col-tip + p`** — 팁 박스 뒤에 문단이 오면 여백 20px(기본 4px이면 들러붙는다).

### STEP 3 확정 내용 (2026-07-29)

```text
STEP 3  재료 넣고 익혀 먹기          ← .column-step--3 (빨강 #F02008)
  소스를 만들어 자리로 돌아온 뒤, 탕이 끓기 시작하면…
  [식탁 전경 그림]
  주문한 재료가 나오면 먹을 만큼씩 나눠 넣어보세요…
  알맞게 익은 재료는 소스바에서 만들어온 소스에 찍어 드세요…
  💡 고기와 해산물은 충분히 익혀 드세요
```

되돌리지 말 것:

- **제목은 요약 그림의 `재료 넣고 익혀 먹기`.** 기획초안의 `재료는 조금씩 넣어 즐겨요`가 아니다.
- 🔴 **도입은 상태를 단정하지 않는다.** 소스를 만들어 오는 시간도, 냄비가 나오는 시점도
  제각각이라 `냄비가 끓고 있어요`·`재료가 나와 있어요`로 쓸 수 없다(사용자 지적).
  `탕이 끓기 시작하면`은 조건문이라 늘 맞는다.
- **`알맞게 익은 재료는 …소스에 찍어 드세요`는 초안에 없는 추가분이다.** 없으면 가이드가
  소스를 만들게만 하고 그 소스를 쓰는 장면 없이 끝난다.
- 뒷문장(`소스가 부족하거나 입에 안 맞으면 다시 가도 돼요`)은 **소스바 왕복이 자유롭다는
  확인된 사실**을 쓴 자리다. STEP 2에서 그 사실을 쓰던 팁을 지우면서 사라졌던 정보를 되살린 것.
- **`한꺼번에 넣으면 국물 속에서 재료를 찾기 어렵고, 못 건진 재료는 결국 너무 익어버려요`** —
  찾기 어렵다 → 못 건진다 → 너무 익는다로 인과가 이어진다. 나중에 거름망 팁과도 이어진다.
- 🔴 **팁 박스는 거름망이다**(2026-07-29 사용자 지시로 교체). 원래는 초안의 `고기와 해산물은
  충분히 익혀 드세요`였는데, 샤브샤브를 아는 한국 독자에겐 새 정보가 아니라 실용성이 낮았다.
  (그 전에 초안의 `조리법이 헷갈리면 직원에게 물어보세요`는 이미 뺐다 — 직원이 익는 시간을
  알려주는지 확인된 바 없어서.)
- 🔴 **거름망 팁은 원래 별도 아티클로 미뤄뒀던 것을 STEP 3으로 옮긴 것이다.** 바로 위 문단이
  `국물 속에서 재료를 찾기 어렵고`로 문제를 던져놓아 거름망이 그 해법으로 바로 이어진다.
  별도 아티클에는 서비스 팁(앞치마·머리끈·케이크 등)만 남는다.
- 재료 예시는 성격이 다른 둘을 골랐다 — `당면`=집게로 못 잡음 / `메기살`=집으면 부서짐.
  🔴 태블릿 실제 메뉴명은 **`팡가시우메기`**지만 아티클 본문이라 알아듣기 쉬운 `메기살`로 썼다
  (사용자 지정). STEP 1의 소제목 `육류·야채류` vs 본문 `고기와 채소`와 같은 판단이다.
- hedge는 문장 안에 녹였다(`준비되어 있지 않을 수 있으니`) — 거름망은 지점·상황에 따라 없을 수 있다.
- 그림(`table-setting.webp` 880×660)은 STEP 2의 소스바 전경과 짝이다(가기 전 ↔ 돌아온 뒤).
  그림 속 맑은 탕 칸과 소스 종지 두 개가 STEP 1·2 팁과 그대로 이어진다.

## H. 다음 대화의 정확한 시작점

1. `CLAUDE.md`와 이 문서를 끝까지 읽는다.
2. `git status`, 현재 브랜치, `HEAD`, 원격 동기화 상태를 확인한다.
3. **STEP 1·2·3은 전부 확정 완료다.** 남은 것은 ① 가이드 마무리 문장,
   ② 별도 아티클 「처음이라면 이 조합!」(나중에) 둘뿐이다. 위 A항·G항 참고.
4. 문장 단위로 확정한 뒤 코드에 반영한다. **문구를 먼저 보여주고 확인받은 다음 고친다** —
   사용자 확인 없이 본문을 써서 커밋하지 말 것(2026-07-29에 실제로 지적받음).
   단 사용자가 "일단 넣고 나중에 보겠다"고 하면 넣되, 초안임을 코드 주석과 이 문서에 남긴다.
5. STEP 2 끝에 **STEP 3으로 넘어가는 이음말이 없다**(소스 카드에서 끊긴다).
   STEP 3 문안을 쓸 때 "자리로 돌아가면…" 식의 연결을 같이 잡기로 했다.

### 🔴 ver(부제) 표기 — 2026-07-29 정리

```text
건희소스   오리지널 · 2021 / 짭짤 · 2021 / 2024 / 리뉴얼 · 2025
영지소스   악마 · 2023 / 2025
쑨디2호소스 2022 / 2024
세훈소스   간장 / 마장     박은영소스  참기름 / 마장
유부 새우완자 업그레이드   (히든메뉴)
```

- `오리지널` ↔ `리뉴얼`이 짝이다(2021 단맛 → 2025 리뉴얼). 건희소스 `2024`는 별개로 만든 소스다.
- 맛 이름과 연도가 한 소스 안에 섞이는 건 **사용자가 괜찮다고 확정**했다.
- 🔴 **`ver`을 늘릴 땐 홈 인기소스 카드부터 재라.** 거기 쓸 수 있는 폭은 390px 기기에서
  **80px뿐**이다(이름·부제 80 + 하트 44). 320px에서만 재면 차이가 안 보인다 — 실제로
  `오리지널 · 2021`(83px)이 두 줄로 감겨 그 카드만 225px로 커진 적 있다(다른 카드는 210px).
- 지금은 `.hp-foot .hp-sub`에 `nowrap + ellipsis`가 들어가 **어떤 긴 부제를 넣어도 카드가
  210px로 고정**된다. 대신 인기소스에서는 `오리지널 · 2...`로 잘려 보인다(사용자 확인 후 확정).
  이 처리는 바로 위 `.hp-name`과 그리드·전체보기의 `.card-sub`가 이미 쓰던 것으로, 여기만 빠져 있었다.

### 🔴 팁 박스 제목 길이는 `.col-tip-body` 폭으로 잰다

전구 아이콘이 폭을 가져가므로 **320px에서 220px**뿐이다. 칼럼 본문 폭(340px)으로 재면 한 줄로
착각한다(실제로 두 번 틀렸다). 다만 **두 줄 자체는 정상**이다 — 기존
`첫 주문이 고민된다면 이렇게 골라보세요`도 두 줄이다.

**STEP 2 확정 내용 (2026-07-29):**

```text
STEP 2  소스바에서 소스 만들기          ← .column-step--2 (초록 #608830)
  주문을 마쳤다면… 자리에서 일어나 셀프소스바에 다녀와요.
  [소스바 전경 그림]
  소스바에는 …다양한 재료가 놓여 있어요.
  소스바 하단에 있는 그릇을 하나 챙겨서… 정답은 없으니 취향대로 조합해보세요.
  💡 소스 종류가 너무 많아 고민된다면 이렇게 해보세요
  → 건희소스 / → 쑨디2호소스              ← 누르면 칼럼 닫고 레시피 상세 열림
```

되돌리지 말 것:

- **큐레이션 소스는 건희소스(s1)·쑨디2호소스(s2)** — 달달/짭짤이라 맛이 안 겹친다는 게 근거.
- **팁 본문은 사용자가 직접 쓴 문안이다.** 맞춤법·말투만 손봤다(쑨디소스→쑨디2호소스,
  먹어보는걸→먹어보는 걸, 안맞는다면→안 맞는다면, 추천드립니다→추천해요).
  🔴 **가이드는 해요체**다. 합쇼체(~습니다)는 고수·안물안궁 아티클에만 쓴다.
- **첫 문장의 `자리에서 일어나`를 빼지 말 것.** STEP 1의 소스바 대목은 앉아서 태블릿으로
  인원을 고르는 일이고 여기는 걸어가는 일인데, 둘 다 '소스바'라 독자가 "아까 했는데 또?" 하고 멈춘다.
- **도입은 `셀프소스바`, 이후는 `소스바`.** 첫 문장에서 정식 이름을 밝히고 뒤로는 짧게 받는다.
- **재료 예시는 `SAUCE_BAR` 정식 목록에서만** 뽑는다 — 레시피 화면 표기와 어긋나지 않게.
- 🔴 **예약 문장 `무엇을 넣어야 할지 모르겠다면…`은 결국 안 쓴다.** 2026-07-28에 STEP 1에서
  빼내 STEP 2용으로 남겨뒀으나, 팁 제목이 이미 같은 말을 하고 소스 카드가 바로 아래 눌려서
  겹쳤다. 지금은 사이트 어디에도 안 쓰이는 문장이다(양쪽 주석에 이력 있음).
- 기획초안의 "인기 소스·셀럽 소스를 **시각적으로** 강조"는 아직 문장 안에만 녹였다.
  **카드 UI로 세우는 건 논의 전**이다.

**검토에서 나왔으나 안 고치기로 한 것(다시 제안하지 말 것):**

- `맑은 탕(물)` — `맑은 탕`은 태블릿 실제 메뉴명이고(`menu-list.md` 확인) 괄호 병기로 충분하다.
- **전골/탕 정의 문장 추가** — 지금 문장(`…냄비를 몇 칸으로 나눌지 고르고, 각 칸에 …탕을 선택`)이
  이미 그 혼동을 잡으려고 고쳐놓은 결과물이고 전골 3종 비교 그림도 붙어 있다.
- STEP 1의 `핵심이에요` + `추천해요` — 중복이 아니라 근거→결론이다.
- **소스팀·주문팀 팁은 BONUS로 미룬다**(2026-07-29 사용자 확정). 기획초안 원문은 순서가
  탕→소스바→재료라 성립했지만, 우리 순서는 탕→재료→소스바여서 STEP 2 시점엔 "주문팀"이
  할 일이 없다. 거름망 팁과 성격이 같으니 BONUS에서 함께 잡는다.

**새로 생긴 장치 — 본문 중간 소스 목록:**

본문 HTML에 아래 한 줄을 넣으면 그 자리에 직접 고른 소스가 붙는다(적은 순서대로, 없는 id는 건너뜀).
누르면 칼럼이 닫히고 해당 레시피 상세가 열린다. **다른 아티클에서도 그대로 쓸 수 있다.**

```html
<div class="column-sauce-list" data-sauce-ids="s1,s2"></div>
```

- 카드 markup은 `buildColumnSauce()` 한 곳에서만 만든다(맨 아래 자동 목록과 공유).
- 클릭 리스너는 `columnOverlay` 전체를 훑는다 — `listEl`만 보면 본문 것이 안 눌린다.
- 기존 `.column-sauces` 자동 목록(재료 필터 + 맨 아래 고정)은 그대로다. 고수 아티클 회귀 확인함.
- `#columnBody .column-sauce-list` 여백은 14/24px. 기본값 4px이면 팁 박스에 들러붙어 보인다.

**기획초안 PDF의 STEP 4 원문**(아직 확정본 아님, 문장 단위로 다시 잡을 것):

```text
STEP 4 하딜고고로 소스를 만들어요
음식이 준비되는 동안 하딜고고를 열고 취향에 맞는 소스를 골라보세요.
```

---

# 2026-07-28 UI 작업 (가이드와 별개)

사이트를 둘러보며 눈에 걸린 것들을 고쳤다. 전부 커밋·푸시 완료, 미배포.

**칼럼 아티클**

- 아티클을 열면 오버레이가 상단바까지 덮었다 → 레시피 상세·발도장 시트와 같은 문법(`--topbar-h`)으로
  상단바 아래에서 시작하게 함. `openColumn()`에서 `syncTopbarH()`를 부른다 —
  **이 호출이 없으면 안 된다.** 화면 크기가 바뀐 뒤 `--topbar-h`가 옛 값(121px)으로 남아 있는 경우가 있었다.
- 아티클이 열린 채 상단바를 누르면 닫히고 그 동작이 실행된다(레시피 상세와 같은 규칙).
  **공유 버튼은 예외** — 화면을 바꾸지 않으므로. 이 예외는 2026-07-25에 모달에서 이미 잡힌 버그다.
- 칼럼 제목이 `titleHtml`을 쓰도록 함 → 가이드 제목이 배너처럼 두 줄. 고수·안물안궁은 `titleHtml`이 없어 그대로.
- `.column-title`에 `line-height: 1.2` 명시(없어서 브라우저 기본값이었음). 폰트 규칙상 iOS 받침 잘림 예방.

**레시피 상세**

- 팁 박스에 Noto 전구 PNG(`assets/icons/bulb.png`, U+1F4A1 128px, Apache-2.0) 복원.
  2026-07-25에 "중복"이라며 뺐던 걸 텍스트 이모지가 아닌 정지 PNG로 되살린 것.
  좌우 패딩은 옛 18px로 돌리지 않고 **재료 박스와 같은 14px** 유지 — 그래야 전구와 재료 이름 시작선이 맞는다.
- 하단에 닫기 버튼 추가(`#modalBottomClose`). 스타일은 `.column-bottom-close` 규칙을 공유한다.
- **좋아요 알약 제거 + 이름·부제 오른쪽으로 이동.** 사이트의 다른 하트(`.hp-like`·`.hc-row-like`)는 전부
  하트+숫자만 놓는데 상세만 알약이었다. 위치는 브라우즈 카드(`.hc-card-foot`)와 같은 구조 —
  `align-items: flex-start` + 하트만 `align-self: center`(부제 없는 레시피에서도 이름이 안 흔들린다).
  하트 20px로 통일, 패딩+음수 마진으로 손가락 영역 44px 유지.
- 눌렀을 때 `hpLikePop` 통통 팝 추가(홈 하트와 같은 키프레임·같은 `popHeart()` 헬퍼).

**메인 배너**

- 제목 크기를 **30px으로 통일**(이달의 소스 34 → 30). 넘길 때 계단처럼 바뀌던 것 정리.
  **가이드만 26px 예외** — 30px이면 320px 폰에서 둘째 줄이 한 번 더 감겨 세 줄이 된다(측정 확인).
- 이달의 소스 부제가 320px에서 잘려서(`.mf-desc`는 nowrap이라 …로 잘린다) 앞머리 `하얼빈 정통파, `를 덜어냄.

**닫기(X) 버튼은 손대지 않기로 함 (2026-07-28 사용자 확정)**

아티클 히어로에서 X가 잘 안 보인다는 얘기가 나와 조사했고, **그대로 두기로 결론.**

- X 아이콘은 **6곳이 완전히 같은 SVG**다(전체보기·발도장 시트·발도장 상세·아티클·레시피 상세·가챠 —
  전부 18×18 / `stroke-width 1.02` / `scale(1.667)`). 스토리 뷰어만 사진 위라 `1.8`로 조금 굵다.
  **사이트 전체가 통일돼 있으므로 아티클만 따로 바꾸지 않는다.**
- 🔴 **흰색으로 바꾸자는 안은 틀렸다.** 안물안궁 히어로의 X 자리 평균색은 `#E2D6A3`(연노랑)이라
  지금의 어두운 `#4D4139`가 **6.75:1**, 흰색은 **1.46:1**이다 — 흰색이면 더 안 보인다.
- 진짜 원인은 색이 아니라 **획 굵기(화면에서 약 1.3px)**다. 대비가 충분해도 무늬 많은 그림 위에서는
  가는 선이 묻힌다. 문제가 생기는 곳은 **아티클 히어로 한 곳뿐**이고 나머지 다섯은 단색 배경이라 또렷하다.
- 다시 고치자는 얘기가 나오면 출발점: `stroke-width 1.02 → 1.3`(화면 1.3px → 1.6px)을
  **6곳 전부에** 적용. 단색 배경에선 차이가 거의 없고 그림 위에서만 또렷해진다.

**발견만 하고 미룬 것**

- ~~`buildCard()`와 딸린 CSS는 호출되는 곳이 없는 죽은 코드다. 정리 시점: 메뉴 탭 완성 → main 배포 후.~~
  → ✅ **2026-07-30 삭제·배포 완료(`0bf09e4`)**. 실제 규모는 CSS 약 60줄이 아니라 **335줄**이었다.
  자세한 내역·남긴 것·검증 방법은 이 문서 앞쪽 A항의 「죽은 코드 삭제」 절 참고.

**카드 반응 규칙 (2026-07-28 사용자 확정 — 사이트 전체 공통)**

두 갈래로 통일했다. 새 카드·버튼을 만들 때도 이 규칙을 따른다.

- **호버 = 틀은 그대로, 안의 사진만 확대**
- **누름(클릭·터치) = 사진은 그대로, 틀이 살짝 작아짐** (`transform: scale(.97)`, 메인 배너만 큰 요소라 `.985`)

탕(`.hc-card`)·히든메뉴(`.hc-row`)만 누를 때 이미지가 `scale(1.09)`로 커져 혼자 달랐던 것을 이 형식으로 바꿨다
(커밋 `0acce39`). **이는 2026-07-24의 '탕·히든은 이미지만 줌인' 결정을 대체한다** —
그때 문제였던 "이미지를 축소하면 줄어든 자리로 컨테이너 배경이 테두리처럼 드러남"은 틀째로 줄이면 생기지 않는다.

- 🔴 **히든메뉴는 행이 아니라 썸네일(`.hc-row-thumb`)만 줄인다.** 행에 위아래 구분선(border)이 붙어 있어
  행을 통째로 `scale`하면 선까지 짧아지며 이웃 행과 어긋난다.
- `.hc-card`는 홈 탕 그리드와 **전체보기 그리드가 함께 쓴다** → 전체보기 카드도 같이 바뀐다(사용자 확인 후 유지).
- 좋아요·즐겨찾기 버튼을 누르는 동안은 카드 눌림 제외(2026-07-25 규칙 유지).

---

# 완료된 이전 작업: 훠궈·하이디라오 잡학 아티클

## 1. 당시 저장소 상태

```text
경로: /Users/Macmini_J/ClaudeCode/03. Projects/Haidilgogo
브랜치: feat/bottom-nav
최신 기능 커밋: 79ee005 fix(칼럼): 아티클 뒤 화면 스크롤 잠금
원격: origin/feat/bottom-nav와 동기화됨 (인계 문서 갱신 커밋 포함)
main: 6e6ec03 (로컬·원격 동일)
배포 상태: 미배포 — main은 건드리지 않음
```

관련 최신 커밋:

```text
7c6d3b6 feat(알쓸신잡): 훠궈·하이디라오 아티클 추가
79ee005 fix(칼럼): 아티클 뒤 화면 스크롤 잠금
f50e07c docs: 알쓸신잡 작업 인계 문서 갱신
```

위 커밋은 `origin/feat/bottom-nav`에 푸시되어 원격 백업까지 완료됐다. **작업 브랜치 푸시는 백업이며 라이브 배포가 아니다.** 실제 GitHub Pages 배포는 `main`에 반영할 때만 일어나므로, 새 대화에서 임의로 `main`에 병합하거나 푸시하지 말 것.

---

## 2. 이번에 완료한 결과

홈 메인 배너의 기획 칼럼 시스템에 두 번째 아티클을 추가했다.

- 아티클 제목: `물어본 사람~ 궁금한 사람~` (2026-07-28 확정, `titleHtml`로 두 줄)
- 부제: `훠궈와 관련된 TMI`
- 칼럼 ID: `col-hotpot-trivia`

> **제목·배너 이미지 결론 (2026-07-28)**
>
> 원래 제목 `알쓸신잡`은 2017년 프로그램이라 낡았다는 판단으로 버렸고, 잠깐 `안물안궁`을 거쳐
> **`물어본 사람~ 궁금한 사람~`으로 확정**했다. 한 줄로는 320px에서 감기므로
> 가이드 배너처럼 `titleHtml`에 `<br>`을 넣어 **일부러 두 줄로 끊는다.**
> 측정값(320px): 두 줄 각 149px, 제목 상자 248px → 여유 99px로 안정적.
>
> **배너 이미지는 사용자가 다시 만들어 교체했다**(1254×1254 → `assets/columns/hotpot-trivia.webp` 1080×1080).
> 새 그림에는 `물어본 사람~ 궁금한 사람~` **글씨가 구워져 있다** — 애초 계획은 글씨 없는 그림이었으나
> 사용자가 글씨를 넣어 만들었고, 화면 제목도 같은 말로 맞췄다. 그래서 **같은 문구가 그림과 흰 글씨로 두 번 보인다.**
> 🔴 앞으로 **제목을 바꾸면 이미지도 함께 바꿔야 한다**(글씨가 그림에 박혀 있음).
>
> 🔴 **화면 제목에만 예능 자막 밈 화살표가 있다 — 의도한 것이다. 맞추려고 건드리지 말 것.**
> 화면 제목은 `물↘어본↗사람~ / 궁↘금한↗사람~`인데 그림 속 손글씨에는 화살표가 없다.
> 어긋난 게 아니라 **"너무 똑같은 것보다 약간 변주를 주는 게 좋다"는 사용자 판단**(2026-07-28)이다.
> 화살표를 그림에 넣거나 화면에서 빼서 둘을 똑같이 만들지 않는다.
> 화살표는 `TITLE_ARROW_DOWN`·`TITLE_ARROW_UP`(script.js) **인라인 SVG**다 —
> ↘(U+2198)·↗(U+2197)을 글자로 쓰면 iOS Safari가 컬러 이모지로 그려 톤이 깨지고 기기마다 달라진다.
> 크기는 `.ttl-arw`(styles.css)에서 `em`이라 배너 30px·아티클 24px에 자동으로 맞고,
> `stroke: currentColor`라 글자색을 따라간다. `title`(낭독·aria)은 화살표 없는 순수 글자로 둔다.
> 측정(320px): 두 줄 각 187px / 제목 상자 248px → 여유 61px.
>
> 🔴 **X 버튼 색 주의.** 옛 썸네일이 어두워서 상단 X를 흰색으로 했었는데, 새 그림이 밝아 X가 안 보이게 됐다.
> 이제 `id` 하드코딩이 아니라 칼럼 데이터의 `lightClose: true`로 켠다(안물안궁은 꺼짐).
> **어두운 썸네일을 쓰는 칼럼이 생기면 그 칼럼에 `lightClose: true`를 준다.**
>
> `id`와 이미지 파일명(`hotpot-trivia`)은 화면에 안 나오므로 바꾸지 않는다.
- 배너 위치: 기존 `CILANTRO_COLUMN` 다음, 가이드 배너 이전
- 본문 구성: 도입부 + 5개 항목 + 출처 5개
- 토끼 훠궈 항목은 사용자 결정으로 제외
- 고수 아티클과 동일한 칼럼 서식 사용

본문 순서:

1. 훠궈와 하이디라오는 무슨 뜻일까?
2. 양고기 훠궈는 전쟁터에서 태어났다고?
3. 마라를 먹으면 왜 입안이 얼얼할까?
4. 아홉 칸으로 나뉜 훠궈 냄비가 있다고?
5. 훠궈에 고기 대신 사람이 들어간다고?

본문은 `script.js`의 `HOTPOT_TRIVIA_COLUMN`에 있다.

---

## 3. 확정된 문안·표기 결정

다음은 사용자와 하나씩 검토해 확정한 내용이다. 새 대화에서 임의로 되돌리거나 다시 제안하지 않는다.

- 첫 항목에 **훠궈(火锅)**와 **하이디라오(海底捞)** 뜻풀이를 함께 넣는다.
- 훠궈는 `불 화(火)`와 `솥 과(锅)`로 풀이한다.
- 하이디라오 설명에는 “글자 그대로”라는 표현을 쓰지 않는다.
- 마작 용어는 `하이디라오웨(海底捞月)` 전체 이름과 쓰촨에서 줄여 부르는 `하이디라오`를 함께 설명한다.
- 창업 일화는 `하이디라오 창업자 장융`과 당시 여자친구의 마지막 패 승리·작명 제안을 포함한다.
- 양고기 항목은 훠궈 전체의 역사적 기원으로 단정하지 않고, 쿠빌라이 칸과 `솬양러우(涮羊肉)`에 관한 전승으로 쓴다.
- 마라는 `저릴 마(麻)`와 `매울 랄(辣)`로 풀이하며, `마비된 듯한 얼얼함`이라는 표현을 쓴다.
- 얼얼함을 만드는 재료는 **화자오(花椒)**만 본문에 언급한다.
- 하이디라오의 최대 네 칸 냄비와 충칭의 **구궁격 훠궈(九宫格火锅)**를 비교한다.
- 구궁격 유래는 과거 부두 노점의 합석·재료 구분·개별 계산에 관한 이야기로 소개한다.
- 온천은 `하얼빈의 펑예샤오전 온천 리조트(枫叶小镇温泉度假区)`로 실명을 넣는다.
- 온천의 붉은 탕은 빨간 조명 연출이며, 떠다니는 고추·채소 가운데 일부는 모형이라고 정리한다.
- 각 문단의 첫 핵심어인 훠궈·하이디라오와 본문의 화자오·구궁격 훠궈는 굵게 표시한다.
- 각 사실 항목의 마지막에 출처 번호 버튼을 둔다.

---

## 4. 이미지와 경로

모든 이미지는 WebP로 용량 최적화해 저장소에 넣었다. 원본 데이터 위치는 저장소 밖의 `02. Data/Haidilgogo`이며 삭제하지 않았다.

```text
assets/columns/hotpot-trivia.webp                         80,484 bytes
assets/columns/hotpot-trivia/mahjong.webp                 44,398 bytes
assets/columns/hotpot-trivia/yuan-dynasty.webp           120,780 bytes
assets/columns/hotpot-trivia/sichuan-pepper.webp          58,282 bytes
assets/columns/hotpot-trivia/9-grid-hotpot.webp            99,330 bytes
assets/columns/hotpot-trivia/harbin-hotpot-spa.webp       158,608 bytes
```

사용자 지정 원본 위치:

```text
본문 이미지:
02. Data/Haidilgogo/main-banner/hotpot-trivia/

썸네일:
02. Data/Haidilgogo/thumbnail/main-banner/columns/hotpot-trivia/
```

이미지 배치 순서는 `mahjong` → `yuan-dynasty` → `sichuan-pepper` → `9-grid-hotpot` → `harbin-hotpot-spa`이다.

---

## 5. 칼럼 공통 UI 변경

이번 작업에서 알쓸신잡 아티클뿐 아니라 기존 고수 아티클에도 적용되는 공통 변경이 들어갔다.

- 출처 목록 아래에 `닫기` 버튼을 추가했다.
- 상단 X와 하단 `닫기`는 같은 `closeColumn()`을 호출한다.
- 어두운 알쓸신잡 썸네일 위에서만 상단 X를 흰색으로 표시한다.
- 다른 칼럼은 기존 X 색상을 유지한다.
- 칼럼을 열면 `html`의 스크롤을 잠그고, 닫으면 복원한다.
- `.column-overlay`에 `overscroll-behavior-y: contain`을 적용해 데스크탑에서 아티클 끝에 닿아도 뒤 페이지로 스크롤이 전달되지 않게 했다.

당시 캐시 버전(현재 값은 A항 참고):

```text
styles.css?v=578
script.js?v=421
```

---

## 6. 검증 결과

자동 테스트 스위트는 없으며, 로컬 서버와 실제 브라우저로 확인했다.

데스크탑 `1024×900`:

- 칼럼을 열면 `html`의 계산된 `overflow`가 `hidden`
- 칼럼 오버레이는 `overscroll-behavior-y: contain`
- 오버레이를 최하단까지 스크롤해도 `window.scrollY`는 `0`
- 하단 `닫기`로 닫으면 `html` 스크롤이 `auto`로 복원
- 콘솔 오류 없음

모바일 `375×812`:

- 칼럼 정상 표시
- `html` 스크롤 잠금 정상
- `clientWidth`와 `scrollWidth`가 모두 `375`로 가로 넘침 없음
- 콘솔 오류 없음

확인 시에는 기존 고수 아티클의 하단 닫기와 알쓸신잡의 흰색 X도 함께 회귀 확인한다.

---

## 7. 출처

아티클의 출처는 `HOTPOT_TRIVIA_COLUMN.notes`에 등록되어 있다.

1. 중국요리협회 — 하이디라오 창업자 장융 인터뷰
2. CCTV — 솬양러우 기원에 관한 세 가지 전설
3. 영국왕립학회 — 화자오의 산쇼올이 일으키는 진동 감각 연구
4. 중국요리협회 — 충칭 훠궈의 변천
5. 중국관광신문 — 펑예샤오전의 원앙궈 온천

본문의 `[1]`~`[5]` 버튼은 URL이나 브라우저 history를 바꾸지 않고 하단의 해당 출처로 스크롤한다.

---

## 8. 새 대화에서 이어갈 때

1. `CLAUDE.md`와 이 문서를 먼저 읽는다.
2. `git status`, 현재 브랜치, 원격과의 차이를 확인한다.
3. 이 아티클의 **본문·출처·UI는 완료**로 취급한다. 단 **제목·부제·배너 이미지는 2026-07-28에 다시 열렸다**(위 2절 참고).
4. 사용자가 추가 수정을 요청하면 `HOTPOT_TRIVIA_COLUMN`과 공통 칼럼 UI의 영향 범위를 구분해 작업한다.
5. 수정한 CSS/JS에 맞춰 `index.html`의 자산 버전만 올린다.
6. `main` 병합·푸시는 사용자가 배포를 명시적으로 지시할 때만 한다.
7. 작업 브랜치에서 커밋하면 `origin/feat/bottom-nav`에도 푸시한다. 이는 원격 백업일 뿐 배포가 아니다.
