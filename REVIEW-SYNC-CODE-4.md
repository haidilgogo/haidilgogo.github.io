# 「내 데이터 코드」 4차 교차 검증 요청서 — 3차 수정분 재검증

> **읽는 사람: 코덱스(또는 다른 검토자).**
> 🔴 **저장소 파일을 읽기만 하고 수정하지 마세요.**
> 발견한 것은 **파일:줄번호 + 증상 + 원인 + 재현 + 심각도**로 정리해 주세요.
>
> 작성 2026-07-31 / 대상 커밋 `3c28840`(main, 배포 완료) / 캐시버전 css v624 · **script v530**
> 앞선 요청서: `REVIEW-SYNC-CODE.md`(1차, **불변식 D1~D8 정의**) · `-2.md`(2차) · `-3.md`(3차)

---

## 0. 3차 검토 결과 — 세 번째도 맞았다

3차 지적 3건이 전부 사실이었고 모두 고쳤다. 이번 건은 특히 **2차에서 내가 도입한 트랜잭션이 만든 새 구멍**이었다 — 세 번 연속으로 "수정이 새 문제를 만드는" 형태가 반복됐다.

또한 3차에서 **「구버전에서 이력 없이 꺼진 즐겨찾기가 되살아남」을 고치지 않기로 한 판단**에 대해 "틀렸다고 보지 않는다"는 회신을 받았다. 이번에도 그대로 둔다.

---

## 1. 이번에 무엇을 바꿨나 (재검증 대상)

| # | 3차 지적 | 수정 |
|---|---|---|
| ① | 전송 중 새로 저장한 것이 커밋 결과에 덮임 (S1, D1·D3 영향) | 커밋 결과를 **그대로 적용하지 않고**, `applyPayload(mergePayloads(syncPayload(), 커밋결과))`로 **지금 로컬과 한 번 더 합쳐서** 넣는다 |
| ② | 코드 A 전송 중 코드 B를 불러오면 A의 늦은 결과가 B를 덮음 | 적용 전에 **`getSyncCode() === code`** 확인. 다르면 적용하지 않는다 |
| ③ | 다른 기기가 고친 스티커가 목록에 옛 내용으로 보임 | `applyPayload`에서 **`stampCardCache.clear()`** |
| R1 | 스냅샷이 완전히 고정되지 않음 | `syncPayload`가 `favMarks`·`likedMarks`를 **얕은 사본**으로 넘긴다 |

**볼 곳(전부 `script.js` 맨 아래 「내 데이터 코드」 절):**
`syncPayload` · `fingerprint` · `marksOf` · `mergeMarkMaps` · `mergePayloads`(순수) · `setsFromMarks` · `applyPayload` · `mergeIntoLocal` · `pushSync`(트랜잭션) · `schedulePush` · `syncOnStart`

---

## 2. 🔴 이번 수정이 새로 만들었을 위험

세 번 연속 그랬으니, 이번에도 그럴 것으로 보고 봐 달라.

### R1. 커밋 결과를 로컬과 다시 합치는 것(①의 부작용)
`applyPayload(mergePayloads(syncPayload(), committed))`

- 전송 중에 사용자가 **기록을 지웠다면**, 그 tombstone은 지금 로컬(`syncPayload()`)에 있다. 커밋 결과에는 그 기록이 살아 있는데, 다시 합칠 때 **되살아나지 않는가?**
- 반대로 전송 중에 **다른 기기가 지운 것**이 커밋 결과에 tombstone으로 들어왔다면, 지금 로컬이 그걸 덮어 되살리지 않는가?
- 합치기가 한 번 더 도는 만큼 **동점 규칙**(스티커: 문자열 큰 쪽, 이력: 끈 쪽)이 한 번 더 적용된다. 이 때문에 값이 왔다 갔다 하는(수렴하지 않는) 조합이 있는가?
- `before = fingerprint()`는 **트랜잭션 시작 전에** 찍는다. 전송 중 사용자가 저장하면 `changed`가 자기 변경 때문에 참이 되어 `refreshAfterSync()`가 불필요하게 도는데, 그게 또 `schedulePush`를 부른다 — **끝없는 왕복**이 되는 입력이 있는가?

### R2. 코드 확인(②의 부작용)
- 코드가 바뀌었으면 **적용을 건너뛴다.** 그런데 트랜잭션은 이미 커밋됐다 — 즉 **서버에는 A의 합친 결과가 쓰였다.** 이게 문제가 되는 상황이 있는가?
- 코드 전환 직후 `pendingPush`가 새 코드로 다시 올리는데, 그 사이(옛 코드로 쓴 뒤 ~ 새 코드로 올리기 전)에 페이지를 닫으면 어떤 상태가 남는가?
- `getSyncCode()`는 이제 `syncCodeMem`을 먼저 본다. 이 비교가 의도대로 "지금 코드"를 가리키는가?

### R3. 카드 캐시 전체 삭제(③의 부작용)
- `applyPayload`는 **저장할 때마다**(모든 push 커밋 후) 불린다. 그때마다 `stampCardCache.clear()`가 돌아 **모든 카드를 다시 만든다.**
  - 기록이 많은 사용자에서 렌더 비용이 문제가 되는가?
  - 스티커 이미지가 다시 로드되며 **깜빡임**이 생기는가?
  - 스크롤 위치나 눌림 상태가 튀는가?
- 캐시를 지우는 것 말고 **바뀐 id만 지우는 편이 맞는가?**

### R4. 스냅샷 사본(R1의 부작용)
- `Object.assign({}, favMarks)`는 **얕은** 사본이다. 항목 객체(`{v,t}`)는 공유된다. `updateMarks`가 항목을 **교체**만 하고 **수정**하지 않는다는 전제가 코드 전체에서 참인가?
- `applyPayload`가 `favMarks = p.favMarks`로 **통째 교체**한다. 그 객체는 `mergePayloads`가 만든 것이라 안전한가? 바깥에서 참조를 들고 있는 곳은 없는가?

### R5. 여전히 남아 있는 것
- `once('value')`/트랜잭션이 실패하면 **자동 재시도가 없다.** 다음 저장이나 다음 앱 시작까지 서버 사본이 뒤처진다. 이게 데이터 유실로 이어지는 경로가 있는가?
- `stamps.deleted`(지운 id)는 **영원히 쌓인다.** 정리 규칙이 없다.

---

## 3. 다시 판정해 줄 것

- **D1 (데이터 보존)** — 3차에서 실패였다. 전송 중 저장·코드 전환 두 경로가 막혔는가?
- **D3 (해제 유지)** — 전송 중 해제가 유지되는가?
- **D5 (Firebase 실패 격리)** — 3차에서 통과. 이번 변경으로 깨지지 않았는가?

---

## 4. 이번에 사용자 쪽에서 실제로 돌려본 것 (참고)

내가(클로드) 브라우저에서 재현한 결과다. **그대로 믿지 말고 다시 확인해 달라.**

| 시나리오 | 결과 |
|---|---|
| 전송 중 즐겨찾기 3개 연속 저장 | 3개 다 로컬·서버에 남음 |
| 전송 중 즐겨찾기 해제 | 꺼진 채 유지 |
| 다른 기기가 스티커 매장·날짜 수정 후 동기화 | 목록 카드가 새 내용으로 갱신 |
| 기본 흐름(코드 발급 → 서버 반영) | 이상 없음 |

**안 해본 것**: 트랜잭션 실패·재시도 한도 초과, 저장소 용량 초과, 두 탭 동시 사용, 실기기 사파리↔홈 화면.

---

## 5. 검증 시 주의 (전부 실제로 당한 것)

1. 🔴 테스트용 코드에 **`O`·`I`·`0`·`1`을 쓰지 말 것.** 형식 검사에서 거부된다. **`A-H · J-N · P-Z · 2-9`** 만(예: `HG-TESTA2`).
2. 🔴 붙여넣기는 **`input.value` 직접 대입으로 테스트하지 말 것.** 실제 경로(`document.execCommand('insertText', …)`)를 쓸 것.
3. 🔴 **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 바뀐다. 눌렀으면 되돌리고 숫자 확인.
4. 🔴 `localStorage`만 비우고 **새로고침을 안 하면 화면이 옛 상태를 들고 있다.**
5. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것.
6. ⚠️ 브라우저 콘솔은 배경에서 타이머가 늘어진다. **시간 측정값을 믿지 말 것.**

---

## 6. 아직 아무도 확인 못 한 것

⬜ **실기기에서 사파리 → 홈 화면 복구.** 이 기능을 만든 이유인데 사용자가 아직 확인하지 못했다. 코드 리뷰로는 답이 안 나온다.

---

## 7. 참고
1차 요청서에 **기능 목적·서버 구조·보안 규칙·의도된 동작 목록**이 있다. 앱 전체 규칙은 `CLAUDE.md`.
🔴 이 문서도 내부 문서다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 했다.
