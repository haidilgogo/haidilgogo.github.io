# 「내 데이터 코드」 교차 검증 요청서

> **읽는 사람: 코덱스(또는 다른 검토자).**
> 🔴 **저장소 파일을 읽기만 하고 수정하지 마세요.** 코드 수정은 Claude 대화에서만 합니다.
> 발견한 것은 **파일:줄번호 + 왜 문제인지 + 재현 방법**으로 정리해 사용자에게 전달해 주세요.
>
> 작성 2026-07-31 / 대상 커밋 `ba470da`(main, 배포 완료) / 캐시버전 css v624 · script v527

---

## 0. 이 기능이 왜 있는가

`localStorage`만으로는 이 사이트의 데이터를 지킬 수 없다는 것이 실기기에서 확인됐다.

1. **iOS 홈 화면 웹앱은 사파리와 저장소가 분리된다.** 사파리에서 남긴 스티커 기록이 홈 화면 앱에선 하나도 안 보였다(사용자 실기기 확인). 홈 화면 추가는 사이트가 마음에 든 사람이 하는 행동이라 **기록을 가장 많이 쌓은 사람이 가장 크게 잃는다.**
2. 사파리는 7일간 방문이 없으면 script-writable 저장소를 비울 수 있다(애플 ITP).
3. 폰을 바꾸면 끝난다.

스티커 방문 일기는 **사용자가 직접 쓴 것이라 복구가 불가능**하다. 그래서 서버에 사본을 둔다.

**방식** = 코드 하나(`HG-` + 6자리). 로그인·이메일·개인정보 없음. 이미 쓰던 Firebase Realtime DB에 얹었다. 사실상 **비밀번호 없는 아이디**다.

---

## 1. 어디를 보면 되는가

| 파일 | 위치 | 내용 |
|---|---|---|
| `script.js` | 맨 아래 「내 데이터 코드」 절 | 코드 생성·업로드·병합·시트 UI 전부 |
| `script.js` | `saveStamps` / `saveFavorites` / `saveLikes` | 세 저장 함수가 모두 `schedulePush()`를 호출 |
| `script.js` | `favMarks` / `likedMarks` 선언부(좋아요 상수 근처) | 켠/끈 이력 |
| `index.html` | `#codeNotice` (스티커 탭) | 안내 띠 |
| `index.html` | `#syncSheetOverlay` | 코드 시트 |
| `styles.css` | `.code-notice*` / `.sync-*` | 관련 스타일 |

**서버 구조**: `sync/<코드>` = `{ stamps: { records, deleted }, favorites, liked, favMarks, likedMarks, updatedAt }`

**보안 규칙**(Firebase 콘솔, 저장소 밖):
```json
"sync": {
  "$code": {
    ".read": "$code.matches(/^HG-[A-Z2-9]{6}$/)",
    ".write": "$code.matches(/^HG-[A-Z2-9]{6}$/) && newData.hasChild('updatedAt')",
    ".validate": "newData.hasChildren(['updatedAt'])"
  }
}
```

---

## 2. 반드시 지켜야 하는 불변식 (여기가 깨지면 심각)

이 목록이 이 문서의 핵심이다. **각 항목이 코드에서 실제로 보장되는지 확인해 달라.**

### D1. 합치기는 절대 데이터를 잃지 않는다
불러오기는 **덮어쓰기가 아니라 합치기**다(사용자 확정). 다른 기기 코드를 불러와도 **지금 기기의 데이터가 사라지면 안 된다.** 이 기능의 목적 자체가 "잃지 않기"라, 여기서 잃으면 만든 의미가 없다.

### D2. 지운 스티커는 되살아나지 않는다
`stampData.deleted`에 지운 id를 남긴다. 이게 없으면 A기기에서 지운 기록이 B기기 동기화 때 되살아난다.

### D3. 끈 즐겨찾기·좋아요도 되살아나지 않는다
`favMarks`/`likedMarks` = `{ id: { v: 1켬|0끔, t: 바꾼시각 } }`. 합칠 때 **시각이 나중인 쪽이 이긴다.**
🔴 스티커식(지운 id 목록)을 쓰면 안 되는 이유: 즐겨찾기는 **껐다 다시 켤 수 있어서** "지웠다"만 적으면 다시 켜는 게 영원히 막힌다.

### D4. 좋아요 **집계 숫자**는 이 기능이 절대 건드리지 않는다
숫자는 `likes/`에 있고, 여기서 다루는 건 "내가 눌렀다"는 표시뿐이다. `getRealLikeCount`(실제분)와 `getLikeCount`(더미+실제)를 섞으면 안 된다.

### D5. 서버가 없거나 막혀도 앱은 그대로 동작한다
동기화는 전부 덤이다. Firebase 실패가 저장·기록 흐름을 멈추면 안 된다.
🔴 실제로 겪은 사고: **Firebase `set()`은 값에 `undefined`가 하나라도 있으면 그 자리에서 예외를 던진다**(비동기 아님). 동행을 안 적은 기록의 `with`가 undefined라 코드 발급 직후 거기서 멈췄고, 화면엔 아무 표시가 없어 원인 찾기가 오래 걸렸다. → `syncPayload()`의 `clean()`으로 털어내고 `pushSync()` 전체를 `try`로 감쌌다. **새 필드를 추가하면 재발할 수 있다.**

### D6. 코드는 한번 생기면 안 지운다
데이터가 비어도 남는다(사용자 확정). 좋아요를 눌렀다 빼면 코드만 남고 내용이 빈 상태가 되는데 **버그가 아니다.** 비었다고 지우면, 적어둔 코드가 나중에 "저장된 데이터가 없어요"가 되는 사고가 난다.

### D7. 코드 생성은 `schedulePush()` 한 곳에만 있다
세 저장 함수가 전부 여기를 지난다. 예전에 `saveStamps`에만 걸어놨다가 **즐겨찾기·좋아요만 쓰는 사람은 서버 저장이 통째로 안 되는** 버그가 있었다. 새 저장 경로를 만들면 반드시 `schedulePush()`를 태워야 한다.

### D8. `sync` 루트는 읽을 수 없어야 한다
열면 **모든 사용자의 데이터가 통째로 노출**된다. 규칙은 `sync/$code`에만 읽기를 허용한다.

---

## 3. 의도된 동작 (버그로 오해하기 쉬움 — 고치자고 제안하지 말 것)

| 현상 | 왜 그런가 |
|---|---|
| **겉보기 똑같은 스티커가 2개 남는다** | 두 기기에서 같은 방문을 각각 적으면 id가 달라 서로 다른 기록으로 본다. 사용자가 **지금처럼 두기로 확정**(합치다 잃는 것보다 낫다). |
| **데이터가 비어도 코드가 남는다** | D6 참고. |
| **자기 코드를 넣어도 막지 않는다** | 폰을 되찾은 사람이 자기 코드를 넣어보는 건 자연스럽다. 합치기라 여러 번 해도 해롭지 않다. |
| **스티커 그림이 없는 매장은 기록 드롭다운에 안 나온다** | 별개 기능의 의도된 동작(새 매장은 스티커부터 만들 것). |
| **띠가 접혀도 사라지지 않는다** | "이 기기에만 저장된다"를 계속 보이게 하는 게 목적. 닫기가 아니라 접기다. |

---

## 4. 재현해 볼 만한 시나리오

브라우저 콘솔에서 `localStorage`를 직접 세팅하고 새로고침하면 기기를 옮긴 상황을 흉내 낼 수 있다.
관련 키: `haidilao_stamps` · `haidilao_sync_code` · `haidilao_favorites` · `haidilao_liked` · `haidilao_fav_marks` · `haidilao_liked_marks` · `haidilao_code_notice_folded`

1. **두 기기 합치기** — A에서 기록 → B(저장소 비움)에서 다른 기록 → B에서 A 코드 불러오기 → **둘 다 남아야 한다**(D1).
2. **삭제 유지** — 지운 id를 서버에 되살려 심고 새로고침 → **안 돌아와야 한다**(D2).
3. **해제 유지** — 즐겨찾기를 끈 뒤, 서버에 *더 오래된* 시각으로 켜진 상태를 심고 새로고침 → **꺼진 채여야 한다**. 반대로 *더 나중* 시각이면 켜져야 한다(D3).
4. **붙여넣기** — `HG-XXXXXX` / 6자리만 / 소문자 / `하딜고고 내 데이터 코드: HG-XXXXXX` 문장째 → 전부 6자리로 정리돼야 한다.
   🔴 **`input.value`에 직접 대입해서 테스트하지 말 것.** 그건 브라우저의 붙여넣기 경로를 우회한다 — 실제로 이것 때문에 `maxlength`가 붙여넣기를 잘라먹는 버그를 놓쳤다. `document.execCommand('insertText', …)`처럼 실제 입력 경로로 확인할 것.
5. **오프라인** — Firebase를 막고 기록·즐겨찾기가 정상 동작하는지(D5).

⚠️ **좋아요 하트는 함부로 누르지 말 것** — 라이브 집계가 실제로 바뀐다. 눌렀으면 반드시 다시 눌러 원래 숫자로 되돌리고 확인할 것.
⚠️ 검증에 쓴 `sync/<코드>`는 빈 값으로 덮어 비울 것(규칙상 통째 삭제는 막혀 있다).

---

## 5. 특히 봐줬으면 하는 것

1. **D1이 정말 보장되는가.** `mergeIntoLocal`에서 로컬 데이터가 사라지는 경로가 하나라도 있는지.
2. **경쟁 조건.** `syncOnStart`(앱 열 때 합치기)와 `schedulePush`(800ms 묶음 업로드)가 겹칠 때, 늦게 도착한 응답이 새 데이터를 덮어쓸 수 있는지.
3. **`extractCode` / `normalizeCode`가 어긋나는 입력**이 있는지. 특히 코드 본체가 `HG`로 시작하는 경우(길이 8일 때만 접두어로 취급한다).
4. **`clean()`이 놓치는 `undefined`** — 중첩 객체 안쪽은 털어내지 않는다. 지금 구조에서 문제가 되는지.
5. **저장소 용량 초과**(`localStorage` 가득 참) 때 조용히 실패하는 경로.

---

## 6. 아직 확인 못 한 것 (검토 범위 밖)

⬜ **실기기에서 사파리 → 홈 화면** — 이 기능을 만든 진짜 이유인데, 배포 직후라 사용자가 아직 확인하지 못했다. 코드 리뷰로는 답이 안 나오는 항목이다.

---

## 7. 참고

- 앱 전체 규칙은 `CLAUDE.md`, 진행 상황은 `HANDOVER.md`.
- 캐시 버전 규칙: CSS/JS를 고치면 `index.html`의 `?v=` 숫자를 올린다.
- 🔴 이 문서는 **내부 문서**다. `_config.yml`의 `exclude`에 넣어 웹에 공개되지 않게 했다. 새 내부 문서를 만들 때도 반드시 같이 넣을 것(과거에 `HANDOVER.md`가 웹에 열려 있던 사고가 있었다).
