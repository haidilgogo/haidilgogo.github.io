# 「내 데이터 코드」 5차 교차 검증 요청서 — 4차 수정분 재검증

> **읽는 사람: 코덱스(또는 다른 검토자).**
> 🔴 **저장소 파일을 읽기만 하고 수정하지 마세요.**
> 발견한 것은 **파일:줄번호 + 증상 + 원인 + 재현 + 심각도**로 정리해 주세요.
>
> 작성 2026-07-31 / 대상 커밋 **`4096316`** / 캐시버전 css v624 · **script v531**
> 앞선 요청서: `REVIEW-SYNC-CODE.md`(1차, **불변식 D1~D8 정의**) · `-2.md`(2차) · `-3.md`(3차) · `-4.md`(4차)

## 🔴 이번엔 **배포 전** 검증이다 (지난 네 번과 다른 점)

- 대상 커밋은 **`main`이 아니라 작업 브랜치 `fix/sync-review4`**에 있다. **라이브는 아직 `3c28840`(script v530)** 이고, 4차 지적 3건이 살아 있는 상태 그대로다.
- 1~4차는 전부 **배포한 뒤에** 검증했고 **매번 결함이 나왔다.** 그래서 이번엔 순서를 바꿨다(사용자 지시). 검증을 통과해야 배포한다.
- 즉 **여기서 잡는 결함은 사용자에게 아직 도달하지 않았다.** 반대로 못 잡으면 라이브로 나간다.

```bash
git fetch origin && git checkout fix/sync-review4   # 또는 origin/fix/sync-review4를 읽기만
```

---

## 0. 4차 검토 결과 — 네 번째도 맞았다

4차 지적 3건이 전부 사실이었고 셋 다 고쳤다. **네 번 연속으로 "내 수정이 새 구멍을 만드는"** 형태가 반복됐다는 사실은 그대로다. 이번 수정도 같은 의심을 받아야 한다.

---

## 1. 이번에 무엇을 바꿨나 (재검증 대상)

전부 `script.js` 맨 아래 「내 데이터 코드」 절이다. 바뀐 함수는 **`syncOnStart` · `applyPayload`** 둘, 새로 생긴 함수는 **`recFingerprint`** 하나뿐이다.

| # | 4차 지적 | 수정 |
|---|---|---|
| ① | 서버가 비어 있으면(`if (!remote) return;`) 업로드를 영영 안 해 백업이 없다 | `remote`가 없어도 **`pushSync()`까지 간다.** 합치기·화면 갱신만 `if (remote)` 안으로 옮겼다 |
| ② | `syncOnStart`에 코드 확인이 없어 코드 A의 늦은 응답이 B에 섞인다 | 응답을 받은 자리에서 **`if (getSyncCode() !== code) return;`** — `pushSync`에만 있던 확인을 여기에도 뒀다 |
| ③ | `applyPayload`가 카드 캐시를 매번 전부 비운다 | **내용이 바뀐 id만** `stampCardCache.delete()`. 비교는 새 함수 `recFingerprint`(키를 정렬해 찍는 지문) |

```js
// ③ 핵심
function recFingerprint(r) { return JSON.stringify(r, Object.keys(r).sort()); }
const nextById = new Map(stampData.records.map((r) => [r.id, r]));
prevRecs.forEach((old) => {
  const nw = nextById.get(old.id);
  if (nw && recFingerprint(nw) !== recFingerprint(old)) stampCardCache.delete(old.id);
});
```

---

## 2. 🔴 이번 수정이 새로 만들었을 위험

네 번 연속 그랬으니, 이번에도 그럴 것으로 보고 봐 달라.

### R1. 서버가 비어 있어도 올린다 (①의 부작용)
- 로컬이 **완전히 빈** 사용자(기록·즐겨찾기·좋아요 0)가 코드만 들고 앱을 열면, 이제 매 실행마다 **빈 payload를 서버에 쓴다.** 이게 **남의 데이터를 지우는 경로**가 되는가? (예: 코드를 손으로 입력해 두고 아직 불러오기 전인 상태, 두 기기가 같은 코드를 쓰는 상태)
- `pullSync`는 실패해도 **`catch(() => null)`로 `null`을 돌려준다.** 즉 "서버가 빈 것"과 "읽기 실패"가 구분되지 않는다. 읽기가 실패한 상태에서 곧바로 `pushSync`가 도는데, 트랜잭션이 서버 최신값을 다시 읽으므로 **유실은 없다는 것이 내 판단**이다. 맞는가? 트랜잭션 콜백이 `null`을 받는 경우(로컬 캐시 기반 낙관적 시도)까지 포함해 확인해 달라.
- 규칙상 `sync/$code` 쓰기는 `updatedAt`을 요구한다. 빈 payload도 `mergePayloads`를 거치므로 `updatedAt`이 붙는다 — **규칙에 걸려 조용히 실패하는 조합**이 있는가?
- 오프라인·규칙 거부가 계속되는 사용자는 이제 **앱을 열 때마다** 트랜잭션을 시도한다. 재시도 폭주나 눈에 보이는 부작용이 있는가?

### R2. `syncOnStart`의 코드 확인 (②의 부작용)

- 코드가 바뀌었으면 **`pushSync`까지 통째로 건너뛴다.** 새 코드 B로의 업로드는 **불러오기 핸들러가 직접 `pushSync()`를 부르므로** 빈 곳이 없다고 봤다(확인함). 맞는가?
- 반대 오탐: **같은 코드**인데 `syncCodeMem`과 `localStorage`가 어긋나 응답을 버리는 경우가 있는가?

#### 🔴 R2-a. 이 수정이 ②를 **부분적으로만** 막는다 (내가 직접 재현함, 미수정)

**판단을 부탁하고 싶은 핵심이 이것이다.** 불러오기 핸들러는 **`pullSync(B)`가 돌아온 뒤에야 `setSyncCode(B)`를 부른다.** 그래서 **"불러오기를 누른 순간 ~ B 응답 도착"** 사이에는 `getSyncCode()`가 아직 A다 → 내 확인이 통과한다.

재현(가짜 스텁, v531):

| 순서 | 일어난 일 |
|---|---|
| 1 | 코드 A로 앱 시작. 로컬엔 `stLocal` 하나. A 서버엔 **이 기기에 없던 `stA`** |
| 2 | 사용자가 코드 B를 입력하고 **불러오기** 누름 (B 응답 대기 중, 코드는 아직 A) |
| 3 | 이때 A의 늦은 응답 도착 → 확인 통과 → **`stA`가 로컬에 합쳐짐** |
| 4 | B 응답 도착 → 합치기 → `setSyncCode(B)` → `pushSync()` |
| 결과 | **B 서버 = `stLocal` + `stA` + `stB`** — 이 기기에 없던 A의 기록이 B로 올라갔다 |

- ⚠️ `syncOnStart`를 막아도 **`pushSync(A)`의 커밋 결과 적용**이 같은 일을 한다(트랜잭션이 A 서버 값과 합쳐 돌려주므로 `stA`가 로컬로 들어온다). 즉 막으려면 **두 곳 모두** 막아야 한다.
- 🔴 **그런데 이게 정말 결함인가?** 이 앱에서 **불러오기는 덮어쓰기가 아니라 합치기**다(사용자 확정 동작: "그 기기 기록 안 지워짐"). 코드 A를 쓰던 기기는 원래 A의 데이터를 합쳐 갖게 되어 있고, 그 기기가 B를 불러오면 가진 것이 B로 올라가는 게 설계다. 그렇다면 3번은 **"A 동기화가 200ms 일찍 끝났을 때와 같은 상태"**일 뿐이고, 유실이 아니라 **합집합**이다.
- 그래서 나는 **고치지 않고 판단을 맡긴다.** 고친다면 "전환 중인 코드"를 기억하는 상태값을 새로 두고 `syncOnStart`·`pushSync` 두 곳에서 봐야 하는데, **이 기능은 네 번 연속 '내 수정이 새 구멍을 만드는' 형태로 당했다.** 상태값을 하나 더 늘릴 값어치가 있는지 봐 달라.
- 판정해 줄 것: **(가)** 설계상 정상이라 그대로 둔다 / **(나)** 사용자가 놀랄 동작이라 막아야 한다 / **(다)** 막되 다른 방법이 있다(예: 불러오기 시작 시점에 코드를 먼저 바꾼다 — 다만 실패하면 코드가 바뀐 채 남는다).

### R3. 바뀐 id만 캐시 삭제 (③의 부작용)
- **지문이 같은데도 카드를 다시 그려야 하는 경우**가 있는가? 카드가 기록 필드 말고 다른 것(지점 스티커 이미지 `STAMP_IMGS`, 날짜 표기, 즐겨찾기·좋아요 상태 등)에 의존한다면 이 비교로는 못 잡는다.
- `recFingerprint`는 `JSON.stringify(r, keys)`의 **replacer 배열**을 쓴다. 이 배열은 **중첩 객체에도 적용**되므로, 기록에 중첩 객체 필드가 생기면 안쪽 키가 조용히 걸러진다. 지금 기록은 평면(`id·name·date·memo·with·addedAt·editedAt`)이라 안전하다고 봤다. 맞는가?
- `undefined` 필드(동행 안 적은 기록의 `with`)는 한쪽엔 있고 한쪽엔 없다. `JSON.stringify`가 둘 다 생략하므로 같게 나온다고 봤다. 맞는가?
- **사라진 id의 캐시**는 일부러 안 지웠다(`renderStamps` 끝의 `liveIds` 정리에 맡김). 그런데 `applyPayload` 뒤에 `refreshAfterSync()`는 **`fingerprint()`가 달라졌을 때만** 돈다. 기록이 사라지면 지문도 반드시 달라지므로 항상 다시 그린다고 봤다 — **안 그려지는 조합**이 있는가? 있다면 옛 카드가 화면에 남는가, 아니면 캐시만 새는가?
- `prevRecs`는 `stampData.records`의 **참조**다. 앱 다른 곳이 이 배열의 원소를 **제자리에서 수정**하면 "바뀐 것"을 못 잡는다. 기록 수정 경로(`stampCardCache.delete(stampEditId)` 근처)가 원소를 교체하는지 수정하는지 봐 달라.

### R4. 여전히 남아 있는 것 (4차와 동일, 고치지 않았다)
- 트랜잭션·읽기 실패에 **자동 재시도가 없다.** ①로 "앱을 열 때마다 한 번은 시도"가 됐지만 그뿐이다.
- `stamps.deleted`(지운 id)는 **영원히 쌓인다.**
- 즐겨찾기·좋아요는 구버전에서 이력 없이 꺼진 것이 되살아날 수 있다(3차에서 "그대로 둔다"로 합의).

---

## 3. 다시 판정해 줄 것

- **D1 (데이터 보존)** — ①이 새 유실 경로를 만들지 않았는가?
- **D3 (해제 유지)** — 변경 없음. 깨지지 않았는가?
- **D5 (Firebase 실패 격리)** — `pullSync` 실패 뒤 `pushSync`가 도는 경로가 생겼다. 앱 본업이 여전히 안 멈추는가?
- **4차 지적 3건이 제대로 고쳐졌는가** — 특히 ②가 "확인만 하고 실제로는 못 막는" 형태가 아닌지.

---

## 4. 이번에 내가(클로드) 실제로 돌려본 것

🔴 **그대로 믿지 말고 다시 확인해 달라.** 방법: `index.html`의 **Firebase CDN 두 줄만 가짜 스텁으로 갈아끼운 임시 페이지**를 만들고, 같은 방식으로 만든 **배포본(`main`) 사본**을 나란히 띄워 같은 시나리오를 돌려 대조했다. **앱 코드(`script.js`)는 한 글자도 안 바꿨다.**

| 시나리오 | 고치기 전(main v530) | 고친 뒤(v531) |
|---|---|---|
| 코드 있음 + 기록 1건, **서버는 빈 상태**로 앱 시작 | 트랜잭션 **0회**, 서버 백업 없음 | 트랜잭션 **1회**, 기록 올라감 |
| 코드 A 응답 도착 전에 코드 B로 **바뀐 뒤**(setSyncCode 완료) | A의 기록이 로컬에 합쳐지고 **B 서버로 업로드됨** | 응답 폐기 — 로컬·B 서버 **그대로** |
| 코드 B **불러오기가 아직 도는 중**(setSyncCode 전)에 A 응답 도착 | 〃 | **전과 같음 — 안 막힘**(위 R2-a) |
| 기록 2건 중 **1건만** 다른 기기에서 수정돼 도착 | 두 카드 **모두 재생성** | 안 바뀐 카드는 **같은 DOM 노드 재사용** |
| 평소 합치기(다른 기기 기록 따라오기 · 지운 것 안 되살아나기 · 즐겨찾기·좋아요 합집합 · 트랜잭션 2회) | — | **전/후 완전히 동일** |

**안 해본 것**: 실제 Firebase(가짜 스텁이라 네트워크·규칙·재시도가 안 걸린다) · 트랜잭션 실패/재시도 한도 · 두 탭 동시 사용 · 저장소 용량 초과 · **실기기 사파리↔홈 화면**.

---

## 5. 검증 시 주의 (전부 실제로 당한 것)

1. 🔴 테스트용 코드에 **`O`·`I`·`0`·`1`을 쓰지 말 것.** 형식 검사에서 거부된다. **`A-H · J-N · P-Z · 2-9`** 만(예: `HG-TESTA2`).
2. 🔴 붙여넣기는 **`input.value` 직접 대입으로 테스트하지 말 것.** 실제 경로(`document.execCommand('insertText', …)`)를 쓸 것.
3. 🔴 **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 바뀐다. 눌렀으면 되돌리고 숫자 확인.
4. 🔴 `localStorage`만 비우고 **새로고침을 안 하면 화면이 옛 상태를 들고 있다.**
5. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것(통째 삭제는 규칙이 막는다).
6. ⚠️ 브라우저 콘솔은 배경에서 타이머가 늘어진다. **시간 측정값을 믿지 말 것.**
7. ⚠️ **라이브(haidilgogo.com)로 검증하면 안 된다.** 라이브는 아직 v530이다. 이 브랜치를 로컬에서 띄워 볼 것.

---

## 6. 아직 아무도 확인 못 한 것

⬜ **실기기에서 사파리 → 홈 화면 복구.** 이 기능을 만든 이유인데 아직 확인하지 못했다. 코드 리뷰로는 답이 안 나온다. **배포 없이 터널 주소로도 확인할 수 있다.**

---

## 7. 참고

1차 요청서에 **기능 목적·서버 구조·보안 규칙·의도된 동작 목록**이 있다. 앱 전체 규칙은 `CLAUDE.md`.
🔴 이 문서도 내부 문서다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 했다.
