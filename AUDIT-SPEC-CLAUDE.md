# 하딜고고 전수조사 요청서 (공통 기준)

> **클로드와 코덱스가 이 문서 하나만 보고 각자 독립으로 조사한다.**
> 🔴 **상대 보고서를 보기 전에** 자기 보고서를 끝낼 것. 그래야 대조에 뜻이 있다.
> 🔴 **저장소 파일은 읽기만 하고 수정하지 않는다.**
>
> 대상 커밋 `3c28840`(main, 배포 완료) · css v624 · script v530
> 규모: `script.js` 4,487줄 · `styles.css` 4,741줄 · `index.html` 912줄 (총 약 10,100줄)
> 결과 파일: 클로드 → `AUDIT-CLAUDE.md` · 코덱스 → `AUDIT-CODEX.md`

---

## 1. 왜 하는가

「내 데이터 코드」를 만들며 교차 검증을 세 번 했고 **세 번 다 실제 결함이 나왔다.** 매번 "고친 수정이 새 구멍을 만드는" 형태였다. 그 기능만 세 번 본 셈이라, **나머지 앱은 아무도 같은 잣대로 본 적이 없다.**

목적은 점수가 아니라 **놓친 것 찾기**다. 두 조사가 겹치면 확신이 생기고, 갈리면 거기가 진짜 애매한 지점이다.

---

## 2. 🔴 조사 방법 — 이게 이 문서의 핵심

**훑어보고 "없음"이라고 쓰지 말 것.** 검색(grep)만으로는 "없다"를 증명할 수 없다.

### 2-1. 커버리지 요구
- `script.js`의 **모든 함수를 한 번은 눈으로 읽는다.** 읽은 범위를 **줄 번호 구간**으로 보고서에 적는다(예: `1–420`, `421–890`, …). 구간의 합이 4,487줄을 덮어야 한다.
- `index.html`은 **모든 `id`와 이벤트 대상**을 확인한다.
- `styles.css`는 전량 정독하지 않아도 된다. 대신 **결함 기준으로만** 본다 — `[hidden]`과 `display` 충돌, `z-index` 겹침, 폭 넘침, `position` 기준 깨짐. 취향(색·간격)은 범위 밖.

### 2-2. "없음" 판정의 조건
구역에 결함이 없다고 쓰려면 **무엇을 어떻게 봤는지** 근거를 함께 적는다. 예: "`renderGrid`·`getFiltered`·`byPopular` 정독, 필터 4종 조합을 브라우저에서 실제로 전환해 확인".
근거 없이 "없음"만 적힌 구역은 **조사하지 않은 것으로 친다.**

### 2-3. 확신도를 심각도와 분리해서 적는다
- `확인함` — 실제로 재현하거나 실행해서 봤다
- `읽음` — 코드 근거는 명확하나 실행은 안 했다
- `추정` — 근거가 정황뿐이다 → **심각도를 한 단계 낮춘다**

---

## 3. 조사 범위

라이브에 올라가 있는 것 전부. 아래 구역을 **모두** 다룬다.

| 구역 | 내용 |
|---|---|
| **A. 데이터·저장** | `localStorage` 키 전부, Firebase `likes`·`sync`, 저장 실패·손상값·용량 초과·다중 탭 |
| **B. 내 데이터 코드** | 코드 생성·입력·트랜잭션·합치기·적용·화면(띠·시트) |
| **C. 스티커 탭** | 기록 추가/수정/삭제, 목록·보기 모달, 스티커 그림 매칭, 카드 캐시 |
| **D. 레시피 화면** | 홈 섹션·전체보기·검색·즐겨찾기·좋아요·정렬·순위 배지·상세 모달·가챠 |
| **E. 스토리 뷰어** | 셀럽 이동·자동재생·탭 좌표 판정·닫기·배경 잠금 |
| **F. 매장·메뉴·배너** | 매장 목록·오픈예정 처리, 메뉴 빈 화면, 배너 캐러셀·칼럼 패널 |
| **G. 전역** | 탭 전환, 스크롤 잠금, 캐시 버전, 공유, 스플래시, 정적 아티클(guide/hotpot/cilantro), SEO 메타 |

**범위 밖(지적하지 말 것)**
- 디자인 취향(색·간격·문구 어감)
- `REVIEW-SYNC-CODE.md` 3절 **「의도된 동작」** — 다만 그 판단이 틀렸다고 보면 근거를 적어 **별도 표시**
- 레시피 내용(재료·설명 값)의 사실 여부
- Firebase 콘솔 설정(저장소 밖)
- 성능 최적화 제안(실제 고장이 아닌 것)
- 이미 문서에 "고치지 않기로 함"이라 적힌 항목 — 반론은 별도 표시

---

## 4. 특히 의심할 것 (지금까지 이 프로젝트에서 실제로 터진 유형)

새 결함은 대개 **옛 전제가 깨진 자리**에서 나온다. 아래는 실제 사고 목록이다.

1. **주석이 단언하는 전제가 아직 참인가.** 예: "여기 들어오는 이름은 항상 목록에 있다" 같은 문장. 기능이 늘면서 깨졌을 수 있다.
2. **`el.hidden = true`인데 CSS가 `display:flex`** → 안 숨겨진다. 이 저장소에서 **네 번** 걸렸다.
3. **DOM 캐시(id→노드) 재사용** → 데이터가 바뀌어도 옛 화면이 남는다.
4. **`localStorage` 저장 실패를 조용히 삼킴** → 화면엔 있는데 새로고침하면 없다.
5. **`undefined`가 섞인 값을 Firebase에 쓰기** → 그 자리에서 예외가 나고 뒤 코드가 통째로 안 돈다.
6. **비동기 응답이 늦게 도착해 새 상태를 덮음.**
7. **집계(전체 공유 숫자)와 내 표시를 섞기** → 좋아요 숫자가 튄다.
8. **캐시 버전(`?v=`)을 안 올림** → 모바일만 옛 파일을 쓴다.
9. **iOS 사파리와 홈 화면 앱의 저장소가 분리됨** → 데이터가 없어 보인다.
10. **정적 아티클(guide/hotpot/cilantro)이 앱 본문과 어긋남** → 생성기를 안 돌리면 옛 글이 남는다.

---

## 5. 심각도 기준 (양쪽 동일하게)

| 등급 | 뜻 |
|---|---|
| **S1** | **데이터 유실·노출.** 사용자가 만든 것이 사라지거나 남에게 보인다 |
| **S2** | **기능 고장.** 눌러도 안 되거나 잘못된 결과가 나온다 |
| **S3** | **오작동·혼란.** 동작은 하지만 화면과 실제가 다르거나 오해를 부른다 |
| **S4** | **사소.** 낭비·죽은 코드·아주 드문 조건에서만 어색함 |

---

## 6. 보고 형식 (반드시 이대로 — 대조를 위해)

각 항목에 **구역 문자 + 일련번호**로 ID를 붙인다(예: `B-3`). 대조할 때 이 ID로 짝을 맞춘다.

```
### [B-3][S1][확인함] 한 줄 요약
- 파일: script.js:1234, index.html:56 (관련된 곳 전부)
- 증상: 무엇이 잘못되는가
- 원인: 왜 그렇게 되는가
- 재현: 1) … 2) … 3) …
- 영향 범위: 누가 언제 겪는가 (모든 사용자 / 두 기기 쓰는 사람 / 저장소가 가득 찬 경우 …)
```

보고서 끝에 **두 표**를 붙인다.

**표1 — 구역별 요약**

| 구역 | 발견 수 | 최고 심각도 | 읽은 줄 범위 | 확인 방법 |
|---|---|---|---|---|

**표2 — 전체 목록** (ID · 심각도 · 확신도 · 한 줄 요약)

---

## 7. 검증 시 주의 (전부 실제로 당한 것)

1. 🔴 테스트용 코드에 **`O`·`I`·`0`·`1`을 쓰지 말 것.** 형식 검사에서 거부된다. **`A-H · J-N · P-Z · 2-9`** 만(예: `HG-TESTA2`).
2. 🔴 붙여넣기는 **`input.value` 직접 대입으로 테스트하지 말 것.** `maxlength`·정리 로직을 우회해 **있는 버그를 없다고 판정하게 된다.** 실제 경로(`document.execCommand('insertText', …)`)를 쓸 것.
3. 🔴 **좋아요 하트를 함부로 누르지 말 것** — 라이브 집계가 실제로 바뀐다. 눌렀으면 되돌리고 숫자가 원래대로인지 확인할 것.
4. 🔴 `localStorage`만 비우고 **새로고침을 안 하면 화면이 옛 상태를 그대로 들고 있다.** 상태를 바꿨으면 새로고침할 것 — 이것 때문에 없는 버그를 봤다고 오판한 적이 있다.
5. 🔴 화면에 뭐가 보인다/안 보인다를 말하기 전에 **그 클래스를 붙이는 코드가 있는지 확인**할 것. CSS 규칙이 있다고 화면에 나오는 게 아니다.
6. ⚠️ 검증에 쓴 `sync/<코드>`는 **빈 값으로 덮어** 비울 것(규칙상 통째 삭제는 막혀 있다).
7. ⚠️ 브라우저 콘솔은 배경에서 **타이머가 늘어진다.** 타이밍 항목은 넉넉히 기다리고 **시간 측정값 자체는 믿지 말 것.**
8. ⚠️ 로컬 서버는 파일이 즉시 도착한다. **로딩 순서·시간차 문제는 로컬에서 재현되지 않는다.**

---

## 8. 제출 전 자기 점검

- [ ] `script.js` 4,487줄을 덮는 줄 범위를 적었는가
- [ ] 모든 구역(A~G)에 근거가 있는가 — "없음"에도 근거를 적었는가
- [ ] 각 항목에 ID·심각도·확신도가 다 있는가
- [ ] 재현 경로 없는 항목에 `추정`을 붙이고 심각도를 낮췄는가
- [ ] 범위 밖 항목(취향·의도된 동작)을 결함으로 올리지 않았는가
- [ ] 검증에 쓴 서버 데이터를 비웠는가, 좋아요 숫자를 되돌렸는가
- [ ] **상대 보고서를 보지 않았는가**

---

## 9. 배경 문서

- `REVIEW-SYNC-CODE.md` — 동기화 기능의 목적·서버 구조·보안 규칙·**불변식 D1~D8**·의도된 동작 목록
- `REVIEW-SYNC-CODE-2.md` / `-3.md` / `-4.md` — 1·2·3차 수정 내역과 그때 생긴 위험
- `CLAUDE.md` — 앱 전체 규칙(배포·스크롤바·이미지 규격·데이터 표기)
- `HANDOVER.md` — 진행 상황

---

## 10. 이후 절차

1. 클로드·코덱스가 **각자** 조사해 `AUDIT-CLAUDE.md` / `AUDIT-CODEX.md`를 낸다.
2. 둘 다 나온 뒤 **코덱스가 대조**한다 — 둘 다 발견 / 클로드만 / 코덱스만 / 판단이 갈린 것 / 실제 재현으로 확정할 것.
3. 대조 결과를 보고 사용자가 무엇을 고칠지 정한다.

🔴 이 문서와 두 보고서 모두 **내부 문서**다. `_config.yml`의 `exclude`에 넣어 웹에 안 나가게 한다.
